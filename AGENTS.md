# Repository Instructions

이 저장소 전체에 적용되는 작업 규칙입니다.

## Writing Rules

- 이모지, 이모티콘, 장식용 유니코드 문자를 사용하지 않는다.
- 설명은 한국어를 기본으로 작성한다.
- 파일명, 코드 식별자, 기술 용어는 필요한 경우 영어를 유지한다.
- 불필요하게 장황하게 작성하지 않는다.
- 사용자가 전체 파일을 요청하면 일부 패치가 아니라 완전한 파일 전체를 제공한다.
- 문서와 코드에는 복사 및 재사용이 쉬운 구조를 우선한다.
- 처음 나오는 전문 용어는 짧게 뜻을 풀어 설명한다.

## Working Style

- 한 번에 한 단계씩 진행한다. 한 단계의 결과를 확인한 뒤 다음 단계로 넘어간다.
- 새 단계, 새 도구, 새 작업 방식을 시작할 때는 먼저 다음 내용을 짧게 설명한다.
  1. 지금 할 일
  2. 왜 이 작업을 하는지
  3. 실제로 무엇을 실행하거나 수정할지
  4. 실행 후 무엇이 바뀌었는지
- 사소한 한 줄 수정마다 위 형식을 반복하지 않는다.
- 사용자가 직접 해야 하는 일과 Agent가 대신 할 수 있는 일을 구분해 안내한다.
- 버튼, 메뉴, 명령어, 파일 경로를 안내할 때는 확인한 정보만 말한다. 확인하지 못한 화면 요소는 지어내지 않는다.
- 결과물을 만든 뒤에는 사용자가 직접 확인할 수 있는 방법을 함께 안내한다.
- 실행하지 않은 작업이나 확인하지 않은 결과를 완료했다고 표현하지 않는다.

## Git Workflow

- 의미 있는 구현 단계가 끝나고 관련 검증이 통과하면 커밋하고 원격 기능 브랜치에 푸시한다.
- 작은 단계마다 Pull Request를 만들지 않는다.
- 여러 구현 단계를 하나의 검토 가능한 MVP 묶음으로 모은 뒤 Pull Request 하나를 만든다.
- Pull Request를 만들기 전에는 관련 테스트와 문서 갱신 여부를 다시 확인한다.

## Reasoning and Decision Transparency

- 내부의 상세한 사고 과정을 그대로 노출하는 대신, 사용자가 작업을 이해할 수 있도록 판단 근거를 요약한다.
- 중요한 결정에는 다음 내용을 필요한 만큼 기록한다.
  - 확인한 사실
  - 현재 가정
  - 선택한 방법
  - 다른 대안이 있었는지
  - 선택 이유
  - 아직 확인되지 않은 점
- 사실, 추측, 권고를 서로 구분한다.
- 여러 방법이 가능할 때는 가능한 선택지를 간단히 비교한 뒤 현재 프로젝트에 가장 적합한 방법을 선택한다.
- 결과가 예상과 다르면 이전 판단을 고집하지 않고 실제 관찰 결과를 기준으로 수정한다.

## Development Rules

- 처음부터 완성형 서비스를 만들지 않는다.
- 최소 동작 버전부터 만들고 실제 결과를 확인한 뒤 기능을 하나씩 추가한다.
- 한 번에 여러 기능을 동시에 추가하지 않는다.
- 각 단계마다 입력, 처리, 출력, 완료 기준을 명확히 한다.
- 오류가 발생하면 전체 구조를 다시 만들기보다 실패 지점을 먼저 분리한다.
- 기존에 정상 동작하는 기능은 명확한 이유 없이 변경하지 않는다.
- 실제 사용자에게 도움이 되는지 확인되지 않은 기능은 우선순위를 낮춘다.
- 오류가 발생하면 관찰한 사실과 원인 가설을 구분하고, 실제 오류 메시지와 로그를 먼저 확인한다.
- 가능한 경우 작은 재현 테스트를 만든 뒤 수정한다.

## Career Analysis Rules

- 직무 적합도를 성격검사 하나만으로 판단하지 않는다.
- 실제 경력, 프로젝트, 행동 사례를 가장 중요한 근거로 본다.
- 기술 역량은 학습 여부가 아니라 실제 사용 수준과 증거를 함께 기록한다.
- 성격검사, 적성검사, 강점검사는 보조 자료로 활용한다.
- 적합도 점수만 제공하지 말고 판단 근거를 함께 기록한다.
- 부족한 역량을 발견했을 때 새 프로젝트를 무조건 추천하지 않는다.
- 기존 프로젝트를 확장하여 부족 역량을 증명할 수 있는지 먼저 검토한다.
- 지원 여부는 절대적인 합격 가능성 예측이 아니라 현재 프로파일과 채용공고의 비교 결과로 표현한다.
- 채용공고 원문에서 확인한 요구사항과 Agent가 해석한 내용을 구분한다.
- 개인 프로파일에서 확인되지 않은 경험이나 기술을 임의로 보유했다고 가정하지 않는다.

## Data and Privacy Rules

- API Key, Access Token, 비밀번호, Webhook URL 등 비밀정보를 커밋하지 않는다.
- 주민등록번호, 상세 주소, 개인 연락처 등 불필요한 개인정보를 저장하지 않는다.
- 공개 저장소에 올리기 부적절한 개인 자료는 원문 대신 구조화된 요약이나 비식별 데이터 사용을 우선한다.
- 외부 채용사이트 데이터를 수집할 경우 이용약관, robots 정책, 공개 API 제공 여부를 먼저 확인한다.
- 개인 자기이해 보고서, 이력서 원본, 지원 회사별 메모 등 민감할 수 있는 자료는 공개 저장소에 원문으로 올리지 않는다.

## File Preservation Rules

- 기존 결과물과 원본 자료를 이유 없이 덮어쓰지 않는다.
- 새 분석 결과나 리포트는 가능한 경우 날짜와 버전을 포함한 새 파일로 저장한다.
- 동일한 이름의 결과물이 이미 있으면 새 버전 이름을 사용한다.
- 기존 파일을 반드시 덮어써야 하는 작업은 사용자가 명시적으로 요청했거나 사전에 합의된 갱신 파일에만 수행한다.
- README, PRD, 스키마처럼 지속적으로 갱신하기로 정한 문서는 별도 승인 없이 갱신할 수 있다.
- 원본 입력 자료와 생성 결과물을 구분한다.

## Work Log Rules

- 저장소 최상위의 `작업일지.md` 하나를 프로젝트 전체 작업일지로 유지한다.
- 작업을 시작할 때 기존 `작업일지.md`를 확인한다. 없으면 만든다.
- 의미 있는 단계가 끝났을 때 기존 기록을 보존하고 새 기록을 아래 형식으로 덧붙인다.
  - 날짜
  - 목표
  - 수행한 작업
  - 생성 또는 수정한 파일
  - 확인된 결과
  - 아직 확인하지 못한 점
  - 막힌 점
  - 주요 판단과 이유
  - 다음 할 일
- 실제로 수행하지 않은 작업은 완료로 기록하지 않는다.
- 사용자가 직접 확인하지 않은 결과는 `사용자 확인 완료`로 적지 않는다.
- 비밀번호, API Key, 개인 연락처, 지원서 원문 같은 민감정보는 작업일지에 기록하지 않는다.
- 별도 `todo.md`, `task.md`, 진행 로그 파일을 만들기보다 진행 기록은 `작업일지.md`에 통합한다.
- 작업일지는 지속 갱신 파일이므로 갱신할 때마다 덮어쓰기 허락을 다시 묻지 않는다.

## Documentation Rules

- 주요 결정은 `docs/`에 기록한다.
- README는 프로젝트 목적, 현재 상태, 실행 방법을 중심으로 유지한다.
- 구현 전에 문서의 범위와 완료 기준을 먼저 확인한다.
- 완료된 기능과 계획 중인 기능을 명확히 구분한다.
- 중요한 설계 변경이 생기면 기존 문서를 조용히 바꾸기보다 변경 이유가 드러나도록 작업일지에도 남긴다.
