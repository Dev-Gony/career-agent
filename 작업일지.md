# 작업일지

이 파일은 `career-agent` 프로젝트의 전체 진행 기록을 한 곳에서 관리합니다.

## 2026-09-13

### 목표

개인용 취업 준비 AI Agent의 방향을 정하고, 구현 전에 필요한 문서와 작업 규칙을 준비한다.

### 수행한 작업

- 개인용 Career Agent의 초기 범위를 정의했다.
- 직무 적합도, 기술 격차, 학습 추천, 포트폴리오 추천, 지원 판단을 핵심 기능으로 정했다.
- 처음부터 채용사이트 자동 수집을 구현하지 않고, 채용공고 수동 입력 분석부터 검증하기로 했다.
- 저장소 전체 작업 규칙을 `AGENTS.md`에 정리했다.
- 제품 기획 문서 `docs/PRD.md`를 작성했다.
- 사용자 프로파일 구조 문서 `docs/USER_PROFILE_SCHEMA.md`를 작성했다.
- 작업 과정과 판단 근거를 계속 남길 수 있도록 이 작업일지를 만들었다.
- `data/user_profile.example.json`을 작성해 사용자 프로파일 구조를 실제 JSON 데이터 형태로 구현했다.
- 공개 저장소에 사용할 수 있도록 예제 프로파일은 비식별 형태로 구성했다.
- 기술 역량은 단순 보유 여부가 아니라 `learning`, `project`, `work` 같은 수준과 근거를 함께 저장하도록 했다.
- 경력, 프로젝트, 행동 사례, 검사 결과, 업무 선호, 학습 선호, 목표 직무를 한 프로파일 안에서 함께 관리하도록 구성했다.
- 채용공고의 표준 구조를 정의한 `docs/JOB_POSTING_SCHEMA.md`를 작성했다.
- 채용공고 예제 데이터 `data/job_posting.example.json`을 추가했다.
- 채용공고에서 필수 조건과 우대 조건, 기술, 경력, 근무 정보, 출처를 분리해 저장하도록 했다.
- 공고 분석 시 원문에서 확인한 사실, Agent의 해석, 아직 확인할 수 없는 내용을 별도로 기록하도록 했다.

### 생성 또는 수정한 파일

- `AGENTS.md`
- `README.md`
- `docs/PRD.md`
- `docs/USER_PROFILE_SCHEMA.md`
- `docs/JOB_POSTING_SCHEMA.md`
- `data/user_profile.example.json`
- `data/job_posting.example.json`
- `작업일지.md`

### 확인된 결과

- GitHub 저장소 `Dev-Gony/career-agent`가 생성되어 있다.
- 저장소는 공개 상태다.
- `AGENTS.md`, `README.md`, `docs/PRD.md`, `docs/USER_PROFILE_SCHEMA.md`가 저장소에 존재한다.
- `data/user_profile.example.json`이 저장소에 추가되었다.
- 사용자 프로파일을 JSON으로 표현할 수 있는 첫 데이터 형식이 생겼다.
- 공개 예제에는 주민등록번호, 상세 주소, 연락처, 비밀번호 같은 민감정보를 포함하지 않았다.
- `docs/JOB_POSTING_SCHEMA.md`가 추가되어 채용공고의 표준 입력 구조가 정의되었다.
- `data/job_posting.example.json`이 추가되어 채용공고 구조를 실제 JSON 형태로 확인할 수 있게 되었다.
- 사용자 데이터와 채용공고 데이터 양쪽의 기본 구조가 준비되었다.
- 프로젝트는 아직 핵심 매칭 로직 구현 전의 데이터 및 문서 설계 단계다.

### 아직 확인하지 못한 점

- 실제 채용공고를 입력했을 때 분석 결과가 취업 준비에 충분히 유용한지 아직 검증하지 않았다.
- 직무 적합도 점수 계산 방식은 아직 확정하지 않았다.
- 공개 예제와 별도로 실제 개인 프로파일을 어떤 방식으로 비공개 관리할지는 아직 확정하지 않았다.
- 사람인, 잡코리아, 인크루트, 원티드 등 채용사이트의 자동 수집 방식은 아직 조사하지 않았다.
- 현재 기술 수준 값이 실제 채용공고 비교에서 충분히 세밀한지는 아직 테스트하지 않았다.
- 채용공고 직무명을 어떤 기준으로 표준화할지는 아직 확정하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 범용 서비스를 처음부터 만들지 않고 본인 전용 Agent부터 시작한다.
  - 이유: 실제 사용자 문제를 빠르게 검증하고 범위를 줄이기 위해서다.
- 채용공고 자동 수집보다 공고 분석 기능을 먼저 만든다.
  - 이유: 수집 기능보다 분석 결과가 실제로 유용한지 확인하는 것이 우선이기 때문이다.
- 실제 경력과 프로젝트를 직무 적합도 판단의 가장 중요한 근거로 사용한다.
  - 이유: 성격검사만으로 직무 적합성을 판단하지 않기 위해서다.
- 부족한 기술이 발견돼도 새로운 프로젝트를 무조건 만들지 않는다.
  - 이유: 기존 프로젝트 확장으로 같은 역량을 증명할 수 있다면 포트폴리오의 깊이를 높이는 편이 더 효율적이기 때문이다.
- 상세한 내부 사고 과정을 저장하는 대신, 확인한 사실과 선택한 방법, 대안, 선택 이유, 미확인 사항을 기록한다.
  - 이유: 작업자가 판단 과정을 이해할 수 있으면서도 불필요하게 장황한 내부 추론 기록을 만들지 않기 위해서다.
- 실제 개인 데이터 파일보다 비식별 예제 파일을 먼저 만들었다.
  - 이유: 공개 저장소의 구조를 먼저 검증하면서 개인정보 노출 위험을 줄이기 위해서다.
- 프로파일의 핵심 항목마다 가능한 경우 근거를 연결한다.
  - 이유: Agent가 추상적인 자기평가보다 실제 경력과 프로젝트 증거를 우선하도록 만들기 위해서다.
- 채용공고도 원문 그대로만 저장하지 않고 비교 가능한 구조로 분해한다.
  - 이유: 필수 역량, 우대 역량, 경력 조건, 기술 스택을 사용자 프로파일과 일관되게 비교하기 위해서다.
- 공고의 사실과 Agent의 해석을 분리한다.
  - 이유: 공고에 없는 내용을 사실처럼 판단하는 오류를 줄이기 위해서다.

### 다음 할 일

1. `docs/MATCHING_RULES.md`에서 직무 적합도와 기술 격차 판단 기준을 정의한다.
2. 사용자 프로파일과 채용공고의 비교 결과 형식을 설계한다.
3. 실제 개인 프로파일을 공개 저장소와 분리해 관리할 방식을 결정한다.
4. 실제 채용공고 1개를 수동으로 입력해 첫 분석 결과를 검증한다.
5. 첫 분석 결과를 보고 점수 계산 방식과 추천 문구를 조정한다.

## 2026-09-13 - 개인정보 보호 준비

### 목표

실제 개인 프로파일을 작성하기 전에 공개 저장소에 개인 데이터와 비밀정보가 커밋되지 않도록 기본 보호 장치를 준비한다.

### 수행한 작업

- 저장소에 `.gitignore`가 없음을 확인했다.
- 로컬 전용 개인 데이터 경로인 `private-data/`를 Git 제외 대상으로 추가했다.
- `.env`, `.env.*`, `*.local.json`을 Git 제외 대상으로 추가했다.
- 공유 가능한 환경변수 예제 파일인 `.env.example`은 추적할 수 있도록 예외 처리했다.

### 생성 또는 수정한 파일

- `.gitignore`
- `작업일지.md`

### 확인된 결과

- 실제 개인 데이터와 일반적인 로컬 비밀정보 파일을 공개 저장소에서 분리할 기본 규칙이 마련되었다.

### 아직 확인하지 못한 점

- 실제 개인 프로파일 파일명과 세부 저장 구조는 아직 정하지 않았다.
- 운영 단계에서 사용할 API별 환경변수 이름은 아직 정하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 실제 개인 데이터를 만들기 전에 Git 제외 규칙을 먼저 추가했다.
  - 이유: 실수로 민감한 자료를 공개 저장소에 커밋할 가능성을 줄이기 위해서다.
- `.env.example`은 제외하지 않았다.
  - 이유: 비밀값 없이 필요한 설정 이름만 공유하는 파일은 향후 실행 방법을 문서화하는 데 필요하기 때문이다.

### 다음 할 일

1. `docs/MATCHING_RULES.md`에서 첫 매칭 규칙과 완료 기준을 정의한다.
2. 분석 결과 데이터 구조를 정의한다.

## 2026-09-13 - 첫 매칭 규칙 정의

### 목표

사용자 프로파일과 채용공고를 비교하는 첫 MVP의 판단 규칙과 완료 기준을 구현 전에 확정한다.

### 수행한 작업

- 필수 조건, 우대 조건, 주요 업무와 지원 가능 조건을 별도 비교 단위로 정의했다.
- 증거 우선순위와 증거 직접성 분류를 정의했다.
- `strong_match`, `match`, `partial`, `gap`, `unknown`의 사용 조건을 구체화했다.
- 프로파일에 기술이 없다는 이유만으로 `gap`으로 처리하지 않도록 규칙을 정했다.
- 적합도 백분율 없이 사용할 첫 지원 판단 기준을 정의했다.
- 현재 예제 공고에 대한 기대 판정을 기록했다.
- 첫 매칭 기능의 완료 기준을 정의했다.

### 생성 또는 수정한 파일

- `docs/MATCHING_RULES.md`
- `작업일지.md`

### 확인된 결과

- 구현 전에 요구사항별 판정과 지원 판단에 사용할 기준이 문서화되었다.
- 필수 조건과 우대 조건을 같은 중요도로 처리하지 않는 기준이 마련되었다.
- 확인된 부족과 정보 부족을 구분할 수 있게 되었다.

### 아직 확인하지 못한 점

- 실제 채용공고에서도 현재 분류만으로 충분한지 확인하지 않았다.
- 기술 별칭과 동등 경험 목록은 아직 정의하지 않았다.
- 분석 결과 JSON 구조는 아직 정의하지 않았다.
- 적합도 백분율이 향후 필요한지는 실제 공고 검증 후 결정해야 한다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 초기 MVP에서는 적합도 백분율을 사용하지 않는다.
  - 이유: 실제 공고 평가 데이터 없이 만든 숫자가 정확한 합격 가능성처럼 오해될 수 있기 때문이다.
- `gap`은 부족이 확인된 경우에만 사용하고, 정보가 없으면 `unknown`으로 처리한다.
  - 이유: 프로파일의 누락을 실제 역량 부족으로 잘못 판단하지 않기 위해서다.
- 역량 적합도와 지역, 경력, 자격 같은 지원 가능 조건을 분리한다.
  - 이유: 기술적으로 잘 맞는 것과 실제 지원 가능한 것은 서로 다른 판단이기 때문이다.

### 다음 할 일

1. 매칭 규칙을 표현하는 분석 결과 스키마를 정의한다.
2. 현재 예제 데이터에 대한 예상 분석 결과를 작성한다.

## 2026-09-13 - 분석 결과 스키마 정의

### 목표

첫 매칭 규칙의 결과를 일관되게 저장하고 판단 근거를 추적할 수 있는 출력 구조를 정의한다.

### 수행한 작업

- 분석 결과의 최상위 구조와 첫 MVP 필수 필드를 정의했다.
- 요구사항별 공고 근거, 판정, 사용자 증거, 미확인 사항과 다음 행동을 저장하는 구조를 정의했다.
- 역량 적합도와 실제 지원 가능 조건을 분리했다.
- 강점, 확인된 부족, 정보 부족, 학습 및 포트폴리오 추천 구조를 정의했다.
- 지원 판단의 이유, 주의사항과 다음 행동을 저장하도록 했다.
- 규칙 버전과 사용자 검토 상태를 기록하도록 했다.

### 생성 또는 수정한 파일

- `docs/MATCH_RESULT_SCHEMA.md`
- `작업일지.md`

### 확인된 결과

- 적합도 백분율 없이도 요구사항별 판정과 최종 지원 판단을 표현할 수 있는 구조가 마련되었다.
- 공고 근거와 사용자 증거를 결과에서 함께 추적할 수 있게 되었다.
- 사용자가 검토하지 않은 결과를 승인된 결과처럼 기록하지 않도록 상태 값이 정의되었다.

### 아직 확인하지 못한 점

- 실제 JSON 예제가 아직 없어 모든 필드 조합을 검증하지 않았다.
- 현재 입력 배열에는 항목별 안정적인 ID가 없어 MVP에서는 위치 기반 참조를 사용해야 한다.
- 자동 수집 공고의 내용 해시와 수집 메타데이터는 아직 입력 스키마에 반영하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 첫 결과에서는 점수 대신 요구사항별 분류와 근거를 저장한다.
  - 이유: 실제 검증 전의 숫자보다 사용자가 직접 확인할 수 있는 증거가 중요하기 때문이다.
- `confidence`는 적합도 점수가 아니라 입력과 증거의 명확성으로 정의했다.
  - 이유: 판단 확실성과 직무 적합성을 혼동하지 않기 위해서다.
- 입력 항목 ID가 없는 현재 단계에서는 `source_index`와 `source_path`를 임시 참조로 사용한다.
  - 이유: 입력 스키마를 동시에 크게 변경하지 않고 결과 구조부터 검증하기 위해서다.

### 다음 할 일

1. 현재 예제 입력에 맞는 `data/match_result.example.json`을 작성한다.
2. 예제 결과가 매칭 규칙과 결과 스키마를 모두 만족하는지 검토한다.

## 2026-09-13 - 첫 매칭 기대 결과 작성

### 목표

현재 예제 사용자 프로파일과 예제 채용공고를 기준으로 향후 구현이 재현해야 할 기대 결과를 작성한다.

### 수행한 작업

- 필수 조건 3개와 우대 조건 3개의 예상 판정을 작성했다.
- 주요 업무 4개를 사용자 프로젝트와 행동 증거에 연결했다.
- 경력, 학력, 고용 형태와 지역 조건을 역량 적합도와 별도로 평가했다.
- 확인된 부족과 정보 부족을 구분했다.
- Docker와 AWS는 보유 여부를 단정하지 않고 `unknown`으로 기록했다.
- 첫 지원 판단과 판단 근거, 주의사항, 다음 확인 행동을 작성했다.

### 생성 또는 수정한 파일

- `data/match_result.example.json`
- `작업일지.md`

### 확인된 결과

- 현재 예제 입력에서 필수 조건 3개는 모두 `strong_match`로 예상된다.
- LLM API 우대 조건은 `strong_match`, Docker와 AWS는 `unknown`으로 예상된다.
- 확인된 필수 역량 부족이나 지원 불가 조건은 없어 기대 지원 판단은 `적극 지원`이다.
- 이 파일은 실행 결과가 아니라 구현 검증용 기대 결과다.
- 예제 결과 JSON이 정상적으로 파싱된다.
- 결과의 사용자 및 공고 ID가 입력 예제와 일치한다.
- 필수 조건과 우대 조건의 판정 배열이 요약 집계와 일치한다.

### 아직 확인하지 못한 점

- 사용자가 기대 판정과 지원 판단을 직접 검토하지 않았다.
- 실제 채용공고에서도 같은 규칙이 유용한지 확인하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- Docker와 AWS를 `gap`이 아니라 `unknown`으로 기록했다.
  - 이유: 현재 프로파일에 보유 여부를 확인할 정보가 없기 때문이다.
- 정보가 부족한 기술에 대해 즉시 학습 추천을 만들지 않았다.
  - 이유: 사용 경험을 먼저 확인한 뒤 실제 부족이 확인될 때 보완 계획을 만드는 편이 정확하기 때문이다.
- 주요 업무의 모니터링 항목은 `match`로 판정했다.
  - 이유: 상태 관리와 재시도 증거는 있지만 운영 모니터링 체계 전체를 직접 증명하지는 못하기 때문이다.

### 다음 할 일

1. 사용자에게 기대 결과의 판단이 실제 경험과 맞는지 검토를 요청한다.
2. 사용자 확인 후 입력 항목의 안정적인 ID 추가 여부를 결정한다.
3. 검증된 기대 결과를 재현하는 최소 비교 기능의 구현 범위를 확정한다.

## 2026-09-13 - 사용자 기술 경험 및 모니터링 근거 반영

### 목표

사용자가 확인한 Docker, AWS 경험과 Tech News Automation의 모니터링 수준을 프로파일과 기대 분석 결과에 반영한다.

### 수행한 작업

- Docker는 설치만 했고 실제 사용 경험이 없음을 확인했다.
- AWS는 CLI 화면을 확인한 적만 있고 실제 서비스 사용 경험이 없음을 확인했다.
- Tech News Automation 원격 저장소의 GitHub Actions 워크플로와 관련 코드를 읽어 실행 상태와 로그 범위를 확인했다.
- 사용자 기술 수준에 `none`과 `exposure`를 추가해 정보 부족과 확인된 경험 부족을 구분했다.
- Docker와 AWS를 `unknown`에서 우대 조건 수준의 `gap`으로 변경했다.
- Tech News Automation에 GitHub Actions 실행 상태 및 로그 확인 역량을 추가했다.
- 기대 결과의 집계, 부족 역량, 학습 및 포트폴리오 추천을 갱신했다.

### 생성 또는 수정한 파일

- `docs/USER_PROFILE_SCHEMA.md`
- `docs/MATCHING_RULES.md`
- `docs/MATCH_RESULT_SCHEMA.md`
- `data/user_profile.example.json`
- `data/match_result.example.json`
- `작업일지.md`

### 확인된 결과

- Docker와 AWS 경험은 정보 부족이 아니라 실제 사용 경험 부족으로 확인되었다.
- 두 기술은 예제 공고의 우대 조건이므로 현재 지원 판단을 막지 않는다.
- Tech News Automation은 GitHub Actions에서 실행 성공과 실패 상태 및 단계별 로그를 확인할 수 있다.
- 워크플로에는 30분 제한, Gemini 오류 재시도, Slack 전송 실패 시 작업 실패 처리가 있다.
- 개별 블로그 수집 및 요약 실패는 로그를 남긴 뒤 계속 진행하므로 실행 성공 상태만으로 모든 소스의 성공을 보장할 수는 없다.
- 변경된 예제 JSON이 정상적으로 파싱된다.
- 우대 조건 판정 집계와 결과 배열이 일치하며 입력 참조 인덱스가 유효하다.

### 아직 확인하지 못한 점

- GitHub 기본 알림 설정에서 워크플로 실패 알림을 실제로 수신하도록 설정되어 있는지는 확인하지 않았다.
- Docker 보완이 실제 채용공고 여러 건에서도 높은 우선순위인지 확인하지 않았다.
- AWS 학습 우선순위는 실제 공고 요구 빈도를 확인한 뒤 결정해야 한다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 설치 또는 화면 확인 수준은 `exposure`로 기록한다.
  - 이유: 직접 사용 경험은 없지만 완전히 접하지 않은 상태와 구분하기 위해서다.
- 실제 사용 경험을 요구하는 조건에서 `exposure`는 `partial`이 아니라 `gap`으로 처리한다.
  - 이유: 설치나 화면 확인만으로는 요구 역량의 일부를 수행했다는 증거가 되지 않기 때문이다.
- Docker는 기존 Tech News Automation 확장 후보로 기록하고 AWS는 바로 학습 과제로 만들지 않는다.
  - 이유: 기존 프로젝트로 Docker 경험을 증명할 수 있지만 AWS는 실제 공고의 반복 요구를 먼저 확인하는 편이 효율적이기 때문이다.
- GitHub Actions 상태와 로그 확인은 기본 모니터링 증거로 인정하되 완전한 운영 모니터링으로 과장하지 않는다.
  - 이유: 실행 단위 상태와 로그는 확인할 수 있지만 소스별 실패 요약과 별도 능동 알림은 구현되어 있지 않기 때문이다.

### 다음 할 일

1. 변경된 기대 결과를 기준으로 최소 비교 기능의 구현 범위를 확정한다.
2. 입력 항목을 안정적으로 참조하기 위한 ID 구조를 추가한다.
3. 첫 자동 공고 발견 기능에 사용할 인크루트 RSS 입력 형식을 설계한다.

## 2026-09-13 - 입력 및 결과 항목의 안정적인 ID 연결

### 목표

배열 순서가 바뀌어도 매칭 결과의 공고 조건과 사용자 근거가 같은 항목을 계속 가리키도록 입력 스키마와 예제 결과의 참조 방식을 정리한다.

### 수행한 작업

- 사용자 프로필의 경력, 성과, 프로젝트, 기술, 행동 사례와 목표 직무에 항목 ID를 추가했다.
- 채용공고의 주요 업무, 필수 조건과 우대 조건에 항목 ID를 추가했다.
- 주요 업무를 문자열 배열에서 `responsibility_id`와 `text`를 가진 객체 배열로 변경했다.
- 매칭 결과의 배열 인덱스 및 경로 참조를 안정적인 `source_id` 참조로 교체했다.
- 세 예제 JSON의 파싱, ID 중복, 빈 ID와 참조 무결성을 확인했다.

### 생성 또는 수정한 파일

- `docs/USER_PROFILE_SCHEMA.md`
- `docs/JOB_POSTING_SCHEMA.md`
- `docs/MATCH_RESULT_SCHEMA.md`
- `data/user_profile.example.json`
- `data/job_posting.example.json`
- `data/match_result.example.json`
- `작업일지.md`

### 확인된 결과

- 사용자 프로필의 비교 대상 ID 22개가 모두 비어 있지 않고 서로 중복되지 않는다.
- 채용공고의 비교 대상 ID 10개가 모두 비어 있지 않고 서로 중복되지 않는다.
- 매칭 결과의 공고 항목 참조 10개가 모두 실제 공고 ID와 연결된다.
- 매칭 결과의 사용자 근거 참조 14개가 모두 실제 프로필 ID와 연결된다.
- 매칭 결과의 사용자 및 공고 ID가 입력 예제의 ID와 일치한다.
- 변경된 세 JSON 파일이 정상적으로 파싱된다.

### 아직 확인하지 못한 점

- 실제 공고를 다시 수집했을 때 문구가 변경된 동일 항목의 ID를 어떻게 유지할지는 구현 단계에서 규칙이 더 필요하다.
- 자동 추출 과정에서 서로 비슷한 요구사항을 합칠지 분리할지에 대한 기준은 아직 정하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 배열 인덱스 대신 의미 기반 ID를 사용한다.
  - 이유: 항목 추가, 삭제 또는 정렬만으로 기존 분석의 참조가 깨지는 문제를 방지하기 위해서다.
- 사람이 읽는 이름과 시스템 참조 ID를 분리한다.
  - 이유: 표시 문구가 수정되어도 같은 대상을 가리키는 연결은 유지해야 하기 때문이다.
- 현재 단계에서는 자동 ID 생성 로직을 만들지 않는다.
  - 이유: 아직 실행 코드를 작성하지 않고 스키마와 기대 결과의 경계를 먼저 확정하는 단계이기 때문이다.

### 다음 할 일

1. README의 현재 상태와 다음 단계 설명을 실제 문서 및 예제 상태에 맞춘다.
2. 첫 자동 공고 발견 입력으로 사용할 인크루트 RSS의 최소 저장 필드를 정의한다.
3. 실행 코드를 만들기 전에 공고 발견과 상세 정보 수집의 경계를 문서로 확정한다.

## 2026-09-13 - README 현재 상태 동기화

### 목표

README가 실제로 완료된 문서와 예제 데이터 상태를 정확히 설명하도록 갱신한다.

### 수행한 작업

- 완료 목록에 채용공고 스키마, 매칭 규칙, 결과 스키마, 예제 데이터와 ID 검증을 반영했다.
- 현재 저장소 구조를 실제 파일 구성에 맞게 수정했다.
- 다음 단계를 자동 공고 발견 경계 정의와 최소 비교 기능의 입출력 확정 순서로 변경했다.
- 예제 공고 입력은 규칙 검증용이며 수동 복사·붙여넣기가 최종 제품 흐름이 아님을 명시했다.

### 생성 또는 수정한 파일

- `README.md`
- `작업일지.md`

### 확인된 결과

- README의 완료 항목과 저장소 파일 구성이 현재 상태와 일치한다.
- 아직 애플리케이션 코드가 구현되지 않았다는 상태가 유지된다.
- 개인용 MVP도 자동 공고 발견 경로를 작은 범위로 연결한다는 목표가 명확해졌다.

### 아직 확인하지 못한 점

- 첫 자동 공고 발견 입력의 구체적인 필드와 갱신 주기는 아직 정의하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 고정 예제 공고와 실제 제품 입력을 구분한다.
  - 이유: 기대 결과를 재현하려면 고정 입력이 필요하지만, 사용자의 최종 목표는 공고 자동 발견이기 때문이다.

### 다음 할 일

1. 인크루트 RSS에서 얻을 수 있는 필드와 얻을 수 없는 필드를 문서화한다.
2. 상세 공고는 허용된 공식 경로만 사용한다는 수집 경계를 정의한다.
3. 이 경계를 바탕으로 첫 자동 발견 단계의 최소 명세를 작성한다.

## 2026-09-13 - 첫 자동 공고 발견 경계 정의

### 목표

사용자의 반복적인 공고 복사·붙여넣기 없이 신규 공고 후보를 찾되, 채용사이트의 이용 조건과 접근 제한을 지키는 가장 작은 자동 발견 단계를 정의한다.

### 수행한 작업

- 공식 API, RSS, 검색엔진 API, 기업 ATS 공개 API, 사이트맵·JSON-LD와 직접 HTML 수집을 비교했다.
- 인크루트 공식 RSS 안내, 개인회원 약관과 IT 직군 RSS 응답을 다시 확인했다.
- 인크루트 RSS의 항목 수와 실제 제공 필드를 확인했다.
- 사람인, 잡코리아, 인크루트, 원티드와 기업 채용페이지의 접근 원칙을 분리했다.
- 공고 발견과 상세 정보 수집을 서로 다른 단계로 정의했다.
- 첫 발견 레코드의 최소 필드, 중복 제거 기준, 선별 범위, 안전장치와 구현 순서를 문서화했다.
- 채용공고 스키마의 고정 예제 입력과 실제 자동 입력을 구분했다.

### 생성 또는 수정한 파일

- `docs/JOB_DISCOVERY_PLAN.md`
- `docs/JOB_POSTING_SCHEMA.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 인크루트 IT 직군 RSS는 확인 시점에 RSS 2.0 XML과 20개 항목을 반환했다.
- 각 항목에는 `title`, `link`, `description`, `author`, `pubDate`가 있었다.
- `description`에는 회사명, 경력, 학력, 지역과 마감일이 포함되었다.
- RSS에는 주요 업무, 필수 조건과 우대 조건 전문이 없어 RSS만으로 최종 매칭 분석을 실행할 수 없다.
- Greenhouse, Lever와 Ashby는 공개 채용공고를 위한 공식 API 문서를 제공한다.
- 사람인 채용공고 API는 이용 신청 승인과 access key가 필요하다.
- 네이버 웹문서 검색 API는 기업 공식 채용 URL을 발견하는 보조 경로로 사용할 수 있다.
- Google Custom Search JSON API는 신규 고객에게 제공되지 않아 신규 MVP 기본 경로에서 제외했다.

### 아직 확인하지 못한 점

- 잡코리아 공식 API의 현재 신청 페이지, 제공 범위와 개인 프로젝트 승인 가능성은 추가 확인이 필요하다.
- 원티드가 개인 프로젝트에 허용하는 공식 공고 API 또는 제휴 경로는 확인하지 못했다.
- 실제 관심 기업 중 어떤 ATS를 사용하는지는 관심 기업 목록이 정해진 뒤 확인해야 한다.
- 인크루트 RSS의 장기적인 갱신 안정성과 항목 정렬 규칙은 한 번의 응답만으로 판단할 수 없다.

### 막힌 점

현재 없음. 공식 경로가 불명확한 공급자는 구현 대상에서 보류하면 된다.

### 주요 판단과 이유

- 첫 자동 입력은 인크루트 RSS의 최소 메타데이터로 시작한다.
  - 이유: 공식적으로 RSS 구독을 제공하고 인증 없이 작은 규모의 신규 공고 발견을 검증할 수 있기 때문이다.
- RSS 후보와 분석 가능한 채용공고를 분리한다.
  - 이유: 제한된 메타데이터만으로 기술 격차나 지원 판단을 만들면 근거가 부족하기 때문이다.
- 상세 분석은 공식 API 또는 허용된 기업 ATS 경로가 있을 때만 연결한다.
  - 이유: 접근 제한을 우회하지 않으면서 구조화된 상세 정보를 얻는 가장 안정적인 방법이기 때문이다.
- 직접 HTML 수집은 기본 전략으로 사용하지 않는다.
  - 이유: 정책 위반 위험과 유지보수 비용이 개인용 MVP에 비해 크기 때문이다.

### 다음 할 일

1. 실행 코드 작성 전에 발견 레코드 스키마와 비식별 합성 예제를 만든다.
2. RSS 항목 1개에서 발견 레코드 1개로 변환되는 기대 결과를 검토한다.
3. 사용자 프로필에서 첫 선별 직무 축과 지역 조건을 도출한다.

## 2026-09-13 - 발견 레코드 스키마와 비식별 예제 정의

### 목표

RSS에서 찾은 후보를 완전한 채용공고와 혼동하지 않고 저장할 수 있는 최소 데이터 구조와 기대 예제를 만든다.

### 수행한 작업

- 발견 항목의 식별자, 출처, 요약, 상세 가용성, 중복 키, 파싱 메모와 메타데이터 구조를 정의했다.
- 인크루트 RSS 항목을 발견 레코드로 바꾸는 필드 변환 규칙을 문서화했다.
- 실제 공고 문구를 포함하지 않는 비식별 합성 JSON 예제를 작성했다.
- README의 문서 및 데이터 구조에 새 파일을 반영했다.
- 예제 JSON 파싱과 ID, 중복 키, 분석 준비 상태의 일관성을 확인했다.

### 생성 또는 수정한 파일

- `docs/JOB_DISCOVERY_SCHEMA.md`
- `data/job_discovery.example.json`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 비식별 예제 JSON이 정상적으로 파싱된다.
- `discovery_id`가 공급자와 외부 공고 ID 조합과 일치한다.
- 중복 키가 `provider:external_id` 형식과 일치한다.
- 메타데이터만 있는 후보는 `match_ready: false`로 표시된다.
- 공개 예제에 실제 공고 전문이 없음을 메타데이터로 명시했다.

### 아직 확인하지 못한 점

- 사용자 프로필에서 첫 선별 직무 축과 지역 조건을 도출하는 규칙은 아직 확정하지 않았다.
- 실제 RSS의 비정상 항목이나 필드 누락 사례는 아직 수집하지 않았다.

### 막힌 점

현재 없음. 필터 조건은 기존 사용자 프로필에서 도출한다.

### 주요 판단과 이유

- 발견 레코드와 분석용 채용공고를 별도 스키마로 유지한다.
  - 이유: RSS 메타데이터만으로 필수 조건과 우대 조건을 추정하지 않도록 구조적으로 제한하기 위해서다.
- 공개 테스트 데이터는 실제 RSS 항목을 복제하지 않고 합성 예제를 사용한다.
  - 이유: 공개 저장소에 실제 공고 문구를 불필요하게 재배포하지 않기 위해서다.

### 다음 할 일

1. 프로필의 목표 직무, 근거와 선호에서 검색 축과 순위 신호를 도출한다.
2. 도출된 조건을 발견 선별 구조로 문서화한다.
3. 실행 코드를 시작하기 전 합성 RSS 입력과 기대 발견 레코드의 변환 사례를 하나 더 만든다.

## 2026-09-13 - 검색 조건 사용자 입력 요청 철회 및 프로필 기반 선별로 수정

### 목표

포함 키워드, 제외 키워드와 지역을 사용자에게 다시 요청한 잘못된 설계를 바로잡고, 이미 제공된 프로필에서 Agent가 검색 계획과 후보 우선순위를 도출하도록 변경한다.

### 문제

- 사용자에게 포함 직무 키워드와 제외 키워드를 요청했다.
- 프로필에 `location_preference: 수도권`과 `employment_type_preference: 정규직`이 있는데도 지역을 다시 요청했다.
- 첫 발견에 필요하지 않고 프로필에 확정되지 않은 재택근무 선호를 질문했다.

이 요청은 Agent가 수행해야 할 분석을 사용자에게 되돌린 것이므로 철회한다.

### 수행한 작업

- 목표 직무 우선순위에서 직무 검색 축과 표현 확장 예시를 도출했다.
- 기술, 프로젝트와 경력에서 긍정 신호를 도출했다.
- `avoid_if_possible`과 `less_preferred`에서 낮은 선호 신호를 도출했다.
- 낮은 선호 신호는 강제 제외가 아니라 후보 순위 조정에 사용하도록 했다.
- 수도권과 정규직은 프로필에서 직접 읽고, 재택근무는 첫 조건에서 제외했다.
- 발견 레코드에 프로필 관련성, 목표 직무 참조, 긍정 및 낮은 선호 신호와 후보 우선순위 근거를 추가했다.

### 생성 또는 수정한 파일

- `docs/JOB_DISCOVERY_PLAN.md`
- `docs/JOB_DISCOVERY_SCHEMA.md`
- `data/job_discovery.example.json`
- `작업일지.md`

### 확인된 결과

- 검색어는 사용자가 입력하는 설정이 아니라 목표 직무와 공고 표현 차이를 줄이기 위해 Agent가 생성하는 값으로 정의되었다.
- 현재 프로필에서 수도권과 정규직 선호를 직접 확인했다.
- 프로필에 없는 재택 선호는 묻거나 추정하지 않는다.
- 제한된 RSS 제목만으로 후보를 과도하게 삭제하지 않고 넓게 발견한 뒤 우선순위를 매기도록 변경했다.
- 후보 우선순위는 최종 적합도나 지원 추천과 분리된다.

### 아직 확인하지 못한 점

- 실제 공고 제목에서 각 직무 축의 검색 확장어가 어느 정도 재현율을 갖는지는 구현 후 관찰이 필요하다.
- 비식별 예제에는 경력 기간이 공개되지 않아 연차 기반 강제 필터를 정의하지 않았다.

### 막힌 점

현재 없음. 검색 계획 생성은 기존 프로필만으로 다음 단계 진행이 가능하다.

### 주요 판단과 이유

- 사용자는 프로필과 이력서를 제공하고 Agent는 그 근거에서 검색 계획을 만든다.
  - 이유: 어떤 직무를 찾아야 하는지 분석하는 것이 Career Agent의 핵심 책임이기 때문이다.
- 제외 키워드를 먼저 만들지 않고 낮은 선호 신호를 사용한다.
  - 이유: 공고 제목만으로 실제 업무 성격을 단정해 적합한 후보를 놓치지 않기 위해서다.
- 지역과 고용형태는 프로필에 명시된 값만 사용한다.
  - 이유: 이미 확인된 정보를 다시 묻거나 확인되지 않은 선호를 추정하지 않기 위해서다.

### 다음 할 일

1. 프로필에서 검색 계획을 생성하는 입력 필드와 기대 출력 형식을 문서화한다.
2. 직무 축별 확장어가 만들어진 근거를 추적할 수 있는 비식별 예제를 작성한다.
3. 사용자의 추가 입력 없이 합성 RSS 후보의 우선순위를 산정하는 기대 결과를 검토한다.

## 2026-09-13 - 프로필 기반 검색 계획 스키마와 기대 예제 작성

### 목표

사용자가 별도의 검색 키워드나 제외 조건을 입력하지 않아도 사용자 프로필만으로 공고 발견 계획이 생성되는 기대 구조를 정의한다.

### 수행한 작업

- 목표 직무를 실제 공고에서 쓰일 수 있는 표현으로 확장하는 `role_axes`를 정의했다.
- 경력, 프로젝트와 기술에서 후보 순위를 높이는 역량 신호를 정의했다.
- 업무 선호와 낮은 선호를 강제 제외가 아닌 순위 근거로 분리했다.
- 수도권과 정규직 선호를 프로필에서 직접 가져오는 구조를 정의했다.
- 경력 연차와 재택 선호처럼 확인되지 않은 조건은 필터링하지 않는 규칙을 추가했다.
- 인크루트 RSS, 사람인 공식 API와 기업 ATS의 초기 사용 상태를 검색 계획에 기록했다.
- 프로필 기반 검색 계획의 비식별 기대 JSON을 작성했다.

### 생성 또는 수정한 파일

- `docs/JOB_SEARCH_PLAN_SCHEMA.md`
- `data/job_search_plan.example.json`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 검색 계획 JSON이 정상적으로 파싱된다.
- 직무 축 5개가 모두 사용자 프로필의 실제 목표 직무 ID를 참조한다.
- 역량 신호 7개가 모두 실제 경력, 성과, 프로젝트 또는 기술 ID를 참조한다.
- Docker와 AWS의 `exposure` 수준은 긍정 역량 신호에 포함되지 않았다.
- 검색 계획의 지역과 고용형태가 프로필의 수도권 및 정규직 선호와 일치한다.
- 수동 키워드, 수동 제외 조건과 재택 선호 입력 요구가 모두 `false`다.

### 아직 확인하지 못한 점

- 직무 축의 표현 확장어가 실제 국내 공고 제목을 충분히 포착하는지는 운영 데이터로 검증해야 한다.
- RSS 메타데이터만으로 각 후보의 `profile_relevance`를 얼마나 안정적으로 정할 수 있는지는 합성 사례를 더 검토해야 한다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 검색은 `broad_then_rank` 방식으로 수행한다.
  - 이유: 국내 채용공고의 직무명이 표준화되어 있지 않아 정확한 키워드 일치만 사용하면 적합한 공고를 놓칠 수 있기 때문이다.
- 검색 확장어와 순위 근거를 결과에 함께 저장한다.
  - 이유: Agent가 왜 해당 공고를 찾았는지 사용자가 검토할 수 있어야 하기 때문이다.
- 연차와 재택 여부는 현재 필터에서 제외한다.
  - 이유: 공개 예제 프로필에 확정 근거가 없으며 첫 발견을 진행하는 데 필수 조건이 아니기 때문이다.

### 다음 할 일

1. 검색 계획과 합성 RSS 항목을 사용한 후보 우선순위 기대 사례를 추가한다.
2. 제목이 직접 일치하는 후보, 간접 관련 후보와 불명확 후보를 각각 검토한다.
3. 판정이 안정되면 합성 RSS 한 항목 변환의 최소 구현 범위를 확정한다.

## 2026-09-13 - 프로필 기반 후보 우선순위 기대 사례 검증

### 목표

프로필에서 생성한 검색 계획이 서로 다른 후보를 어떤 우선순위로 분류해야 하는지 실행 코드 없이 기대 사례로 검증한다.

### 수행한 작업

- 실제 공고 문구를 사용하지 않은 합성 후보 4개를 작성했다.
- 최우선 직무 직접 일치, 솔루션 직무 간접 관련, 조건부 데이터 직무와 낮은 우선순위 클라우드 운영 사례를 구분했다.
- 각 사례에 목표 직무 ID, 긍정 신호, 순위 하향 신호, 지역 및 고용형태 판정, 우선순위와 근거를 기록했다.
- 모든 사례가 상세 분석 준비 전 단계임을 명시했다.

### 생성 또는 수정한 파일

- `data/job_discovery_ranking_cases.example.json`
- `docs/JOB_SEARCH_PLAN_SCHEMA.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 합성 사례 JSON이 정상적으로 파싱된다.
- 4개 사례 ID가 서로 중복되지 않는다.
- 목표 직무 참조 6개가 모두 사용자 프로필의 실제 ID와 연결된다.
- `high`, `medium`, `review`, `low` 기대 사례가 각각 하나씩 존재한다.
- 네 사례 모두 `match_ready: false`이며 최종 매칭 결과를 생성하지 않는다.

### 아직 확인하지 못한 점

- 실제 공고 제목의 모호한 표현과 약어는 더 다양한 사례가 필요할 수 있다.
- 상세 공고가 확보됐을 때 발견 우선순위가 최종 적합도와 얼마나 달라지는지는 구현 후 비교해야 한다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 제목이 직접 일치해도 상세 내용이 없으면 최종 적합도로 승격하지 않는다.
  - 이유: 같은 직무명이라도 실제 주요 업무와 요구 역량이 크게 다를 수 있기 때문이다.
- 조건부 데이터 직무는 `review`로 유지한다.
  - 이유: 분석이 자동화나 실제 행동으로 연결되는 역할인지 확인해야 사용자 프로필과의 적합성을 판단할 수 있기 때문이다.
- 낮은 우선순위 후보도 발견 목록에서 바로 삭제하지 않는다.
  - 이유: 제한된 메타데이터에 따른 오판을 줄이고 필요하면 사용자가 근거를 확인할 수 있게 하기 위해서다.

### 다음 할 일

1. 실제 공고 내용이 없는 비식별 합성 RSS 입력 1개를 작성한다.
2. 합성 RSS 입력과 발견 레코드 예제의 필드별 변환 계약을 정의한다.
3. 변환 계약이 확정되면 첫 실행 코드의 범위를 파싱 함수 하나로 제한한다.

## 2026-09-13 - 합성 RSS 입력과 발견 레코드 변환 계약 검증

### 목표

실제 네트워크 호출이나 실행 코드를 만들기 전에 RSS 항목 1개가 발견 레코드 1개로 변환되는 정확한 입출력 계약을 확정한다.

### 수행한 작업

- 실제 회사와 공고 내용을 포함하지 않는 합성 RSS 2.0 XML을 작성했다.
- RSS 정규화와 프로필 기반 정렬을 서로 다른 처리 단계로 구분했다.
- 외부 ID, 회사, 제목, 게시 시점, 경력, 학력, 지역과 마감일의 필드별 변환 규칙을 정의했다.
- 필수 필드 누락, 외부 ID 누락, 게시 시점 오류와 일부 description 누락 시 처리 규칙을 정의했다.
- 상세 요건이 없는 RSS 후보가 최종 매칭 결과를 만들지 않도록 범위를 명시했다.
- 합성 XML과 기대 발견 JSON의 모든 변환 값을 대조했다.

### 생성 또는 수정한 파일

- `data/incruit_rss_item.example.xml`
- `docs/INCRUIT_RSS_MAPPING.md`
- `data/job_discovery.example.json`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 합성 RSS XML이 정상적으로 파싱된다.
- URL의 `job` 값과 `external_id`가 일치한다.
- 발견 ID와 중복 키가 공급자 및 외부 ID 조합 규칙과 일치한다.
- 회사명 접두사를 제거한 제목, 회사명과 게시 시점이 기대 출력과 일치한다.
- 경력, 학력, 지역과 마감일이 description에서 기대대로 추출된다.
- 상세 정보가 없는 후보의 `match_ready`가 `false`로 유지된다.
- 변환 검증 항목 13개가 모두 통과했다.

### 아직 확인하지 못한 점

- 실제 RSS에서 회사명 대괄호 형식이 다른 경우와 복수 지역 표기 사례는 아직 검증하지 않았다.
- 실제 RSS 네트워크 오류와 응답 인코딩 변화는 구현 이후 별도로 검증해야 한다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 첫 실행 코드는 합성 RSS 항목 하나를 정규화하는 책임만 가진다.
  - 이유: 네트워크, 반복 처리, 저장, 분석과 알림을 한 번에 구현하면 실패 원인을 분리하기 어렵기 때문이다.
- 프로필 기반 정렬 실패가 후보 삭제로 이어지지 않게 한다.
  - 이유: 검색 계획 문제로 신규 공고 자체를 놓치지 않고 나중에 재평가할 수 있어야 하기 때문이다.
- 마감일에 연도를 자동 추정하지 않는다.
  - 이유: RSS 표시 문자열만으로 연도를 잘못 확정할 가능성을 피하기 위해서다.

### 다음 할 일

1. 현재 문서와 예제 상태를 사용자에게 보고한다.
2. 코드 작성 승인이 있기 전까지 실행 구현은 시작하지 않는다.
3. 구현을 시작할 경우 합성 RSS 항목 하나를 변환하는 순수 파싱 함수와 테스트 하나만 작성한다.

## 2026-09-13 - 첫 기능 구현 시작: 인크루트 RSS 발견 레코드 변환

### 목표

문서로 확정한 계약을 기준으로 합성 인크루트 RSS 항목 1개를 프로필 기반 발견 레코드 1개로 변환하는 최소 기능을 구현한다.

### 수행한 작업

- Python 표준 라이브러리만 사용하는 RSS 변환 모듈을 작성했다.
- RSS 형식, 입력 크기, 필수 제목과 링크, URL 및 시간대 입력을 검증했다.
- RSS description에서 회사, 경력, 학력, 지역과 마감일을 정규화했다.
- URL의 `job` 값으로 발견 ID와 중복 키를 생성하고, 값이 없을 때 canonical URL 해시를 사용하는 대체 경로를 추가했다.
- 프로필 기반 검색 계획에서 직무 확장어와 지역 선호를 적용해 발견 우선순위를 생성했다.
- 상세 공고 정보가 없으면 `match_ready: false`를 유지했다.
- 외부 엔티티 처리 위험을 줄이기 위해 `DOCTYPE`과 `ENTITY`가 포함된 XML을 거부했다.
- 기대 JSON 전체 일치, 제목 누락과 위험 XML 거부 테스트를 작성했다.
- Python 생성 파일이 Git에 포함되지 않도록 `.gitignore`를 갱신했다.
- README를 문서 단계에서 첫 기능 구현 단계로 갱신하고 테스트 명령을 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/__init__.py`
- `src/career_agent/discovery/__init__.py`
- `src/career_agent/discovery/incruit_rss.py`
- `tests/test_incruit_rss.py`
- `.gitignore`
- `data/job_discovery.example.json`
- `docs/JOB_DISCOVERY_SCHEMA.md`
- `docs/INCRUIT_RSS_MAPPING.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- Python 3.14.7 환경에서 구현했다.
- 단위 테스트 3개가 모두 통과했다.
- `compileall` 문법 검사가 통과했다.
- 합성 입력의 전체 발견 레코드가 기대 JSON과 일치한다.
- 실제 인크루트 네트워크 호출은 실행하지 않았다.
- 외부 Python 패키지를 추가하지 않았다.

### 아직 확인하지 못한 점

- 여러 RSS 항목을 한 번에 처리하는 기능은 아직 없다.
- 실제 RSS 응답의 인코딩과 다양한 필드 변형은 아직 실행 코드로 확인하지 않았다.
- 발견 결과를 실행 간에 저장하고 중복 제거하는 기능은 아직 없다.
- Slack 또는 Telegram 인터페이스는 아직 구현하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 외부 패키지 없이 Python 표준 라이브러리로 첫 기능을 구현했다.
  - 이유: 사용자가 Docker와 AWS 경험이 없어도 로컬 Python만으로 실행하고 검증할 수 있게 하기 위해서다.
- 실제 네트워크 호출보다 합성 입력 변환을 먼저 구현했다.
  - 이유: 파싱과 네트워크 문제를 분리하고 문서의 기대 결과를 정확히 재현하는지 먼저 확인하기 위해서다.
- 발견 정렬까지 포함하되 최종 매칭 분석은 포함하지 않았다.
  - 이유: 사용자가 검색 키워드를 제공하지 않아도 프로필에서 후보 우선순위가 생성되는 최소 Agent 동작을 검증하기 위해서다.

### 다음 할 일

1. 합성 RSS의 여러 항목을 독립적으로 변환하는 기능과 테스트를 추가한다.
2. 외부 공고 ID 기준으로 실행 간 중복을 제거하는 가장 작은 저장 방식을 정의하고 구현한다.
3. 두 단계가 통과한 뒤 실제 인크루트 RSS를 읽기 전용으로 한 번 호출한다.

## 2026-09-13 - RSS 여러 항목 처리와 실행 간 중복 제거 구현

### 목표

실제 RSS의 여러 공고를 항목별로 변환하고 같은 공고가 다음 실행에서 다시 신규 후보로 처리되지 않게 한다.

### 수행한 작업

- RSS의 모든 `item`을 순서대로 변환하는 배치 함수를 추가했다.
- 한 항목이 잘못되어도 정상 항목은 보존하고 오류 항목의 번호와 원인을 별도로 반환하도록 했다.
- `deduplication.key`를 기준으로 신규 공고와 기존 공고를 구분하는 로컬 JSON 저장 기능을 추가했다.
- 저장 도중 파일 손상을 줄이기 위해 같은 폴더의 임시 파일을 완전히 쓴 뒤 원본 경로로 교체하도록 했다.
- 기존 저장 파일이 손상됐거나 스키마가 다르면 덮어쓰지 않고 오류를 반환하도록 했다.
- 테스트에서는 실제 개인 폴더가 아니라 운영체제 임시 폴더만 사용했다.

### 생성 또는 수정한 파일

- `src/career_agent/discovery/incruit_rss.py`
- `src/career_agent/discovery/store.py`
- `src/career_agent/discovery/__init__.py`
- `tests/test_incruit_rss.py`
- `tests/test_discovery_store.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 정상 항목 1개와 제목이 없는 항목 1개가 있을 때 정상 레코드 1개와 항목 오류 1개가 반환된다.
- 같은 발견 레코드를 두 번 저장하면 첫 실행에서만 신규 1건으로 처리된다.
- 두 번째 실행에서는 동일 중복 키가 반환되고 저장된 전체 건수는 1건으로 유지된다.
- 손상된 JSON 저장 파일에 새 데이터를 쓰지 않고 기존 내용을 보존한다.
- 전체 단위 테스트 6개가 통과했다.
- Python 문법 컴파일 검사가 통과했다.

### 아직 확인하지 못한 점

- 실제 RSS 네트워크 응답은 아직 새 실행 코드로 호출하지 않았다.
- 실제 20개 안에 파싱 오류가 있는 항목이 존재하는지는 확인하지 않았다.
- 신규 발견 결과를 사람이 읽기 쉬운 형태로 출력하는 기능은 아직 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 항목 하나의 오류가 전체 RSS 처리를 중단하지 않게 했다.
  - 이유: 실제 공고 하나의 형식 예외 때문에 정상 신규 공고를 모두 놓치지 않기 위해서다.
- 손상된 저장 파일은 자동 초기화하지 않는다.
  - 이유: 문제를 숨기고 기존 발견 기록을 잃는 것보다 오류를 명확히 드러내는 편이 안전하기 때문이다.
- 중복 제거 저장소는 공개 저장소가 아닌 `private-data/`에서 사용하도록 한다.
  - 이유: 실제 관심 공고와 사용자 검토 기록이 공개 저장소에 커밋되지 않게 하기 위해서다.

### 다음 할 일

1. 허용된 인크루트 RSS 호스트만 읽는 네트워크 함수를 구현한다.
2. 응답 크기, 콘텐츠 형식과 리디렉션 목적지를 검증한다.
3. 단위 테스트 통과 후 실제 공식 RSS를 읽기 전용으로 한 번 호출한다.

## 2026-09-13 - 실제 인크루트 RSS 발견 명령 구현 및 실행

### 목표

공식 RSS 읽기, 다중 항목 변환, 프로필 기반 정렬과 중복 저장을 한 번의 사용자 명령으로 실행하는 첫 동작 가능한 공고 발견 기능을 만든다.

### 수행한 작업

- HTTPS와 `www.incruit.com` 호스트만 허용하는 RSS 네트워크 함수를 구현했다.
- HTTP 상태, 최종 리디렉션 호스트, XML 콘텐츠 형식, 선언된 크기와 실제 응답 크기를 검증했다.
- 별도 재시도 없이 제한 시간 안에서 한 번 요청하도록 했다.
- 응답 헤더 및 XML 선언의 인코딩을 확인하고 UTF-8과 CP949 대체 해석을 추가했다.
- RSS 읽기, 변환과 중복 저장을 조합하는 서비스 함수를 구현했다.
- `python scripts/discover_incruit.py` 실행 명령을 추가했다.
- 실행 결과에는 실제 공고 문구 대신 건수, 오류와 우선순위 집계만 출력하도록 했다.
- 실제 발견 데이터는 Git 제외 영역인 `private-data/discoveries.json`에 저장했다.

### 생성 또는 수정한 파일

- `src/career_agent/discovery/incruit_feed.py`
- `src/career_agent/discovery/service.py`
- `src/career_agent/discovery/__init__.py`
- `scripts/discover_incruit.py`
- `tests/test_incruit_feed.py`
- `tests/test_discovery_service.py`
- `README.md`
- `작업일지.md`
- `private-data/discoveries.json` - Git 제외 로컬 실행 데이터

### 확인된 결과

- 허용된 XML 응답, 허용되지 않은 호스트와 응답 크기 초과 테스트가 통과했다.
- 전체 실행 조합과 다음 실행의 중복 제거 테스트가 통과했다.
- 전체 단위 테스트 10개가 통과했다.
- Python 문법 컴파일 검사가 통과했다.
- 실제 공식 RSS 응답 12,570자를 읽었다.
- 실제 공고 20개를 항목 오류 없이 변환했다.
- 첫 저장 실행에서 신규 20건, 중복 0건, 전체 저장 20건으로 확인됐다.
- 두 번째 실행에서 신규 0건, 중복 20건, 전체 저장 20건으로 확인됐다.
- 현재 20건은 제목에서 목표 직무 축과 직접 일치하는 표현이 없어 모두 `review`로 분류됐다.
- `private-data/discoveries.json`이 `.gitignore` 대상임을 확인했다.

### 관찰된 오류와 조치

- 첫 실제 검증 명령에서 Windows Python에 IANA `tzdata`가 없어 `ZoneInfo("Asia/Seoul")` 생성이 실패했다.
- RSS 읽기는 이미 완료된 뒤 로컬 집계 시간 생성 단계에서 발생한 오류였다.
- 외부 패키지를 추가하지 않고 Windows의 현재 로컬 시간대를 사용하는 `datetime.now().astimezone()`으로 변경해 재검증했다.
- 네트워크 모의 테스트에서 `Request` 객체를 딕셔너리처럼 읽은 테스트 오류가 발생했다.
- 실제 `Request.get_header()`를 사용하도록 테스트를 수정한 뒤 통과했다.

### 아직 확인하지 못한 점

- 저장된 20개 후보를 사람이 읽기 쉬운 목록으로 보여주는 기능은 아직 없다.
- 현재 인크루트 RSS 20개만으로는 목표 직무 관련 공고를 찾지 못했다.
- 상세 공고의 주요 업무와 자격 요건을 공식 경로로 가져오는 기능은 아직 없다.
- Slack 또는 Telegram 인터페이스는 아직 구현하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 네트워크 접근은 공식 RSS 주소와 허용 호스트로 제한한다.
  - 이유: 의도하지 않은 외부 URL 호출이나 리디렉션을 방지하고 정책 범위를 지키기 위해서다.
- 실제 공고 원문은 공개 저장소에 저장하지 않는다.
  - 이유: 개인용 후보 관리에 필요한 최소 데이터만 로컬에서 사용하고 불필요한 재배포를 피하기 위해서다.
- 실제 20건을 모두 `review`로 유지했다.
  - 이유: 목표 직무 표현이 없는 제목을 프로필과 관련 있다고 과장하지 않기 위해서다.
- 문서 전용 단계는 종료하고 기능 단위 구현과 필요한 문서 갱신을 함께 진행한다.
  - 이유: 입력, 처리와 완료 기준이 이미 첫 기능을 구현할 만큼 확정됐기 때문이다.

### 다음 할 일

1. 로컬 저장 후보의 회사, 제목, 지역, 우선순위와 URL을 사람이 읽을 수 있게 출력한다.
2. `review` 후보를 상세 내용 없이도 안전하게 구분할 수 있는 범위를 검토한다.
3. 첫 공식 ATS 상세 공고 연결 대상을 선정한다.

## 2026-09-13 - 로컬 발견 후보 목록 구현 및 실제 출력 확인

### 목표

Git 제외 로컬 저장소의 발견 후보를 사용자가 직접 확인할 수 있는 읽기 전용 목록으로 제공한다.

### 수행한 작업

- 검증된 발견 저장 파일을 읽는 공개 함수를 추가했다.
- 우선순위, 회사, 제목, 지역, 게시 시점, 발견 이유와 원문 링크를 표시하는 보고서 함수를 구현했다.
- 우선순위 순서와 같은 우선순위 안의 최신 게시 시점 순서로 정렬했다.
- 표시 개수와 특정 우선순위 필터를 지원하는 `list_discoveries.py` 명령을 추가했다.
- 발견 우선순위가 최종 적합도나 지원 추천이 아니라는 경고를 출력에 포함했다.
- Windows 터미널 출력을 UTF-8로 고정했다.
- CDATA 안의 `&amp;` 같은 HTML 엔티티를 제목과 목록 표시에서 정리했다.
- `|경기>성남시` 같은 RSS 지역 표기를 `경기 > 성남시` 형식으로 정리했다.

### 생성 또는 수정한 파일

- `src/career_agent/discovery/report.py`
- `src/career_agent/discovery/store.py`
- `src/career_agent/discovery/incruit_rss.py`
- `src/career_agent/discovery/__init__.py`
- `scripts/list_discoveries.py`
- `scripts/discover_incruit.py`
- `tests/test_discovery_report.py`
- `tests/test_incruit_rss.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `python scripts/list_discoveries.py --limit 2` 실행이 성공했다.
- 저장된 20개 후보 중 최신 2개가 회사, 제목, 정리된 지역과 링크로 표시됐다.
- Windows 터미널에서 한글이 정상 표시됐다.
- HTML 엔티티가 포함된 합성 제목이 정상 문자열로 변환됐다.
- 기존 저장 데이터의 지역 구분자가 삭제되지 않아도 표시 단계에서 정상화됐다.
- 전체 단위 테스트 14개가 통과했다.
- Python 문법 컴파일 검사가 통과했다.

### 아직 확인하지 못한 점

- 현재 20개 후보에는 `high` 또는 `medium` 후보가 없어 해당 우선순위의 실제 목록 출력은 확인하지 못했다.
- RSS 제목만으로는 AI 자동화 및 솔루션 역할의 실제 업무 관련성을 충분히 판단할 수 없다.
- 상세 공고 공식 API 연결과 최종 프로필 매칭은 아직 구현하지 않았다.

### 막힌 점

현재 없음. 인크루트 RSS만으로 관련 공고가 부족한 것은 오류가 아니라 다음 입력 경로를 추가해야 한다는 관찰 결과다.

### 주요 판단과 이유

- 목록은 로컬 터미널에만 표시하고 공개 파일로 만들지 않는다.
  - 이유: 사용자가 관심을 보인 공고 이력을 공개 저장소에 불필요하게 남기지 않기 위해서다.
- `review` 후보를 사용자에게 확인해야 할 작업으로 해석하지 않는다.
  - 이유: 현재 단계에서는 Agent가 상세 정보를 더 확보하거나 다른 입력 경로에서 재평가해야 할 내부 보류 상태이기 때문이다.
- Slack 알림에서는 향후 `high`와 `medium` 후보를 우선하고 `review` 전체를 그대로 보내지 않는다.
  - 이유: 관련성이 확인되지 않은 대량 후보를 사용자에게 다시 분류하게 만들지 않기 위해서다.

### 다음 할 일

1. 제목 메타데이터의 한계를 보완할 첫 공식 상세 공고 입력 경로를 연결한다.
2. 구조화된 상세 공고와 사용자 프로필의 최소 매칭 기능을 구현한다.
3. 매칭 결과가 유용한 수준이 되면 Slack의 `career_break` 봇 인터페이스를 연결한다.

## 2026-09-13 - 기술 요구사항 최소 매처 구현

### 목표

구조화된 채용공고의 명시적 기술 요구사항을 사용자 프로필의 실제 증거 수준과 비교하고 `gap`과 `unknown`을 구분한다.

### 수행한 작업

- 공고의 `skill`과 `cloud` 유형만 처리하는 규칙 기반 기술 매처를 구현했다.
- 사용자 기술 수준 `work`·`real_work`·`project`를 `strong_match`, `basic`·`learning`을 `partial`, `exposure`·`none`을 `gap`으로 판정했다.
- 프로필에 해당 기술 정보가 없거나 수준 값을 안전하게 해석할 수 없으면 `unknown`으로 유지했다.
- 공고 원문 근거, 사용자 기술 ID, 증거 수준과 상세 증거를 각 판정에 연결했다.
- 예제 입력을 바로 확인할 수 있는 `match_job_technologies.py` 명령을 추가했다.
- 중복 기술명, 학습 수준, 노출 수준과 정보 부재를 검증하는 단위 테스트를 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/__init__.py`
- `src/career_agent/matching/technology.py`
- `scripts/match_job_technologies.py`
- `tests/test_technology_matching.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 예제 공고의 Python과 LLM API는 `strong_match`로 판정됐다.
- Docker와 AWS는 설치 또는 화면 확인만 있고 실제 사용 경험이 없다고 확인되어 `gap`으로 판정됐다.
- 프로필에 없는 Kubernetes 합성 항목은 `gap`이 아니라 `unknown`으로 판정됐다.
- 학습 중인 SQL 합성 항목은 `partial`로 판정됐다.
- 업무 경험 유형인 REST API 연동과 자동화 프로젝트는 현재 기술 전용 범위에서 제외됐다.
- 전체 단위 테스트 18개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다. 줄바꿈 형식 전환 경고만 확인됐다.

### 아직 확인하지 못한 점

- 기술 이름의 별칭은 최소 목록만 정의되어 있어 실제 공고의 다양한 표현은 아직 검증하지 않았다.
- 업무 경험, 주요 업무, 경력·학력·지역·고용 형태는 아직 비교하지 않는다.
- 전체 매칭 결과 스키마와 최종 지원 추천을 코드에서 생성하지 않는다.
- 실제 상세 공고 입력과 Slack 인터페이스는 아직 연결하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 기술 매처를 공식 ATS 연결보다 먼저 구현했다.
  - 이유: 외부 소스가 바뀌어도 재사용할 핵심 판정 규칙을 고정된 예제 입력으로 먼저 검증하기 위해서다.
- `exposure`는 실제 사용 경험을 요구하는 기술 항목에서 `partial`로 올리지 않았다.
  - 이유: Docker 설치와 AWS CLI 화면 확인은 컨테이너 실행이나 클라우드 운영 경험을 증명하지 못하기 때문이다.
- 프로필에 없는 기술은 `gap`으로 단정하지 않았다.
  - 이유: 실제 미보유와 단순 정보 부재를 구분해야 Agent가 불필요한 질문이나 잘못된 지원 포기를 만들지 않기 때문이다.
- LLM을 호출하지 않는 결정 규칙부터 구현했다.
  - 이유: 같은 입력에서 핵심 판정이 재현되고, 향후 LLM은 표현 해석과 설명 보조에 집중하게 하기 위해서다.

### 다음 할 일

1. REST API 연동과 자동화 프로젝트 요구사항을 프로젝트 및 행동 증거와 연결한다.
2. 기술·경험 판정을 조합해 지원 가능 조건과 근거 기반 지원 추천을 생성한다.
3. 공식 공개 경로를 제공하는 기업 ATS 1곳을 연결해 실제 상세 공고 1건을 구조화한다.
4. 실제 공고 판정이 유용한지 확인한 뒤 Slack `creer_break`의 최소 대화 및 파일 입력을 구현한다.

## 2026-09-13 - 경험 요구사항 매처 구현

### 목표

채용공고의 명시적 경험 요구사항을 사용자 프로필의 완료 프로젝트, 기술 및 행동 증거에 연결한다.

### 수행한 작업

- 공고의 `experience` 유형을 처리하는 규칙 기반 경험 매처를 구현했다.
- REST API 연동 요구를 `REST API` 기술 항목과 API·Webhook을 사용한 완료 프로젝트에 연결했다.
- 자동화 프로젝트 요구를 완료된 자동화 프로젝트와 반복 업무 자동화 행동 사례에 연결했다.
- 완료 프로젝트 또는 프로젝트 수준 기술 증거는 `strong_match`, 계획 중 프로젝트나 학습 수준 기술은 `partial`로 처리했다.
- 관련 행동 사례만 있으면 직접 프로젝트 증거와 구분해 `match`로 처리했다.
- 해석 규칙이나 사용자 근거가 없으면 `gap`으로 단정하지 않고 `unknown`으로 유지했다.
- 예제 입력으로 결과를 확인하는 `match_job_experiences.py` 명령과 단위 테스트를 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/experience.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job_experiences.py`
- `tests/test_experience_matching.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- REST API 연동은 `REST API` 기술, Gemini API 및 Slack Webhook 프로젝트 증거와 연결되어 `strong_match`로 판정됐다.
- 자동화 프로젝트 경험은 완료된 `Tech News Automation`과 행동 사례에 연결되어 `strong_match`로 판정됐다.
- 계획 상태의 자동화 프로젝트만 남긴 합성 입력은 `partial`로 판정됐다.
- 사용자 근거가 전혀 없는 합성 입력과 규칙이 없는 경험 요구는 `unknown`으로 판정됐다.
- 전체 단위 테스트 22개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 현재 경험 의미 규칙은 REST API 연동과 자동화 프로젝트 두 종류만 포함한다.
- 주요 업무 `responsibilities`와 지원 가능 조건은 아직 평가하지 않는다.
- 기술 매처와 경험 매처의 결과는 아직 하나의 전체 결과로 조합되지 않는다.
- 실제 상세 공고의 다양한 한국어·영어 표현은 아직 검증하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 완료 프로젝트와 계획 중 프로젝트의 판정을 분리했다.
  - 이유: 아이디어나 계획을 실제 수행 경험으로 과장하지 않기 위해서다.
- 프로젝트의 API 및 Webhook 기술 목록을 직접 근거로 사용했다.
  - 이유: 단순 관심이나 자기평가보다 실제 구현 기록이 공고의 연동 경험을 더 잘 증명하기 때문이다.
- 인식하지 못한 경험 요구는 `unknown`으로 유지했다.
  - 이유: 제한된 키워드 규칙으로 관련 없는 사용자 경험을 자동 부재로 판단하지 않기 위해서다.
- 최종 지원 추천은 이번 단계에 포함하지 않았다.
  - 이유: 먼저 기술과 경험 판정을 하나의 재현 가능한 결과로 조합해야 필수 조건 전체를 빠뜨리지 않고 판단할 수 있기 때문이다.

### 다음 할 일

1. 기술·경험 판정을 공고 원래 순서대로 합친 전체 필수·우대 판정 결과를 생성한다.
2. 집계 결과와 지원 가능 조건을 바탕으로 근거 기반 지원 추천을 만든다.
3. 공식 기업 ATS 상세 공고 1건을 연결해 실제 입력 표현으로 검증한다.
4. 실제 판정이 유용한 수준이 되면 Slack `creer_break` 인터페이스를 연결한다.

## 2026-09-13 - 강점·부족·미확인 인사이트 생성 구현

### 목표

요구사항과 주요 업무 판정에 연결된 근거를 지원 시 강조할 강점, 확인된 부족과 추가 확인 항목으로 변환한다.

### 수행한 작업

- 통합 판정에서 `strengths`, `gaps`, `unknowns`를 생성하는 `build_match_insights` 함수를 구현했다.
- 여러 공고 조건에 반복 연결된 사용자 근거를 안정적인 `source_id`로 묶었다.
- 연결 조건 수, 강한 일치 수와 증거 유형을 기준으로 상위 강점 3개를 정렬했다.
- 필수 조건 부족은 `immediate`, 우대 조건 부족은 `preferred_only`, 지원 불가 조건은 `blocking`으로 구분했다.
- 필수·우대·주요 업무의 `unknown`과 지원 가능 조건의 `needs_confirmation`을 별도 미확인 목록으로 만들었다.
- `unknown` 항목이 `gaps`에 섞이지 않도록 분리 테스트를 추가했다.
- 통합 `match_job.py` 출력에 강점, 부족과 추가 확인 항목을 추가했다.
- 출력에서 기술명 뒤 조사가 어색하지 않도록 보완 행동 문구를 수정했다.
- 아직 없는 결과 식별자와 사실·해석 분리 영역은 미완성 목록에 유지했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/insights.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/technology.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `tests/test_match_insights.py`
- `tests/test_requirement_matching_service.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `Tech News Automation`이 6개 공고 조건과 연결되어 첫 번째 지원 강점으로 생성됐다.
- LLM API와 REST API 활용 경험이 다음 강점으로 생성됐다.
- Docker와 AWS는 `preferred_only` 부족으로 생성됐다.
- 현재 예제에는 필수 또는 지원 가능 조건의 미확인 항목이 없어 `unknowns`가 비어 있다.
- 프로필에 없는 Kubernetes 필수 조건 합성 입력은 `gap`이 아니라 `unknown` 목록에 포함됐다.
- Python 필수 조건의 확인된 노출 경험은 `immediate` 부족으로 분류됐다.
- 확인된 경력 미달은 `blocking` 부족으로 분류됐다.
- 전체 단위 테스트 47개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 실제 기업 상세 공고의 긴 조건 문장에서 생성되는 강점 순위는 아직 검증하지 않았다.
- 강점은 현재 연결 조건이 많은 상위 3개 근거만 표시한다.
- 결과 `identity`와 `analysis_notes`는 아직 코드에서 생성하지 않는다.
- 통합 결과를 JSON 파일로 저장하는 기능은 아직 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 강점은 단순 기술 목록이 아니라 여러 공고 조건에 연결되는 실제 증거를 우선했다.
  - 이유: 지원서와 면접에서 반복 활용할 수 있는 핵심 프로젝트를 먼저 보여주기 위해서다.
- 우대 조건의 부족을 `preferred_only`로 표시했다.
  - 이유: Docker와 AWS 부족이 필수 역량 부족처럼 보이거나 지원 포기 사유가 되지 않게 하기 위해서다.
- 프로필 정보 부재를 부족 목록에 넣지 않았다.
  - 이유: 모르는 것과 실제로 경험이 없다고 확인된 것을 계속 구분해야 하기 때문이다.
- 전체 스키마 완료를 과장하지 않았다.
  - 이유: 분석 결과 식별자와 사실·해석 분리 영역이 아직 구현되지 않았기 때문이다.

### 사용자 확인 방법

예제 프로필과 공고의 전체 결과를 확인한다.

    python scripts/match_job.py

전체 자동 테스트를 실행한다.

    python -m unittest discover -s tests -v

Docker, AWS 또는 외부 API Key는 필요하지 않다.

### 다음 할 일

1. 공개 공식 API를 제공하는 기업 ATS 한 곳의 상세 공고를 구조화된 입력으로 변환한다.
2. 실제 상세 공고 1건을 현재 통합 매처로 실행한다.
3. 결과 식별자와 사실·해석 분리 영역을 추가하고 로컬 JSON 저장을 구현한다.
4. 실제 판정이 유용한 수준이면 Slack `creer_break` 인터페이스를 연결한다.

## 2026-09-13 - 주요 업무 근거 매칭 구현

### 목표

공고의 주요 업무를 사용자 기술, 완료 프로젝트와 행동 증거에 연결하고 자격조건 일치와 실제 역할 적합도를 함께 검토한다.

### 수행한 작업

- 공고 `responsibilities`의 모든 항목을 원래 순서대로 처리하는 `match_responsibilities` 함수를 구현했다.
- 반복 업무 자동화, 외부 API 연동, LLM 기반 업무 도구 개발과 자동화 모니터링 업무 규칙을 정의했다.
- 완료 프로젝트와 프로젝트 수준 기술은 직접 증거, 계획 상태 프로젝트는 부분 증거로 구분했다.
- GitHub Actions 상태·로그 확인, 상태 관리와 재시도 처리를 모니터링 관련 증거로 연결했다.
- 개별 실패 요약이나 능동 알림이 없는 현재 상태는 모니터링 업무의 `strong_match`가 아닌 `match`로 판정했다.
- 해석하지 못하거나 근거가 없는 주요 업무는 `unknown`으로 유지했다.
- 주요 업무 결과를 통합 매칭 요약과 지원 추천에 연결했다.
- 근거가 부족한 주요 업무가 있으면 기존 `적극 지원`을 `지원 추천`으로 조정하도록 했다.
- `match_job.py` 출력에 주요 업무 판정을 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/responsibility.py`
- `src/career_agent/matching/recommendation.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `tests/test_responsibility_matching.py`
- `tests/test_application_recommendation.py`
- `tests/test_requirement_matching_service.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 내부 반복 업무 자동화, 외부 API 연동과 LLM 기반 업무 도구 개발은 `strong_match`로 판정됐다.
- 자동화 결과 모니터링 및 개선은 기본 상태·로그 확인과 재시도 증거를 근거로 `match`로 판정됐다.
- 주요 업무 4건 모두 프로젝트 또는 행동 증거와 연결되어 현재 예제의 `적극 지원` 판단이 유지됐다.
- 해외 영업처럼 근거와 해석 규칙이 없는 합성 업무는 `unknown`으로 판정됐다.
- 근거 없는 주요 업무를 예제에 추가하면 지원 판단이 `적극 지원`에서 `지원 추천`으로 조정됐다.
- 계획 상태의 자동화 프로젝트만 남긴 합성 입력은 해당 업무가 `partial`로 판정됐다.
- 중복 주요 업무 ID는 오류로 거부됐다.
- 전체 단위 테스트 43개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 현재 주요 업무 의미 규칙은 예제 공고의 네 가지 업무 축을 중심으로 한다.
- 웹 개발·서버 운영 경력과 연결되는 다른 업무 표현은 아직 구현하지 않았다.
- 강점, 부족과 미확인 항목의 별도 최상위 배열은 아직 생성하지 않는다.
- 실제 기업 상세 공고의 긴 문장과 복합 업무는 아직 검증하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 주요 업무를 필수 자격조건과 별도로 평가했다.
  - 이유: 기술 이름이 일치해도 실제 역할의 일상 업무가 사용자 경험과 맞지 않을 수 있기 때문이다.
- 모니터링 업무를 `strong_match`로 올리지 않았다.
  - 이유: 실행 상태와 로그 확인은 가능하지만 개별 실패 요약과 능동 알림까지 구현됐다는 증거는 없기 때문이다.
- 계획 상태의 Career Agent 프로젝트를 완료 경험으로 사용하지 않았다.
  - 이유: 설계 중인 기능을 이미 수행한 경험으로 과장하지 않기 위해서다.
- 모르는 주요 업무를 추천 과정에서 무시하지 않았다.
  - 이유: 핵심 업무 근거가 부족한데 필수 자격조건만 보고 과도하게 적극적인 지원 판단을 만들지 않기 위해서다.

### 다음 할 일

1. 요구사항과 주요 업무 판정에서 강점, 확인된 부족과 미확인 항목을 생성한다.
2. 전체 결과를 `MATCH_RESULT_SCHEMA` 형태로 완성하고 예제 기대 결과와 비교한다.
3. 기업 공식 ATS 상세 공고 1건을 구조화해 실제 문장으로 전체 흐름을 검증한다.
4. 실제 판정이 유용한 수준이 되면 Slack `creer_break` 인터페이스를 연결한다.

## 2026-09-13 - 근거 기반 지원 추천 구현

### 목표

필수 조건, 우대 조건과 지원 가능 조건을 조합해 합격 확률이 아닌 현재 지원 우선순위를 근거와 함께 생성한다.

### 수행한 작업

- 지원 판단을 생성하는 `build_application_recommendation` 함수를 구현했다.
- 지원 불가 조건, 필수 역량 부족, 필수 조건 미확인, 직접 증거 충족 순서로 판정 우선순위를 정의했다.
- `적극 지원`, `지원 추천`, `조건부 지원`, `역량 보완 후 지원`, `현재는 우선순위 낮음` 값을 사용하도록 했다.
- 우대 조건의 `gap`과 `unknown`은 지원 등급을 낮추지 않고 주의사항에만 반영했다.
- 추천 근거, 주의사항, 다음 행동과 합격 가능성 예측이 아니라는 해석 문구를 생성했다.
- 주요 업무 매칭이 아직 없다는 제한을 추천 주의사항에 자동으로 포함했다.
- 통합 `match_job.py` 명령에 지원 판단 출력을 추가했다.
- 매칭 결과 스키마와 기대 결과 예제에 `interpretation` 필드를 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/recommendation.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `tests/test_application_recommendation.py`
- `tests/test_requirement_matching_service.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `data/match_result.example.json`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 현재 예제 공고는 필수 조건 3건의 직접 증거와 `eligible` 상태를 근거로 `적극 지원`으로 판정됐다.
- Docker와 AWS의 우대 조건 `gap`은 주의사항에 포함됐지만 지원 판단을 낮추지 않았다.
- Python 필수 경험을 `exposure`로 바꾼 합성 입력은 `역량 보완 후 지원`으로 판정됐다.
- 미지원 필수 자격증 조건을 추가한 합성 입력은 `조건부 지원`으로 판정됐다.
- 최소 경력 연수가 미확인인 합성 입력은 `조건부 지원`으로 판정됐다.
- 확인된 경력이 공고 최소 경력보다 짧은 합성 입력은 `현재는 우선순위 낮음`으로 판정됐다.
- 전체 단위 테스트 37개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 공고 주요 업무와 사용자 증거의 직접 비교는 아직 없다.
- 강점, 부족, 미확인 항목의 별도 최상위 배열은 아직 코드에서 생성하지 않는다.
- 실제 상세 공고의 모호한 표현을 해석하는 단계는 아직 없다.
- 추천 결과를 로컬 JSON으로 저장하거나 Slack으로 전송하지 않는다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 확인된 지원 불가 조건을 가장 먼저 판정했다.
  - 이유: 역량이 잘 맞더라도 명시적인 최소 경력이나 필수 조건을 충족하지 못하면 현재 지원 우선순위를 낮춰야 하기 때문이다.
- 필수 역량 `gap`과 필수 조건 `unknown`을 분리했다.
  - 이유: 확인된 부족은 보완 행동이 필요하지만 정보 부족은 먼저 사실 확인이 필요하기 때문이다.
- 우대 조건 부족을 추천 등급에서 제외했다.
  - 이유: Docker와 AWS가 우대 조건인 현재 예제에서 실제 사용 경험 부족만으로 지원을 포기하게 만들지 않기 위해서다.
- 추천 `confidence`를 합격 확률과 분리했다.
  - 이유: `high`는 입력 근거가 명확하다는 뜻이며 채용 결과를 예측하는 숫자가 아니기 때문이다.
- 주요 업무 매칭 미구현을 주의사항으로 표시했다.
  - 이유: 현재 `적극 지원` 판단을 전체 역할 적합도 평가가 끝난 것으로 오해하지 않게 하기 위해서다.

### 다음 할 일

1. 공고 주요 업무를 사용자 프로젝트, 경력과 행동 증거에 연결한다.
2. 강점, 부족과 미확인 항목을 전체 매칭 결과 구조로 생성한다.
3. 기업 공식 ATS 상세 공고 1건을 구조화해 실제 표현으로 전체 흐름을 검증한다.
4. 실제 판정이 유용한 수준이 되면 Slack `creer_break` 인터페이스를 연결한다.

## 2026-09-13 - 지원 가능 조건 판정 구현

### 목표

경력, 학력, 지역과 고용 형태를 역량 적합도와 분리해 판정하고 확인된 불충족과 정보 부족을 구분한다.

### 수행한 작업

- 경력 연수, 학력, 지역, 원격·출근 방식과 고용 형태를 비교하는 `assess_eligibility` 함수를 구현했다.
- 공고 최소 경력이 0년이면 사용자 경력 연수가 비공개여도 지원 가능한 것으로 판정했다.
- 최소·최대 경력이 있고 사용자 연수가 없으면 `needs_confirmation`, 확인된 연수가 범위를 벗어나면 `not_met`으로 판정했다.
- 학력 필수가 아니면 `not_applicable`, 필수인데 사용자 정보가 없으면 `needs_confirmation`으로 구분했다.
- 서울·경기·인천을 수도권 선호에 포함하는 최소 지역 정규화 규칙을 추가했다.
- `full_time`과 정규직 같은 고용 형태 표현을 정규화했다.
- 지역과 고용 형태의 단순 선호 불일치는 강제 조건이 아니므로 `needs_confirmation`으로 처리했다.
- 공고에 원격·출근 방식이 명시됐지만 사용자 조건이 없으면 확인 필요로 남겼다.
- 공개 프로필 예제에 확인된 경력 연수를 담을 `duration_years`와 선택적 학력 영역을 추가했다.
- 통합 `match_job.py` 출력에 지원 가능 조건을 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/eligibility.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `tests/test_eligibility_matching.py`
- `tests/test_requirement_matching_service.py`
- `data/user_profile.example.json`
- `docs/USER_PROFILE_SCHEMA.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 현재 예제 공고는 경력 `met`, 학력 `not_applicable`, 고용 형태 `met`, 지역 `met`이며 전체 `eligible`로 판정됐다.
- 경력 3년 이상 공고에서 사용자 연수가 비공개인 합성 입력은 `conditional`로 판정됐다.
- 확인된 경력 1년과 최소 경력 3년을 비교한 합성 입력은 `ineligible`로 판정됐다.
- 부산·계약직 공고와 수도권·정규직 선호의 불일치는 `ineligible`이 아니라 `conditional`로 판정됐다.
- 필수 학력인데 사용자 학력이 미확인인 합성 입력은 `conditional`로 판정됐다.
- 출근 방식이 명시됐지만 사용자 원격·출근 선호가 없는 합성 입력은 `conditional`로 판정됐다.
- 전체 단위 테스트 32개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 지역 포함 관계는 현재 수도권의 서울·경기·인천만 정의했다.
- 사용자 선호를 강제 조건으로 선언하는 별도 필드는 아직 없다.
- 복수 경력이 겹치는 기간인지 자동 계산하지 않으며 확인된 `duration_years`만 사용한다.
- 필수 자격증, 언어와 취업 허가 조건은 아직 지원 가능 조건에 포함하지 않는다.
- 주요 업무 매칭과 최종 지원 추천은 아직 생성하지 않는다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 지원 가능 조건을 기술 및 경험 적합도와 분리했다.
  - 이유: 역량이 잘 맞아도 필수 경력이나 학력 조건 때문에 지원이 어려울 수 있고, 반대로 단순 선호 불일치는 자동 탈락 사유가 아니기 때문이다.
- 비공개 경력 연수를 0년으로 해석하지 않았다.
  - 이유: 확인되지 않은 정보와 실제 경력 부족을 구분하기 위해서다.
- 현재 공고의 최소 경력 0년 조건은 사용자 연수 확인 없이 `met`으로 처리했다.
  - 이유: 공고가 신입 또는 경력을 모두 허용하므로 연수 확인이 지원 가능 여부를 바꾸지 않기 때문이다.
- 학력 저장 영역을 선택 필드로 추가하되 공개 예제는 `unknown`으로 유지했다.
  - 이유: 이력서 입력으로 나중에 채울 수 있는 구조는 필요하지만 현재 정보를 임의로 만들면 안 되기 때문이다.
- 원격근무 선호를 지금 사용자에게 다시 묻지 않았다.
  - 이유: 공고가 실제 근무 방식을 명시해 판단에 영향을 줄 때만 확인 대상으로 올리는 편이 현재 MVP에 적합하기 때문이다.

### 다음 할 일

1. 필수·우대 조건과 지원 가능 조건을 조합해 근거 기반 지원 추천을 생성한다.
2. 주요 업무를 프로젝트·경력 증거에 연결해 실제 역할 적합도 설명을 보강한다.
3. 기업 공식 ATS 상세 공고 1건을 구조화해 실제 표현으로 전체 흐름을 검증한다.
4. 실제 판정이 유용한 수준이 되면 Slack `creer_break` 인터페이스를 연결한다.

## 2026-09-13 - 통합 요구사항 매칭 서비스 구현

### 목표

분리된 기술 및 경험 판정을 공고의 원래 항목 순서로 합치고 누락 없는 필수·우대 조건 결과를 제공한다.

### 수행한 작업

- 기술 매처와 경험 매처를 조합하는 `match_job_requirements` 서비스를 구현했다.
- 안정적인 `requirement_id`와 `qualification_id`를 기준으로 판정을 다시 조립했다.
- 공고의 필수 조건과 우대 조건 순서를 그대로 유지했다.
- 아직 지원하지 않는 조건 유형을 제거하지 않고 `unknown` 판정으로 남겼다.
- 필수·우대 조건별 `strong_match`, `match`, `partial`, `gap`, `unknown` 집계를 생성했다.
- 같은 ID가 공고 안에서 중복되거나 둘 이상의 매처가 같은 항목을 판정하면 오류로 중단하도록 했다.
- 경험 우대 조건도 판정할 수 있도록 경험 매처의 입력 범위를 확장했다.
- 사용자가 통합 결과를 확인할 수 있는 `match_job.py` 명령과 단위 테스트를 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/service.py`
- `src/career_agent/matching/experience.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `scripts/match_job_experiences.py`
- `tests/test_requirement_matching_service.py`
- `tests/test_experience_matching.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 예제 공고의 필수 조건이 Python, REST API 연동, 자동화 프로젝트 순서로 유지됐다.
- 필수 조건 3건은 모두 `strong_match`로 집계됐다.
- 우대 조건은 Docker `gap`, AWS `gap`, LLM API `strong_match` 순서로 유지됐다.
- 지원하지 않는 합성 자격증 조건은 결과에서 누락되지 않고 `unknown`으로 표시됐다.
- 필수 조건과 우대 조건에 같은 ID를 사용한 합성 입력은 오류로 거부됐다.
- `python scripts/match_job.py`에서 통합 결과가 한글로 정상 출력됐다.
- 전체 단위 테스트 26개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 경력, 학력, 지역, 고용 형태 같은 실제 지원 가능 조건은 아직 평가하지 않는다.
- 공고의 주요 업무 `responsibilities`는 아직 사용자 증거와 비교하지 않는다.
- 최종 지원 추천은 아직 생성하지 않는다.
- 실제 기업 상세 공고 입력에서는 아직 통합 매처를 검증하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 공고 배열 순서가 아니라 안정적인 항목 ID로 각 매처 결과를 연결했다.
  - 이유: 매처의 내부 처리 순서가 바뀌어도 원문 항목과 사용자 근거 연결이 어긋나지 않게 하기 위해서다.
- 지원하지 않는 조건도 `unknown`으로 출력했다.
  - 이유: Agent가 평가하지 못한 필수 조건을 조용히 누락하면 지원 판단이 과도하게 긍정적으로 바뀔 수 있기 때문이다.
- 우대 조건의 `gap`을 필수 조건의 `gap`과 별도 집계했다.
  - 이유: Docker나 AWS 우대 경험 부족만으로 지원 자체를 포기하지 않게 하기 위해서다.
- 통합 결과에 미완성 영역을 명시했다.
  - 이유: 현재 결과를 전체 지원 추천으로 오해하지 않도록 지원 가능 조건, 주요 업무와 최종 추천이 아직 미구현임을 기계적으로 남기기 위해서다.

### 다음 할 일

1. 경력, 학력, 지역과 고용 형태를 역량 적합도와 분리해 판정한다.
2. 확인된 필수 조건과 지원 가능 조건을 조합해 근거 기반 지원 추천을 생성한다.
3. 기업 공식 ATS 상세 공고 1건을 구조화해 실제 표현으로 전체 흐름을 검증한다.
4. 실제 판정이 유용한 수준이 되면 Slack `creer_break` 인터페이스를 연결한다.

## 2026-09-13 - Greenhouse 실공고 자동 입력과 매칭 연결

### 목표

사용자가 채용공고를 복사해 붙여넣지 않아도 기업의 공식 ATS 공개 API에서 상세공고 1건을 가져와 기존 매칭 흐름에 넣는다.

### 수행한 작업

- Greenhouse 공식 Job Board API 문서에서 공개 GET 공고 조회는 인증이 필요하지 않고 지원서 제출 POST는 별도 인증 영역임을 확인했다.
- Sendbird 공개 Greenhouse 보드에서 서울 근무 `Software Engineer, AI Agent` 공고가 현재 게시 중임을 확인했다.
- 허용 호스트, HTTPS, GET, JSON, 응답 크기와 최종 응답 경로를 제한한 `fetch_greenhouse_job`을 구현했다.
- 공고 본문의 명시적인 섹션 제목을 기준으로 주요 업무, 필수 조건, 우대 조건과 역할 소개를 분리했다.
- Python, LLM API, 경력 연수, 서울 지역과 하이브리드 근무처럼 명시된 값만 구조화했다.
- 본문에 명시되지 않은 정규직 여부는 추정하지 않고 `unknown`으로 남겼다.
- 실제 공고 전문을 저장하지 않고 구조화된 항목과 원문 URL만 `private-data/`에 저장하는 CLI를 추가했다.
- 비식별 합성 API 응답으로 허용 경로, 외부 리디렉션 거부, 섹션 추출과 고용 형태 미추정을 검증했다.
- 실제 Sendbird 공고를 로컬 JSON으로 생성하고 기존 `match_job.py`로 전체 매칭을 실행했다.
- Greenhouse 접근 경계와 필드 변환 기준을 별도 문서로 기록하고 README의 현재 상태와 실행 방법을 갱신했다.

### 생성 또는 수정한 파일

- `src/career_agent/ingestion/__init__.py`
- `src/career_agent/ingestion/greenhouse.py`
- `scripts/import_greenhouse_job.py`
- `tests/test_greenhouse_ingestion.py`
- `docs/GREENHOUSE_API_MAPPING.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `python scripts/import_greenhouse_job.py --board sendbird --job-id 8395379002` 실행이 성공했다.
- 실제 공고에서 주요 업무 8개, 필수 조건 5개, 우대 조건 5개가 구조화됐다.
- 실제 결과는 Python과 LLM API를 `strong_match`, 자동화 도구를 활용한 업무 1건을 `strong_match`로 판정했다.
- 공고의 3년 이상 경력 조건은 추출됐지만 사용자 총 경력 연수가 비공개여서 `needs_confirmation`으로 판정됐다.
- 고용 형태는 원문에 명시되지 않아 `needs_confirmation`, 서울 지역은 확인됐지만 하이브리드 선호가 프로필에 없어 `needs_confirmation`으로 판정됐다.
- 최종 지원 판단은 `조건부 지원`이었으며 합격 가능성 예측이 아니라 미확인 필수 조건과 근무 조건을 먼저 확인하라는 결과다.
- 실제 출력 파일 `private-data/greenhouse-sendbird-8395379002.json`은 Git 추적 대상이 아니다.
- 전체 단위 테스트 52개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 사용자 총 경력 연수와 하이브리드 근무 허용 여부는 공개 예제 프로필에서 확인되지 않는다.
- 시스템 설계, 분산 시스템, 클라우드 네이티브 배포와 영어 수준은 현재 규칙으로 사용자 근거를 판정하지 못한다.
- Greenhouse 회사마다 다른 섹션 제목을 현재 제한된 제목 목록이 모두 인식하는지 확인하지 않았다.
- 인크루트 RSS에서 발견한 후보가 Greenhouse 상세공고로 자동 연결되는 것은 아니다.
- 현재 실공고가 마감된 뒤 같은 ID로 계속 조회 가능한지는 보장되지 않는다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- Greenhouse의 문서화된 공개 GET API만 연결했다.
  - 이유: 인증 우회나 HTML 직접 수집 없이 상세공고를 자동 입력할 수 있고 접근 경계가 명확하기 때문이다.
- 실제 공고 전문을 Git 제외 로컬 파일에도 보관하지 않았다.
  - 이유: 매칭에 필요한 구조화 항목과 원문 링크만으로 현재 검증이 가능하고 불필요한 원문 복제를 줄일 수 있기 때문이다.
- 복합 조건을 억지로 기술별로 분해하거나 충족으로 판정하지 않았다.
  - 이유: `AWS 또는 GCP를 포함한 클라우드 네이티브 시스템 설계` 같은 문장을 단순 AWS 보유 여부로 바꾸면 요구 수준을 왜곡할 수 있기 때문이다.
- 고용 형태는 일반적인 기업 관행으로 추정하지 않았다.
  - 이유: 공고 원문에서 확인한 사실과 Agent 해석을 분리하는 규칙을 지키기 위해서다.
- 실제 기업 공고를 공개 테스트 fixture로 커밋하지 않았다.
  - 이유: 공개 저장소에는 비식별 합성 데이터만 두고 실제 공고 내용은 실행 시점에 공식 API에서 가져오는 편이 안전하기 때문이다.

### 다음 할 일

1. 실제 결과에서 지원 판단에 영향이 큰 `3년 이상 운영 환경 경험`과 복합 클라우드·시스템 설계 조건을 근거 단위로 안전하게 판정한다.
2. 상세 구조화 결과와 매칭 결과를 함께 저장하는 한 번의 실행 명령을 만든다.
3. Greenhouse 보드의 신규 공고 목록을 프로필 기준으로 선별해 상세 분석으로 이어지게 한다.
4. 실제 결과가 충분히 읽기 쉬워진 뒤 Slack `creer_break`의 최소 대화 인터페이스를 연결한다.

## 2026-09-13 - Greenhouse 조회·매칭·결과 저장 단일 실행 구현

### 목표

별도 두 명령으로 실행하던 Greenhouse 공고 구조화와 매칭을 한 명령으로 묶고, 검토 가능한 분석 결과 JSON을 로컬에 저장한다.

### 수행한 작업

- Greenhouse 조회, 내부 공고 변환, 요구사항 매칭과 결과 포장을 조합한 `analyze_greenhouse_job` 워크플로를 구현했다.
- 기존 매칭 결과에 `analysis_id`, 완료 상태, 생성 시각과 공고 출처 정보를 추가했다.
- 공고 구조화 단계의 사실과 미확인 사항을 매칭 결과의 `analysis_notes`에 연결했다.
- 결과에 스키마 버전, 생성 주체와 사용자 검토 상태 `not_reviewed`를 기록했다.
- 사용자 프로필 원문을 결과 JSON에 복제하지 않도록 했다.
- `analyze_greenhouse_job.py` 단일 실행 CLI를 추가했다.
- 합성 입력을 사용해 저장 가능한 메타데이터와 개인정보 복제 방지를 검증했다.
- 실제 Sendbird 공고로 단일 명령을 실행하고 로컬 분석 JSON을 확인했다.

### 생성 또는 수정한 파일

- `src/career_agent/workflows/__init__.py`
- `src/career_agent/workflows/greenhouse_analysis.py`
- `scripts/analyze_greenhouse_job.py`
- `tests/test_greenhouse_analysis_workflow.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `python scripts/analyze_greenhouse_job.py --board sendbird --job-id 8395379002` 실행이 성공했다.
- 결과는 `private-data/analysis-greenhouse-sendbird-8395379002.json`에 저장됐다.
- 분석 ID, `completed` 상태, `not_reviewed` 검토 상태와 공고 출처 URL이 저장됐다.
- 기존 미완성 표시였던 `identity`와 `analysis_notes`가 채워져 `incomplete_sections`가 빈 배열이 됐다.
- 결과 JSON에는 사용자 프로필 원문이 포함되지 않았다.
- 실제 결과 요약은 `조건부 지원`, 필수 조건 미확인 3개, 강조 강점 3개, 추가 확인 항목 18개였다.
- 전체 단위 테스트 53개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 실제 사용자가 분석 JSON의 판정과 읽기 편의성을 검토하지 않았다.
- 추가 확인 18개 중 지원 판단에 중요한 항목만 먼저 보여주는 우선순위 축약은 아직 없다.
- 실행 결과를 Slack 메시지 형식으로 변환하지 않는다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 기존 조회와 매칭 함수를 다시 구현하지 않고 워크플로에서 조합했다.
  - 이유: 검증된 단계를 유지하면서 Slack 같은 인터페이스가 호출할 하나의 경계를 만들기 위해서다.
- 분석 결과에 사용자 프로필 전체를 포함하지 않았다.
  - 이유: 입력 ID와 사용된 근거만으로 판정을 추적할 수 있고 개인 자료의 불필요한 복제를 줄일 수 있기 때문이다.
- 사용자 검토 상태를 자동으로 완료 처리하지 않았다.
  - 이유: 프로그램 실행 성공과 사용자 판정 동의를 구분해야 하기 때문이다.

### 다음 할 일

1. 실제 공고의 복합 필수 조건 중 사용자 근거로 일부 확인 가능한 항목을 보수적으로 판정한다.
2. 화면에 표시할 미확인 항목을 지원 판단 영향 순서로 축약한다.
3. 이후 Greenhouse 보드 목록 발견과 상세 분석을 한 흐름으로 연결한다.

## 2026-09-13 - 복합 소프트웨어 경력 조건의 부분 근거 연결

### 목표

실제 공고의 소프트웨어 엔지니어링 복합 경력 조건에서 프로필에 확인된 웹개발·서버 운영 경력을 활용하되, 확인되지 않은 연수와 운영 시스템 소유 범위를 충족으로 추정하지 않는다.

### 수행한 작업

- 경험 매처가 사용자 프로필의 `career_history`를 읽도록 확장했다.
- `software engineering experience`를 별도 경험 개념으로 인식하도록 했다.
- 웹개발·유지보수, 서버 운영, 백엔드 관련 경력을 소프트웨어 엔지니어링 경험의 관련 근거로 연결했다.
- 복합 조건에 관련 경력이 있으면 `partial`과 `related`로 판정하고 경력 증거 ID를 결과에 남겼다.
- 총 경력 연수와 AI/ML 또는 백엔드 운영 시스템 직접 소유 범위를 미확인 항목으로 유지했다.
- 경력이 없을 때는 기존처럼 `unknown`을 유지하는 테스트를 추가했다.
- 실제 Sendbird 공고를 다시 분석해 변경된 판정을 확인했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/experience.py`
- `tests/test_experience_matching.py`
- `docs/MATCHING_RULES.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 실제 공고의 `software engineering experience` 판정이 `unknown`에서 `partial`로 변경됐다.
- 사용자 근거로 `career-001`의 웹개발·유지보수와 서버 운영 경력이 연결됐다.
- 실제 공고의 필수 조건 `unknown`은 3개에서 2개로, 전체 추가 확인 항목은 18개에서 17개로 줄었다.
- 사용자 총 경력 연수는 계속 확인되지 않아 경력 지원 조건은 `needs_confirmation`으로 유지됐다.
- 최종 지원 판단은 `조건부 지원`으로 유지됐다.
- 전체 단위 테스트 55개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 사용자 총 경력 연수는 공개 예제 프로필에서 확인되지 않는다.
- 실제 업무에서 AI/ML 또는 백엔드 운영 시스템을 직접 소유한 범위는 현재 프로필 문장만으로 확인되지 않는다.
- 일반적인 소프트웨어 경력 표현 전체를 현재 제한된 용어 목록이 인식하지는 않는다.
- `partial` 항목 내부의 미확인 세부 조건은 최상위 추가 확인 목록에 별도 승격되지 않는다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 관련 실무 경력을 `unknown`으로 버리지 않고 `partial`로 연결했다.
  - 이유: 현재 프로필에는 실제 웹개발·유지보수와 서버 운영 경력이 명시되어 있기 때문이다.
- 실무 경력이 있다는 이유만으로 복합 조건 전체를 `match` 또는 `strong_match`로 올리지 않았다.
  - 이유: 공고는 3년 이상과 운영 환경 AI/ML 또는 백엔드 시스템 직접 소유라는 추가 수준을 요구하기 때문이다.
- 경력 연수 판정은 기존 지원 가능 조건 매처에 남겼다.
  - 이유: 경험의 관련성과 최소 연수 충족은 서로 다른 판단이므로 한 결과로 합치지 않기 위해서다.

### 다음 할 일

1. 필수 조건, 지원 가능 조건과 주요 업무의 미확인 항목을 영향도 순으로 정렬해 화면에는 상위 항목만 표시한다.
2. `partial` 복합 조건 안의 미확인 세부 조건도 사용자 확인 목록에 포함한다.
3. 이후 Greenhouse 보드 목록에서 프로필 관련 공고를 자동 선별한다.

## 2026-09-13 - 미확인 항목 우선순위와 상위 질문 출력

### 목표

실공고 분석에서 발생한 많은 미확인 항목을 지원 판단 영향도 순으로 정렬하고 사용자 화면에는 먼저 확인할 일부 항목만 보여준다.

### 수행한 작업

- 미확인 항목에 `critical`, `high`, `medium`, `low` 우선순위를 추가했다.
- 전체 목록을 필수 조건, 지원 가능 조건, 주요 업무, 우대 조건 순으로 생성하도록 변경했다.
- `partial` 복합 조건 안의 미확인 세부 범위를 최상위 확인 목록에 포함했다.
- 경력 연수는 지원 가능 조건에서 이미 확인하므로 복합 경험 내부 목록에는 시스템 직접 소유 범위만 남겼다.
- 지원 가능 조건의 내부 이름을 경력 연수, 학력 조건, 고용 형태와 근무 지역·방식으로 표시하도록 했다.
- Greenhouse 단일 분석 CLI가 우선 확인할 상위 5개와 나머지 개수를 출력하도록 했다.
- 전체 미확인 목록은 분석 JSON에서 삭제하지 않고 그대로 유지했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/experience.py`
- `src/career_agent/matching/insights.py`
- `scripts/analyze_greenhouse_job.py`
- `tests/test_experience_matching.py`
- `tests/test_match_insights.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 실제 Sendbird 공고의 우선 확인 항목은 운영 시스템 직접 소유 범위, 시스템 설계·클라우드, 영어, 경력 연수, 고용 형태 순으로 출력됐다.
- 여섯 번째인 근무 지역·방식과 이후 주요 업무·우대 조건은 저장 JSON에 유지됐다.
- 실제 전체 미확인 항목은 `partial` 세부 범위가 추가되어 18개이며 콘솔에는 5개와 나머지 13개 안내만 표시됐다.
- 내부 코드값 `experience`, `employment` 대신 `경력 연수`, `고용 형태`가 출력됐다.
- 전체 단위 테스트 57개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 상위 5개라는 개수가 실제 사용자와 Slack 메시지에 적절한지는 사용자 검토 전이다.
- 시스템 설계 복합 조건 이름이 길어 현재 화면에서 말줄임된 상태다.
- 미확인 질문에 사용자가 답했을 때 프로필 또는 분석 결과를 갱신하는 기능은 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 미확인 항목을 삭제하지 않고 표시만 축약했다.
  - 이유: 화면 가독성을 높이면서도 나중에 전체 판정 근거를 다시 검토할 수 있어야 하기 때문이다.
- 우대 조건을 주요 업무와 지원 가능 조건 뒤로 배치했다.
  - 이유: 우대 조건은 부족하거나 미확인이어도 일반적으로 지원 가능 여부를 직접 막지 않기 때문이다.
- 복합 경험의 경력 연수는 별도 지원 가능 조건 질문과 중복 생성하지 않았다.
  - 이유: 같은 사실을 두 번 묻지 않고 운영 시스템 소유 범위라는 별도 미확인 사실에 집중하기 위해서다.

### 다음 할 일

1. 긴 복합 조건을 원문 근거는 보존하면서 사람이 읽기 쉬운 표준 이름으로 표시한다.
2. Greenhouse 보드 공고 목록을 조회하고 프로필의 목표 직무 축으로 관련 후보를 선별한다.
3. 선별 후보의 상세 분석을 자동으로 이어 붙이기 전에 목록 결과를 사용자에게 검토받는다.

## 2026-09-14 - Greenhouse 보드 목록 자동 발견과 프로필 선별

### 목표

사용자가 Greenhouse 공고 ID를 직접 찾지 않아도 기업 보드의 현재 게시 공고를 조회하고 프로필에서 도출한 목표 직무와 선호 조건으로 관련 후보를 선별한다.

### 수행한 작업

- Greenhouse 공식 목록 GET 엔드포인트를 읽는 `fetch_greenhouse_jobs`를 구현했다.
- 상세 본문 요청 옵션 없이 공고 ID, 제목, 회사, 근무지, 게시 시점과 원문 URL만 가져오도록 했다.
- 인크루트에 있던 프로필 기반 후보 정렬을 공급자 공통 `ranking.py`로 분리했다.
- Greenhouse 목록 항목을 기존 `job_discovery` 구조로 변환했다.
- Greenhouse 기업 보드와 외부 공고 ID 조합으로 중복 키를 만들었다.
- `Seoul`, `Gyeonggi`, `Incheon`을 기존 서울·경기·인천 선호와 비교할 수 있도록 제한된 지역 별칭을 추가했다.
- 제목에 명시된 인턴, 계약직과 파트타임만 고용 형태로 인식하고 정규직 선호와 다르면 후보를 삭제하지 않고 한 단계 낮췄다.
- 프로필의 최우선 AI 자동화 직무에서 파생한 `AI Agent`를 검색 계획의 발견 표현에 추가했다.
- 목록 조회, 변환, 로컬 저장과 후보 출력을 실행하는 `discover_greenhouse.py`를 추가했다.
- 기본 화면은 `high`와 `medium`만 표시하고 `--include-review`에서 전체 후보를 확인하도록 했다.
- 실제 Sendbird 보드를 연속 두 번 실행해 신규와 중복 처리를 확인했다.

### 생성 또는 수정한 파일

- `src/career_agent/ingestion/greenhouse.py`
- `src/career_agent/ingestion/__init__.py`
- `src/career_agent/discovery/greenhouse_board.py`
- `src/career_agent/discovery/ranking.py`
- `src/career_agent/discovery/incruit_rss.py`
- `src/career_agent/discovery/service.py`
- `src/career_agent/discovery/__init__.py`
- `scripts/discover_greenhouse.py`
- `tests/test_greenhouse_ingestion.py`
- `tests/test_greenhouse_discovery.py`
- `data/job_search_plan.example.json`
- `docs/GREENHOUSE_API_MAPPING.md`
- `docs/JOB_DISCOVERY_SCHEMA.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 실제 Sendbird 보드는 현재 게시 공고 10건을 반환했다.
- 첫 실행에서 10건이 신규 저장됐고 두 번째 실행에서 신규 0건, 중복 10건으로 판정됐다.
- `Software Engineer, AI Agent`는 최우선 목표 표현과 서울 지역이 일치해 `high`로 판정됐다.
- `AI Agent Engineer, Intern`은 직무 표현과 서울 지역은 맞지만 인턴이 정규직 선호와 달라 `medium`으로 판정됐다.
- 나머지 8건은 제목만으로 목표 직무 관련성이 확인되지 않아 `review`로 유지됐다.
- 기본 화면에는 `high`와 `medium` 2건만 표시됐으며 8건은 저장 데이터에서 삭제되지 않았다.
- 기존 인크루트 변환 기대 결과는 공통 정렬 모듈로 옮긴 뒤에도 동일하게 유지됐다.
- 전체 단위 테스트 61개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- Sendbird 외 다른 Greenhouse 기업 보드에서 같은 메타데이터 변환이 유용한지는 실제 실행하지 않았다.
- 제목에 목표 직무 표현이 없지만 상세 업무는 관련된 공고를 목록 단계에서 찾아내지는 못한다.
- 선별된 `high` 후보를 상세 분석 명령으로 자동 전달하지 않는다.
- 현재 보드 목록은 코드의 기본값 1곳이며 관심 기업 설정 파일은 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 목록 단계에서는 상세 본문을 요청하지 않았다.
  - 이유: 먼저 최소 메타데이터로 후보를 좁히고 선택된 공고만 상세 조회해 외부 데이터 접근량을 줄이기 위해서다.
- `AI Agent` 표현을 사용자 입력이 아니라 기존 목표 직무에서 파생했다.
  - 이유: 사용자가 검색 키워드를 다시 설계하게 하지 않고 Agent가 프로필과 실제 시장 표현의 차이를 보완해야 하기 때문이다.
- 인턴 후보를 제거하지 않고 `medium`으로 낮췄다.
  - 이유: 정규직은 현재 선호이며 강제 제외 조건으로 선언되지 않았고 인턴 공고에는 정규직 전환 기회가 명시될 수 있기 때문이다.
- 제목만으로 관련성이 없는 후보를 삭제하지 않고 `review`로 보존했다.
  - 이유: 목록 메타데이터만으로 실제 업무 관련성을 완전히 판정할 수 없기 때문이다.

### 다음 할 일

1. `high` 후보의 공고 ID를 목록 결과에서 가져와 상세 분석 워크플로로 자동 전달한다.
2. 자동 분석은 우선 `high` 1건으로 제한하고 실패 시 다른 후보로 무분별하게 확장하지 않는다.
3. 결과를 확인한 뒤 관심 기업 보드 목록을 로컬 설정으로 분리한다.

## 2026-09-14 - 공고 ID 없는 Greenhouse 자동 발견·상세 분석 연결

### 목표

사용자가 공고 ID를 찾거나 채용공고를 복사해 넣지 않아도 현재 Greenhouse 목록에서 프로필 관련성이 높은 공고 1건을 선택하고 기존 상세 분석까지 연결한다.

### 수행한 작업

- Greenhouse 목록 실행 결과에 현재 조회된 외부 공고 ID 목록을 추가했다.
- 현재 보드에 게시 중이고 우선순위가 `high`인 후보만 상세 분석 대상으로 제한했다.
- 조건에 맞는 후보가 여러 개이면 게시 시점이 가장 최근인 1건만 선택하도록 했다.
- 현재 `high` 후보가 없으면 상세 본문을 조회하지 않고 정상 종료하도록 했다.
- 로컬 발견 저장소에만 남아 있고 현재 목록에는 없는 과거 공고를 선택하지 않도록 했다.
- 선택한 공고의 조회나 분석이 실패했을 때 다른 후보로 자동 확장하지 않도록 했다.
- 목록 조회, 후보 선택, 상세 구조화, 프로필 매칭과 실행별 JSON 저장을 한 명령으로 연결했다.
- 현재 후보 제한과 무후보 종료를 합성 데이터 단위 테스트로 검증했다.
- 실제 Sendbird 보드에서 공고 ID 인자 없이 전체 명령을 실행했다.

### 생성 또는 수정한 파일

- `src/career_agent/discovery/service.py`
- `src/career_agent/workflows/greenhouse_agent.py`
- `src/career_agent/workflows/__init__.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_greenhouse_agent_workflow.py`
- `docs/GREENHOUSE_API_MAPPING.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `python scripts/run_greenhouse_agent.py`가 공고 ID 인자 없이 성공했다.
- 실제 Sendbird 현재 공고 10건 중 `Software Engineer, AI Agent`를 선택했다.
- 선택된 실제 Greenhouse 공고 ID는 `8395379002`였다.
- 선택 정책은 `newest_current_high_only`로 결과에 기록됐다.
- 분석 결과는 `조건부 지원`, 지원 시 강조할 강점 3개, 우선 확인 항목 5개였다.
- 결과는 `private-data/agent-runs/` 아래 고유 분석 ID 파일로 저장됐다.
- 바깥 실행 상태는 `analyzed`, 내부 분석 상태는 `completed`였다.
- 저장된 결과에 사용자 프로필 원문이 복제되지 않았음을 확인했다.
- 전체 단위 테스트 63개가 통과했다.
- Python 문법 컴파일 검사와 `git diff --check`가 통과했다.

### 아직 확인하지 못한 점

- 여러 관심 기업의 Greenhouse 보드를 어떤 순서와 한도로 실행할지는 아직 정하지 않았다.
- 현재 기본 실행 보드는 Sendbird 1곳만 설정돼 있다.
- 같은 공고와 판정 규칙이 변하지 않아도 실행할 때마다 상세 분석을 다시 수행한다.
- 하루 1회 같은 예약 실행과 실행 성공·실패 이력은 아직 연결하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 현재 목록의 `high` 후보 1건만 자동 분석했다.
  - 이유: 개인용 MVP에서 실제 유용성을 먼저 검증하고 외부 조회량과 잘못된 자동 확장을 제한하기 위해서다.
- 과거 저장 공고는 현재 목록에 없으면 선택하지 않았다.
  - 이유: 마감됐을 수 있는 공고를 Agent가 현재 기회처럼 추천하지 않아야 하기 때문이다.
- `high` 후보가 없을 때 `medium`이나 `review`를 자동 상세 조회하지 않았다.
  - 이유: 목록 근거가 부족한 후보를 사용자의 확인 없이 연쇄 분석하지 않기 위해서다.
- 실행 결과마다 새 분석 ID 파일을 만들었다.
  - 이유: 이전 결과를 덮어쓰지 않고 판정 변화와 사용자 검토 이력을 나중에 비교할 수 있어야 하기 때문이다.

### 다음 할 일

1. 공고 내용과 판정 규칙이 변하지 않았으면 반복 상세 분석을 건너뛰는 기준을 추가한다.
2. 관심 기업 Greenhouse 보드 목록을 공개 예제와 비공개 사용자 설정으로 분리한다.
3. 반복 실행, 조회 실패와 분석 실패가 다른 공고의 무분별한 조회로 이어지지 않는지 테스트를 보강한다.

## 2026-09-14 - 동일 Greenhouse 분석 재사용

### 목표

같은 현재 공고를 반복 실행할 때 상세 본문 조회와 매칭을 불필요하게 되풀이하지 않고, 입력이나 규칙이 달라진 경우에만 새 분석을 만든다.

### 수행한 작업

- Greenhouse 목록 레코드에 공급자가 제공한 `updated_at`을 추가했다.
- Agent 후보 선택을 과거 로컬 저장 레코드가 아니라 이번 실행에서 조회하고 다시 순위를 계산한 현재 레코드 기준으로 변경했다.
- 분석 결과에 프로필 전체 내용의 SHA-256 지문과 분석 파이프라인 버전을 추가했다.
- 기존 매칭 규칙 버전을 상수로 분리해 실제 판정과 재사용 검사가 같은 값을 사용하도록 했다.
- 공고 ID와 갱신 시각, 프로필 지문, 매칭 규칙 버전과 분석 파이프라인 버전이 모두 같을 때만 기존 분석을 재사용하도록 했다.
- 이전 결과에 비교 값이 없거나 값이 달라지면 새 상세 분석을 실행하도록 했다.
- 재사용 시 기존 파일 경로를 표시하고 새 JSON을 만들지 않도록 실행 명령을 변경했다.
- 같은 입력 재사용과 프로필 변경 시 새 분석을 각각 단위 테스트로 추가했다.
- 실제 Sendbird 명령을 두 번 연속 실행해 새 분석과 재사용을 확인했다.

### 생성 또는 수정한 파일

- `src/career_agent/discovery/greenhouse_board.py`
- `src/career_agent/discovery/service.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `src/career_agent/workflows/greenhouse_analysis.py`
- `src/career_agent/workflows/greenhouse_agent.py`
- `src/career_agent/workflows/__init__.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_greenhouse_discovery.py`
- `tests/test_greenhouse_analysis_workflow.py`
- `tests/test_greenhouse_agent_workflow.py`
- `data/match_result.example.json`
- `docs/JOB_DISCOVERY_SCHEMA.md`
- `docs/MATCH_RESULT_SCHEMA.md`
- `docs/GREENHOUSE_API_MAPPING.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 첫 실제 실행은 이전 결과에 새 비교 정보가 없어 안전하게 새 분석을 만들었다.
- 첫 실행 뒤 `private-data/agent-runs/`의 파일 수는 1개에서 2개로 증가했다.
- 두 번째 실제 실행은 `공고 갱신 시각, 프로필 내용과 분석 규칙 버전이 동일함`을 이유로 기존 분석을 재사용했다.
- 두 번째 실행 뒤 파일 수는 2개로 유지돼 중복 결과가 생기지 않았다.
- 두 실행 모두 현재 Sendbird의 `Software Engineer, AI Agent`를 선택했다.
- 새 결과의 프로필 지문은 SHA-256 길이 64자이고 분석 파이프라인 버전은 `0.1`이었다.
- 저장된 결과에 사용자 프로필 원문이 복제되지 않았다.
- 전체 단위 테스트 65개가 통과했다.

### 아직 확인하지 못한 점

- Greenhouse의 `updated_at` 의미가 회사별 보드에서도 같은 방식으로 유지되는지는 여러 기업에서 확인하지 않았다.
- 매칭 또는 구조화 로직을 바꿀 때 버전 상수를 올리는 절차는 아직 자동 검사하지 않는다.
- 손상된 과거 실행 파일이 있을 때 운영자가 어떻게 복구할지 안내가 없다.
- 예약 실행과 여러 관심 기업 보드 설정은 아직 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 현재 조회 레코드를 후보 선택의 기준으로 삼았다.
  - 이유: 과거 중복 저장본은 새로운 프로필 검색 계획으로 다시 계산된 우선순위를 반영하지 못하기 때문이다.
- 프로필 스키마 버전만으로 재사용 여부를 판단하지 않았다.
  - 이유: 스키마 형태가 같아도 사용자의 경력, 기술과 목표 내용은 바뀔 수 있기 때문이다.
- `updated_at`이 없으면 기존 분석을 재사용하지 않았다.
  - 이유: 공고 내용이 그대로라고 확인할 근거가 없을 때는 새 분석이 더 안전하기 때문이다.
- 프로필 지문은 Git 제외 분석 파일에만 저장하고 프로필 원문은 복제하지 않았다.
  - 이유: 비교 가능성과 개인정보 최소 저장 원칙을 함께 지키기 위해서다.

### 다음 할 일

1. 관심 기업 Greenhouse 보드 목록을 공개 예제 설정과 Git 제외 개인 설정으로 분리한다.
2. 여러 보드에서 발견된 후보를 전체 합산한 뒤 자동 상세 분석 수를 제한하는 정책을 정의한다.
3. 손상된 실행 파일, 목록 조회 실패와 상세 분석 실패 테스트를 보강한다.

## 2026-09-14 - Greenhouse 공식 소스 등록부 분리

### 목표

검증된 Greenhouse 기업 보드를 코드에 직접 고정하지 않고 공개 예제와 Git 제외 개인 설정으로 관리하되, 현재 상세 분석 1건 제한을 유지한다.

### 수행한 작업

- Greenhouse 기업 보드 설정 로더를 별도 `config` 모듈로 추가했다.
- 공개 예제 소스 등록부에 실제 검증을 마친 Sendbird 1곳만 등록했다.
- 회사명, board token, 공식 채용 URL, 허용 접근 방식, 정책 확인 날짜와 활성 상태를 설정 필드로 정의했다.
- HTTPS가 아닌 채용 URL과 허용되지 않은 접근 방식을 거부하도록 했다.
- board token 형식과 대소문자를 무시한 중복을 검증하도록 했다.
- 현재 개인용 MVP에서는 활성 보드를 정확히 1개만 허용하도록 했다.
- Agent 실행 명령의 하드코딩된 Sendbird와 정책 확인 날짜를 설정 파일 입력으로 교체했다.
- 기본 공개 설정과 `--board-config` 개인 설정 경로 사용법을 README에 추가했다.
- 관심 기업 조합이 개인 구직 의도를 드러낼 수 있어 개인 등록부는 `private-data/`에 두도록 문서화했다.

### 생성 또는 수정한 파일

- `src/career_agent/config/__init__.py`
- `src/career_agent/config/greenhouse_boards.py`
- `data/greenhouse_boards.example.json`
- `docs/GREENHOUSE_BOARD_CONFIG_SCHEMA.md`
- `scripts/run_greenhouse_agent.py`
- `tests/test_greenhouse_board_config.py`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 공개 예제 설정에서 `sendbird` 활성 보드를 정상적으로 읽었다.
- 활성 보드가 2개이면 외부 조회 전에 설정 오류로 종료하는 검증이 통과했다.
- 중복 board token, HTTP 채용 URL과 `html_scraping` 접근 방식 거부 테스트가 통과했다.
- 설정 파일을 사용하는 기본 Agent 명령이 실제 Sendbird 현재 목록을 조회하고 기존 분석을 재사용했다.
- 사용자는 기존과 동일하게 `python scripts/run_greenhouse_agent.py`로 실행할 수 있다.
- 전체 단위 테스트 71개가 통과했다.

### 아직 확인하지 못한 점

- 여러 활성 보드의 후보를 합쳐 전역 순위를 계산하는 기능은 없다.
- Agent가 기업 채용 URL에서 Greenhouse 사용 여부와 board token을 자동 발견하는 기능은 없다.
- Sendbird 외 기업의 board token과 접근 정책은 등록하거나 검증하지 않았다.
- 정책 확인 날짜가 오래됐을 때 자동으로 재확인을 요청하는 기준은 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 공개 예제에는 Sendbird 1곳만 넣었다.
  - 이유: 실제 공식 API 실행으로 확인하지 않은 기업을 검증된 소스처럼 등록하지 않기 위해서다.
- 현재 활성 보드를 정확히 1개로 제한했다.
  - 이유: 여러 보드를 단순 반복하면 기업 수만큼 상세 분석이 실행돼 현재 MVP의 1건 제한을 깨기 때문이다.
- `html_scraping` 같은 접근 방식을 설정으로 허용하지 않았다.
  - 이유: 현재 구현과 검증 범위는 Greenhouse 공식 공개 Job Board API의 읽기 전용 GET 요청뿐이기 때문이다.
- 관심 기업 등록부를 프로필 검색 키워드와 분리했다.
  - 이유: 사용자가 직무 조건을 다시 설계하게 하지 않으면서도 Agent가 확인한 외부 소스의 출처와 정책을 추적해야 하기 때문이다.

### 다음 할 일

1. 여러 보드의 목록만 먼저 조회하고 모든 `high` 후보를 합산해 전역 1건만 선택하는 워크플로를 설계한다.
2. 보드 하나의 조회 실패가 전체 실행에 미칠 영향을 명시하고 테스트한다.
3. 공식 기업 채용 URL에서 Greenhouse 보드를 자동 발견하는 안전한 방법을 조사한다.

## 2026-09-14 - 여러 Greenhouse 보드의 전역 후보 1건 선택

### 목표

여러 검증된 Greenhouse 기업 보드의 목록을 조회하되 기업 수만큼 상세 분석하지 않고, 성공한 모든 보드의 현재 후보 전체에서 `high` 1건만 선택한다.

### 수행한 작업

- Greenhouse 설정 로더가 활성 보드 1개 이상을 반환하도록 확장했다.
- 개인용 MVP의 목록 요청량을 제한하기 위해 활성 보드 상한을 10개로 설정했다.
- 기존 단일 보드 호출은 호환 함수로 유지했다.
- 여러 보드의 목록을 먼저 순서대로 조회하고 현재 발견 레코드를 합산하는 워크플로를 추가했다.
- 합산된 `high` 후보의 게시 시각을 시간대가 반영된 실제 시각으로 비교하도록 했다.
- 전체에서 가장 최근인 `high` 후보 1건만 상세 분석 또는 기존 분석 재사용 대상으로 선택했다.
- 일부 보드 목록 조회가 실패하면 해당 오류를 기록하고 나머지 보드를 계속 처리하도록 했다.
- 모든 보드 목록 조회가 실패하면 상세 분석 없이 오류로 종료하도록 했다.
- 선택한 상세 공고 분석이 실패해도 다른 후보를 연쇄 분석하지 않는 기존 경계를 유지했다.
- 콘솔에 보드 조회 성공 수와 실패 수, 실패한 board token을 표시하도록 했다.
- 공식 Moloco 채용페이지와 Greenhouse 공개 API 보드를 읽기 전용으로 확인하고 비공개 설정에서 실제 2개 보드 실행을 검증했다.

### 생성 또는 수정한 파일

- `src/career_agent/config/greenhouse_boards.py`
- `src/career_agent/config/__init__.py`
- `src/career_agent/workflows/greenhouse_agent.py`
- `src/career_agent/workflows/__init__.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_greenhouse_board_config.py`
- `tests/test_greenhouse_agent_workflow.py`
- `docs/GREENHOUSE_BOARD_CONFIG_SCHEMA.md`
- `docs/GREENHOUSE_API_MAPPING.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- Moloco 공식 채용페이지 `https://www.moloco.com/careers`가 현재 운영 중임을 확인했다.
- Greenhouse 공식 공개 API의 `moloco` 보드는 현재 공고 46건을 반환했고 서울 공고가 포함돼 있었다.
- Git 제외 개인 설정으로 Sendbird와 두 번째 검증 보드를 함께 활성화했다.
- 실제 Agent 실행에서 보드 조회 성공 2개, 실패 0개가 출력됐다.
- 두 보드의 현재 후보를 합산한 뒤 `Software Engineer, AI Agent` 1건만 선택했다.
- 선택된 공고는 기존 분석 재사용 조건과 일치해 상세 분석을 반복하지 않았다.
- 실제 분석 파일 수는 실행 전후 `2개 → 2개`로 유지됐다.
- 시간대 표기가 다른 두 게시 시각의 실제 순서를 비교하는 테스트가 통과했다.
- 일부 보드 실패 시 다른 보드 후보 1건 분석, 모든 보드 실패 시 상세 분석 0건 테스트가 통과했다.
- 전체 단위 테스트 77개가 통과했다.

### 아직 확인하지 못한 점

- 현재 전역 후보 정렬은 같은 `high` 등급 안에서 게시 시각만 비교하며 기업 선호도나 역할별 목표 우선순위 점수는 별도로 비교하지 않는다.
- 재사용 실행에서는 목록 조회 결과와 부분 실패를 별도 실행 이력 파일로 저장하지 않는다.
- 공식 기업 채용 URL에서 Greenhouse board token을 자동 발견하고 검증하는 기능은 없다.
- 일부 보드 실패 정책은 합성 테스트만 했으며 실제 네트워크 실패를 의도적으로 만들지는 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 여러 보드를 조회해도 상세 분석 상한을 전역 1건으로 유지했다.
  - 이유: 개인용 MVP에서 외부 조회와 분석 결과가 기업 수에 따라 갑자기 늘어나는 것을 막기 위해서다.
- 보드 목록 실패는 서로 격리했다.
  - 이유: 한 기업의 일시적 장애 때문에 다른 공식 소스의 현재 기회를 모두 놓치지 않아야 하기 때문이다.
- 모든 보드가 실패하면 과거 저장 후보로 대신 분석하지 않았다.
  - 이유: 현재 게시 상태를 확인하지 못한 공고를 현재 기회로 취급하면 안 되기 때문이다.
- 시간대 문자열을 단순 비교하지 않았다.
  - 이유: 서로 다른 UTC 오프셋의 표기 순서와 실제 게시 시각 순서가 다를 수 있기 때문이다.
- 실제 두 번째 소스 설정은 `private-data/`에만 저장했다.
  - 이유: 공개 저장소에는 최소 예제만 두고 실제 실행용 소스 조합은 로컬에 보존하기 위해서다.

### 다음 할 일

1. 보드별 목록 조회 결과와 실패를 상세 분석 재사용 여부와 분리된 실행 이력으로 저장한다.
2. 기업 선호도를 사용자에게 다시 입력받지 않고 프로필과 공개 기업 정보에서 판단할 수 있는 근거 구조를 설계한다.
3. 공식 기업 채용 URL에서 Greenhouse board token 후보를 자동 발견하고 검증하는 방법을 조사한다.

## 2026-09-14 - Greenhouse 성공·실패 실행 이력 분리

### 목표

새 상세 분석이 생기지 않는 재사용과 무후보 실행도 실제 외부 소스 확인 여부를 추적하고, 실패 실행을 다음 점검에서 확인할 수 있도록 최소 이력을 남긴다.

### 수행한 작업

- Greenhouse 실행 이력 구조와 원자적 JSON 저장 모듈을 추가했다.
- 실행 ID에 시간대와 마이크로초를 포함해 빠른 연속 실행에서도 파일명이 겹치지 않도록 했다.
- 새 분석, 기존 분석 재사용, `high` 무후보와 실패 상태를 구분했다.
- 보드별 성공·실패, 처리 공고 수, 항목 오류 수, 신규와 중복 건수를 최소 이력에 기록했다.
- 상세 분석 결과는 분석 ID와 파일명만 참조하도록 했다.
- 사용자 프로필, 공고 본문과 전체 현재 공고 목록은 실행 이력에 복제하지 않았다.
- 모든 보드 실패 예외에 보드별 실행 요약을 연결해 실패 이력에서도 확인할 수 있게 했다.
- CLI가 정상 실행과 실패 실행 모두 별도 실행 이력 파일을 저장하도록 했다.
- 오래된 PRD 체크리스트를 현재 구현 사실에 맞게 갱신하고 확장된 개인용 MVP 진행률을 사용자 가치 기준으로 산정했다.

### 생성 또는 수정한 파일

- `src/career_agent/execution/__init__.py`
- `src/career_agent/execution/log.py`
- `src/career_agent/workflows/greenhouse_agent.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_execution_log.py`
- `tests/test_greenhouse_agent_workflow.py`
- `docs/EXECUTION_LOG_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 실제 다중 보드 재사용 실행 뒤 `reused` 상태의 실행 이력 파일이 새로 생성됐다.
- 실제 이력에는 보드 시도 2개와 기존 분석 참조가 기록됐다.
- 실제 이력의 `contains_profile_content`와 `contains_job_description_content`는 모두 `false`였다.
- 의도적으로 활성 보드가 없는 설정을 실행했을 때 종료 코드 1과 `failed` 이력이 생성됐다.
- 실패 이력에는 오류가 기록됐고 임시 실패 테스트 설정은 확인 후 제거했다.
- 실행 이력 경로가 `private-data/` 규칙으로 Git에서 제외됨을 확인했다.
- 전체 단위 테스트 80개가 통과했다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 58%로 판단했다.

### 아직 확인하지 못한 점

- 예약 실행이 없으므로 실행 이력은 사용자가 명령을 실행할 때만 쌓인다.
- 이력 목록을 사람이 읽기 쉽게 요약하는 명령은 없다.
- 실행 이력 보존 기간과 오래된 파일 정리 정책은 없다.
- 실제 분석 결과에서 학습 추천과 포트폴리오 개선 추천은 아직 비어 있다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 분석 결과와 실행 이력을 분리했다.
  - 이유: 공고와 프로필이 바뀌지 않아 분석을 재사용해도 외부 소스를 실제로 확인한 실행 사실은 별도로 남아야 하기 때문이다.
- 실행 이력에는 처리 통계와 참조만 저장했다.
  - 이유: 진단 가능성을 확보하면서 개인정보와 외부 공고 데이터의 불필요한 복제를 피하기 위해서다.
- 실패 이력 저장 자체가 실패하면 원래 오류를 가리지 않도록 했다.
  - 이유: 보조 기록 기능 때문에 실제 실패 원인을 잃으면 안 되기 때문이다.
- 진행률은 단순 체크박스 비율로 계산하지 않았다.
  - 이유: 학습·포트폴리오 추천, 실제 10건 검토와 대화 인터페이스는 남은 항목 수보다 사용자 가치와 구현 위험이 크기 때문이다.

### 다음 할 일

1. 현재 `gaps`와 `unknowns`를 실제 결과물 중심의 학습 추천으로 변환한다.
2. 기존 완료 프로젝트의 확장을 우선하는 포트폴리오 개선 추천을 구현한다.
3. 실제 관심 공고 상세 분석을 10건까지 늘리고 사용자 판단과 비교한다.

## 2026-09-14 - 확인된 기술 부족을 첫 학습 과제로 변환

### 목표

매칭 결과의 확인된 부족을 기술명 목록으로 끝내지 않고, 기존 완료 프로젝트에서 실행하고 검증할 수 있는 가장 작은 학습 과제 1개로 변환한다.

### 수행한 작업

- 확인된 `skill`과 `cloud` 부족을 학습 추천으로 변환하는 규칙 기반 모듈을 추가했다.
- 필수 조건을 우대 조건보다 먼저 처리하고 한 분석에서 추천을 최대 1개로 제한했다.
- 완료된 기존 프로젝트가 있으면 새 프로젝트 대신 그 프로젝트에 부족 기술을 적용하도록 했다.
- 행동, 산출물과 새 환경의 재현 성공 로그를 각각 기록하도록 했다.
- 프로필의 `unknown`과 추가 확인이 필요한 `partial`은 학습 과제로 만들지 않았다.
- 우대 조건에만 있는 클라우드 부족은 여러 실제 공고의 반복 수요를 아직 측정할 수 없으므로 보류했다.
- 매칭 규칙 버전을 `0.2`로 올려 이전 규칙의 저장 분석이 잘못 재사용되지 않게 했다.
- 예제 매칭, 단일 Greenhouse 분석과 자동 Agent CLI에서 학습 과제 수를 확인할 수 있게 했다.
- 학습 추천 동작과 현재 MVP 범위를 README, PRD와 결과 스키마 문서에 반영했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/learning.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `scripts/analyze_greenhouse_job.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_learning_recommendation.py`
- `tests/test_greenhouse_agent_workflow.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 예제 공고의 Docker 부족에서 `Tech News Automation`에 Docker를 적용하고 새 환경 재현 로그를 남기는 과제 1개가 생성됐다.
- 같은 예제의 AWS는 우대 클라우드 조건이므로 즉시 학습 과제로 생성되지 않았다.
- Kubernetes 사용 경험처럼 프로필에 정보가 없는 항목은 학습 과제가 아니라 확인 질문으로 유지됐다.
- 전체 단위 테스트 84개가 통과했다.
- 실제 Sendbird `Software Engineer, AI Agent` 공고를 새 규칙으로 다시 분석했다.
- 실제 공고에는 확인된 기술 부족이 없어서 학습 과제는 0개였고, 미확인 시스템 설계·영어·경력 정보가 학습 부족으로 오인되지 않았다.
- 로컬의 실제 Greenhouse 보드 2곳 조회가 모두 성공했고 최신 규칙 분석이 재사용됐다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 62%로 판단했다.

### 아직 확인하지 못한 점

- 중요한 `partial` 항목 중 사용자 증거가 충분한 경우를 학습 후보로 올리는 세부 규칙은 아직 없다.
- 여러 실제 공고에서 같은 기술이 반복되는 빈도를 집계해 우대 클라우드 학습 우선순위를 올리는 기능은 아직 없다.
- 생성된 학습 과제가 사용자에게 실제로 유용한지에 대한 사람의 검토는 예제 외에는 진행하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- `unknown`을 학습 부족으로 바꾸지 않았다.
  - 이유: 정보가 없다는 사실은 경험이 없다는 뜻이 아니며, 사용자가 이미 제공한 자료를 근거 없이 부족으로 판단하면 Agent의 신뢰를 떨어뜨리기 때문이다.
- 첫 추천은 1개로 제한했다.
  - 이유: 개인용 MVP에서 긴 기술 목록보다 바로 시작하고 완료 여부를 확인할 수 있는 한 가지 행동이 더 유용하기 때문이다.
- 기존 완료 프로젝트를 먼저 확장한다.
  - 이유: 새 프로젝트를 계속 만드는 것보다 이미 동작하는 결과물에 부족 역량을 더하는 편이 빠르고 증거도 명확하기 때문이다.
- 우대 클라우드 부족은 반복 수요 확인 전까지 보류한다.
  - 이유: AWS처럼 계정, 비용과 운영 범위가 큰 학습을 단일 우대 공고만 보고 즉시 시작하면 우선순위가 왜곡될 수 있기 때문이다.

### 다음 할 일

1. 학습 과제를 기존 프로젝트의 구체적인 변경점으로 연결하는 포트폴리오 개선 추천을 구현한다.
2. 실제 관심 공고 상세 분석을 10건까지 늘리고 추천 결과를 사용자 판단과 비교한다.
3. 반복 공고에서 기술 수요를 집계해 우대 기술의 학습 우선순위를 조정한다.

## 2026-09-14 - 기존 완료 프로젝트 중심 포트폴리오 개선 추천

### 목표

학습 과제와 약한 업무 증거를 새 프로젝트 생성으로 연결하지 않고, 이미 동작이 확인된 프로젝트에서 강화할 변경점과 기대 증거로 변환한다.

### 수행한 작업

- 학습 추천을 완료된 기존 프로젝트의 포트폴리오 개선점으로 변환하는 모듈을 추가했다.
- 학습 추천이 없을 때는 `match` 또는 `partial` 업무 판정 중 완료 프로젝트의 직접 증거와 구체적인 다음 행동이 모두 있는 항목만 사용하도록 했다.
- 포트폴리오 추천을 한 분석에서 최대 1개로 제한했다.
- 대상 프로젝트, 연결된 부족 또는 약한 증거, 변경점, 이유와 기대 증거를 결과에 저장하도록 했다.
- 정보가 없는 `unknown` 업무와 아직 계획 중인 프로젝트는 추천 근거에서 제외했다.
- 매칭 규칙 버전을 `0.3`으로 올려 포트폴리오 추천이 없는 과거 분석을 재사용하지 않도록 했다.
- 예제 매칭과 Greenhouse CLI에서 포트폴리오 개선 과제를 확인할 수 있게 했다.
- README, PRD와 결과 스키마 문서에 현재 규칙과 진행률을 반영했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/portfolio.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `scripts/match_job.py`
- `scripts/analyze_greenhouse_job.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_portfolio_recommendation.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 예제의 Docker 학습 과제가 `Tech News Automation` 포트폴리오 개선 과제로 연결됐다.
- 기대 증거에는 Docker 적용 코드·설정, 실행 문서와 새 환경 재현 성공 로그가 포함됐다.
- Docker 부족을 제거한 예제에서는 기존 모니터링 경험의 다음 단계인 실패 요약·능동 알림 추가가 같은 프로젝트의 개선 과제로 생성됐다.
- 미확인 분산 시스템 업무는 포트폴리오 과제로 생성되지 않았다.
- 완료 프로젝트가 없고 계획 중인 프로젝트만 있으면 추천 결과가 비어 있음을 확인했다.
- 전체 단위 테스트 88개가 통과했다.
- 실제 Greenhouse 보드 2곳 조회가 모두 성공했고 Sendbird `Software Engineer, AI Agent` 공고를 규칙 `0.3`으로 다시 분석했다.
- 실제 공고의 강한 업무 증거 1개와 미확인 업무 7개에서는 포트폴리오 개선 과제가 0개였다.
- 미확인 업무를 부족으로 가정하지 않는 안전 기준이 실제 분석에서도 유지됐다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 67%로 판단했다.

### 아직 확인하지 못한 점

- 포트폴리오 변경점의 개발 비용과 예상 소요 시간을 계산하지 않는다.
- 같은 프로젝트에 여러 개선 후보가 있을 때 사용자 가치와 구현 비용을 함께 비교하는 규칙은 없다.
- 실제 관심 공고 여러 건에서 같은 개선점이 반복되는지 아직 검토하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 완료된 프로젝트만 첫 개선 대상으로 사용했다.
  - 이유: 이미 실행이 확인된 결과물이어야 작은 변경의 효과와 새 증거를 분리해 검증할 수 있기 때문이다.
- `unknown` 업무를 개선 과제로 만들지 않았다.
  - 이유: 경험 정보가 없을 뿐인데 새 프로젝트나 기능 구현을 지시하면 사용자 자료를 분석한다는 원칙과 충돌하기 때문이다.
- 학습과 포트폴리오 추천을 같은 실행 증거로 연결했다.
  - 이유: 공부와 포트폴리오 작업을 따로 만들지 않아도 하나의 작은 구현으로 역량과 공개 증거를 함께 강화할 수 있기 때문이다.

### 다음 할 일

1. 실제 관심 공고 10건을 순차 분석하고 사용자 판단과 비교할 검토 자료를 만든다.
2. 반복 공고에서 기술 및 업무 수요를 집계해 학습·포트폴리오 우선순위를 교정한다.
3. 사용자의 이력서·포트폴리오 자료를 안전하게 갱신할 입력 흐름을 설계한다.

## 2026-09-14 - 실제 공고 10건 검토 큐 생성

### 목표

실제 공고 10건을 한꺼번에 상세 조회하지 않고, 현재 목록 메타데이터에서 프로필 기준 검토 후보를 자동 선정하고 최신 분석 여부를 표시한다.

### 수행한 작업

- 최근 Greenhouse Agent 분석 파일에 저장된 현재 보드 목록을 읽는 검토 큐 모듈을 추가했다.
- `high`, `medium`, `review` 후보를 사용하고 `low` 후보를 제외했다.
- 우선순위, 지역 일치, 고용 형태 일치, Agent 생성 검색 계획의 넓은 직무 단어 신호와 공고 시각 순서로 정렬했다.
- 같은 보드와 외부 공고 ID의 중복을 제거했다.
- 현재 프로필 내용, 공고 갱신 시각, 매칭 규칙과 분석 파이프라인 버전이 같은 기존 분석 여부를 표시했다.
- 사용자 검토 상태와 판단 필드는 자동으로 채우지 않고 `not_reviewed`와 `null`로 유지했다.
- 사용자 프로필과 공고 본문 없이 검토 큐 스냅샷을 원자적으로 새 파일에 저장하도록 했다.
- 큐 생성 명령과 검토 큐 스키마 문서를 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/review/__init__.py`
- `src/career_agent/review/queue.py`
- `scripts/build_greenhouse_review_queue.py`
- `tests/test_greenhouse_review_queue.py`
- `docs/REVIEW_QUEUE_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 검토 큐 관련 단위 테스트 6개를 포함해 전체 테스트 94개가 통과했다.
- 실제 최신 Greenhouse 보드 2곳의 목록 스냅샷에는 검토 가능한 현재 후보 56개가 있었다.
- 실제 큐는 `high` 1건, `medium` 1건, `review` 8건으로 총 10건이 생성됐다.
- 10건 중 현재 프로필과 규칙으로 상세 분석이 완료된 공고는 1건, 분석이 필요한 공고는 9건이었다.
- 첫 정렬에서는 최신순 `review` 후보 때문에 Tokyo와 미국 공고가 서울 공고보다 먼저 나타나는 문제를 발견했다.
- 지역과 Agent 생성 직무 표현의 넓은 단어 일치를 반영한 뒤 상위 10건이 AI Agent, Machine Learning Engineer, Software Engineer 등 서울 기술 직무 중심으로 바뀌었다.
- 실제 큐의 메타데이터는 프로필 및 공고 본문 포함 여부가 모두 `false`였다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 69%로 판단했다.

### 아직 확인하지 못한 점

- 큐의 미분석 9건은 상세 본문 분석 전이므로 실제 관심 직무인지 확정하지 않았다.
- 제목 단어 신호가 넓어 Machine Learning Engineer처럼 현재 프로젝트 증거와의 연결이 약할 수 있는 직무도 포함된다.
- 사용자 검토 값을 큐에 안전하게 기록하는 입력 명령은 아직 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 10건 상세 조회 전에 메타데이터 큐를 먼저 만들었다.
  - 이유: 일반 Agent의 상세 분석 1건 제한을 유지하면서 어떤 공고를 왜 검토하는지 먼저 확인할 수 있어야 하기 때문이다.
- `review` 후보도 검증 표본에 포함했다.
  - 이유: 완전한 직무 표현 일치만 검토하면 현재 발견 규칙이 놓친 관련 공고를 찾을 수 없기 때문이다.
- 지역과 직무 단어 신호를 큐 정렬에 추가했다.
  - 이유: 단순 최신순은 해외의 무관한 공고를 상위에 배치해 10건 표본의 사용자 가치를 떨어뜨렸기 때문이다.
- 사용자 검토 값을 자동 완성하지 않았다.
  - 이유: Agent 분석 결과와 사용자가 실제로 동의한 판단을 구분해야 하기 때문이다.

### 다음 할 일

1. 검토 큐에서 `needs_analysis`인 첫 후보 1건만 현재 공식 API로 상세 분석한다.
2. 상세 분석 결과를 큐의 분석 참조와 연결하되 사용자 검토 상태는 그대로 유지한다.
3. 같은 절차를 한 건씩 반복해 실제 상세 분석 표본을 10건까지 늘린다.

## 2026-09-14 - 검토 큐의 첫 미분석 공고 1건 상세 분석

### 목표

검토 큐에서 프로필 선호와 명백히 충돌하지 않는 첫 미분석 후보를 자동 선택해 공식 API로 1건만 상세 분석하고, 분석 결과·실행 이력·후속 큐를 연결한다.

### 수행한 작업

- 지역 또는 고용 형태가 명시적으로 불일치하는 후보를 검토 큐 뒤로 보내도록 정렬 기준을 수정했다.
- 큐에 원본 목록 분석 파일명과 프로필 내용 지문을 추가하고 스키마 버전을 `0.2`로 올렸다.
- 현재 프로필, 매칭 규칙과 분석 파이프라인 버전이 같은 큐만 상세 분석에 사용하도록 검증했다.
- 첫 `needs_analysis` 후보 중 명시적 지역·고용 불일치가 없는 공고 1건만 자동 선택하도록 했다.
- 상세 분석 결과가 선택한 보드와 공고 ID에 해당하는지 저장 전에 검증했다.
- 분석 결과에 원본 큐 ID, 파일명과 위치를 연결하고 사용자 검토 상태는 `not_reviewed`로 유지했다.
- 분석 파일, 최소 실행 이력과 분석 ID가 연결된 후속 큐를 모두 새 파일로 저장했다.
- 필수 조건 또는 주요 업무 추출 결과가 0개이면 콘솔에 구조화 품질 경고를 표시했다.

### 생성 또는 수정한 파일

- `src/career_agent/review/analysis.py`
- `src/career_agent/review/queue.py`
- `src/career_agent/review/__init__.py`
- `scripts/build_greenhouse_review_queue.py`
- `scripts/analyze_next_greenhouse_review.py`
- `tests/test_greenhouse_review_queue.py`
- `tests/test_greenhouse_review_analysis.py`
- `docs/REVIEW_QUEUE_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 검토 큐와 다음 후보 분석 관련 테스트 12개를 포함해 전체 단위 테스트 100개가 통과했다.
- 정렬 수정 후 Sendbird 인턴 공고 대신 Moloco의 서울 `Machine Learning Engineer`가 첫 미분석 후보가 됐다.
- 공식 Greenhouse 상세 API 요청 1건으로 해당 공고를 분석했다.
- 기존 Sendbird 분석을 포함해 현재 큐의 상세 분석 완료 수가 1건에서 2건으로 늘었고 미분석 수는 8건이 됐다.
- 새 분석 파일, 후속 큐와 성공 실행 이력이 각각 로컬에 저장됐다.
- 사용자 검토 상태는 `not_reviewed`로 유지됐다.
- 실제 Moloco 분석에서 우대 조건은 2개가 추출됐지만 필수 조건과 주요 업무는 각각 0개였다.
- 현재 결과는 `조건부 지원`, 우선 확인 4개, 학습 및 포트폴리오 과제 0개였지만 섹션 추출이 불완전하므로 최종 검토 표본으로 확정하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 71%로 판단했다.

### 아직 확인하지 못한 점

- Moloco 공고 본문에서 필수 조건과 주요 업무가 비어 버린 정확한 HTML 또는 제목 구조 원인은 아직 확인하지 않았다.
- 두 번째 분석 결과의 지원 판단이 사용자 경험과 실제 공고 요구를 올바르게 반영하는지는 구조화 교정 전에는 판단할 수 없다.
- 분석 파일 저장 후 후속 큐 저장이 실패하는 극단적인 경우 두 파일을 하나의 트랜잭션으로 되돌리는 기능은 없다.

### 막힌 점

현재 없음. 다음 단계에서 실제 Moloco 본문 구조와 현재 섹션 인식 규칙을 비교해야 한다.

### 주요 판단과 이유

- 명시적 인턴·해외 불일치를 우선순위보다 먼저 뒤로 보냈다.
  - 이유: 검토 표본도 사용자가 이미 제공한 정규직·서울 선호를 우선 반영해야 하기 때문이다.
- 실행당 상세 조회를 1건으로 제한했다.
  - 이유: 실제 구조화 품질을 한 건씩 확인하고 실패 규칙을 교정한 뒤 다음 공고로 넘어가기 위해서다.
- 빈 필수 조건이나 주요 업무를 성공처럼 숨기지 않고 경고했다.
  - 이유: 저장과 API 호출 성공은 Career Agent 분석 품질 성공과 같지 않기 때문이다.

### 다음 할 일

1. 실제 Moloco 공고의 섹션 제목과 현재 Greenhouse 구조화 규칙을 비교한다.
2. 확인된 형식 차이만 최소 수정해 필수 조건과 주요 업무 추출을 복구한다.
3. 같은 공고를 새 파이프라인 버전으로 다시 분석한 뒤 다음 큐 후보로 진행한다.

## 2026-09-14 - Moloco 직급 그룹 공고 구조화 교정

### 목표

실제 Moloco `Machine Learning Engineer` 공고에서 0개로 추출된 필수 조건과 주요 업무의 원인을 확인하고, 확인된 본문 형식만 최소 수정해 검토 가능한 결과로 복구한다.

### 수행한 작업

- Greenhouse 공식 상세 API의 실제 Moloco 공고 본문을 읽기 전용으로 확인했다.
- 주요 업무 제목 `What You Will Do`를 명시적인 업무 섹션 제목으로 추가했다.
- `Basic Qualifications (N Titles)` 아래에 여러 직급이 이어지는 형식을 별도로 인식했다.
- 여러 직급의 2년, 5년, 8년 경력 조건을 하나로 합치지 않고 원문 첫 그룹만 필수 조건으로 구조화했다.
- 선택한 첫 직급 이름을 분석 사실에, 다른 직급을 비교하지 않은 제한을 미확인 정보에 기록했다.
- 구조화 의미 변경에 따라 분석 파이프라인 버전을 `0.2`로 올렸다.
- 합성 공고로 첫 직급 조건만 선택되고 이후 직급 조건이 섞이지 않는 회귀 테스트를 추가했다.
- 기존 테스트의 분석 버전 기대값을 고정 문자열 대신 현재 상수로 연결했다.
- 최신 파이프라인으로 Sendbird와 Moloco를 다시 분석하고 검토 큐를 갱신했다.

### 생성 또는 수정한 파일

- `src/career_agent/ingestion/greenhouse.py`
- `src/career_agent/workflows/greenhouse_analysis.py`
- `tests/test_greenhouse_ingestion.py`
- `tests/test_greenhouse_analysis_workflow.py`
- `tests/test_greenhouse_agent_workflow.py`
- `docs/GREENHOUSE_API_MAPPING.md`
- `docs/REVIEW_QUEUE_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 실제 Moloco 공고에서 주요 업무 6개, 필수 조건 7개와 우대 조건 2개가 구조화됐다.
- 필수 조건은 원문 첫 그룹인 `Machine Learning Engineer II`만 사용했고 최소 경력은 2년으로 추출됐다.
- Senior 5년과 Staff 8년 조건은 첫 직급의 필수 조건에 섞이지 않았다.
- 실제 재분석 결과는 `조건부 지원`이었고 구조화 사실과 직급 선택 제한이 함께 저장됐다.
- Sendbird와 Moloco 결과를 분석 파이프라인 `0.2`로 다시 생성했다.
- 현재 검토 큐는 10건 중 2건 분석 완료, 8건 분석 필요 상태다.
- 전체 단위 테스트 101개가 통과했다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 73%로 판단했다.

### 아직 확인하지 못한 점

- 첫 번째 직급 그룹이 항상 사용자에게 가장 적합한 직급이라는 보장은 없다.
- 다른 기업이 사용하는 직급별 자격 요건 형식까지 같은 규칙으로 인식되는지는 확인하지 않았다.
- 실제 분석 결과에 대한 사용자의 적합·부적합 판단을 큐에 기록하는 입력 명령은 아직 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 여러 직급의 조건을 모두 합치지 않고 첫 그룹만 선택했다.
  - 이유: 서로 대체 관계인 2년, 5년, 8년 조건을 동시에 필수로 처리하면 매칭 결과가 명백히 왜곡되기 때문이다.
- 직급 그룹 선택을 숨기지 않고 결과에 기록했다.
  - 이유: 현재 MVP는 사용자 경력에 맞는 직급을 자동 결정할 충분한 규칙이 없으므로 보수적 선택과 한계를 검토자가 확인할 수 있어야 하기 때문이다.
- 분석 파이프라인 버전을 올리고 기존 결과를 재생성했다.
  - 이유: 같은 공고라도 구조화 규칙이 달라졌으므로 이전 결과를 최신 분석으로 재사용하면 안 되기 때문이다.

### 다음 할 일

1. 검토 큐의 다음 미분석 후보 1건만 공식 상세 API로 분석한다.
2. 새로운 섹션 형식이 발견되면 해당 형식만 회귀 테스트와 함께 최소 교정한다.
3. 분석 완료 표본에 사용자의 적합·부적합 판단을 안전하게 기록하는 가장 작은 입력 흐름을 설계한다.

## 2026-09-14 - 세 번째 실제 공고 분석과 사용자 판단 기록 경계

### 목표

세 번째 실제 공고의 구조화 품질을 한 건만 확인하고, 향후 Slack에서 받게 될 사용자의 공고 판단을 Agent 판단과 구분해 안전하게 저장하는 최소 내부 기능을 구현한다.

### 수행한 작업

- 최신 검토 큐의 세 번째 후보인 Moloco `Machine Learning Engineer-Technical Research Personnel`을 공식 Greenhouse 상세 API로 1건만 분석했다.
- 상세 분석이 완료된 큐 위치만 사용자 검토 대상으로 선택하도록 검증했다.
- 사용자 적합 판단을 `fit`, `hold`, `not_fit`으로 제한했다.
- Agent 추천 유용성을 `true`, `false`, `null`로 구분하고 선택 메모는 최대 1000자로 제한했다.
- 사용자 판단을 원본 큐와 분석 파일을 덮어쓰지 않는 별도 불변 JSON으로 저장하도록 했다.
- 사용자 판단 파일에서 프로필 원문과 공고 본문을 제외했다.
- 최신 검토 큐와 위치를 입력받는 명령행 도구를 추가했다.
- Slack 또는 Telegram이 이 저장 함수를 호출할 수 있다는 최종 인터페이스 경계를 PRD에 명시했다.

### 생성 또는 수정한 파일

- `src/career_agent/review/feedback.py`
- `src/career_agent/review/__init__.py`
- `scripts/record_greenhouse_review.py`
- `tests/test_greenhouse_review_feedback.py`
- `docs/REVIEW_QUEUE_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 세 번째 실제 공고에서 주요 업무 6개와 필수 조건 6개가 구조화됐다.
- 세 번째 공고에는 별도의 우대 조건 섹션이 없어 우대 조건은 0개였지만 필수 조건과 주요 업무 누락 경고는 발생하지 않았다.
- 현재 검토 큐는 10건 중 3건 분석 완료, 7건 분석 필요 상태다.
- 사용자 판단 기능의 합성 테스트 5개가 통과했다.
- 명령행 도움말에서 큐 위치, 적합 판단, 추천 유용성과 메모 입력을 확인했다.
- 실제 최신 큐에서 아직 분석되지 않은 4번 공고의 판단 기록은 종료 코드 1로 거부됐고 피드백 파일도 생성되지 않았다.
- 실제 사용자 판단은 제공받지 않았으므로 비공개 피드백 파일을 임의로 생성하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 74%로 판단했다.

### 아직 확인하지 못한 점

- 실제 공고 3건에 대한 사용자의 적합·보류·부적합 판단은 아직 받지 않았다.
- 별도 저장된 사용자 판단을 새 검토 큐와 검색 우선순위에 자동 반영하지 않는다.
- Slack 메시지와 첨부 문서를 받는 인터페이스는 아직 구현하지 않았다.

### 막힌 점

현재 없음. 실제 사용자 판단을 기록하려면 사용자가 해당 공고에 대한 판단을 제공해야 한다.

### 주요 판단과 이유

- 실제 판단을 Agent가 대신 만들지 않았다.
  - 이유: 자동 추천과 사용자의 실제 평가를 분리해야 추천 품질을 정직하게 검증할 수 있기 때문이다.
- 사용자 판단을 기존 큐에 직접 쓰지 않고 별도 파일로 저장했다.
  - 이유: 원본 보존 규칙을 지키고 Slack 입력 실패나 재시도 때 분석 결과를 손상하지 않기 위해서다.
- Slack 연결 전에 저장 함수를 먼저 만들었다.
  - 이유: 대화 채널이 바뀌어도 동일한 검증·개인정보 경계를 재사용할 수 있어야 하기 때문이다.

### 다음 할 일

1. 별도 사용자 판단 기록을 새 검토 큐에 병합해 이전 피드백이 사라지지 않도록 한다.
2. 남은 실제 공고를 한 건씩 분석하고 새로운 형식 오류가 있는지 확인한다.
3. 사용자 문서 입력을 위한 비공개 원본 저장과 구조화 경계를 설계한다.

## 2026-09-14 - 사용자 판단의 후속 검토 큐 병합

### 목표

별도 파일로 저장한 사용자의 적합·보류·부적합 판단이 검토 큐 재생성이나 다음 공고 분석 후 사라지지 않도록 현재 분석 결과에 안전하게 병합한다.

### 수행한 작업

- 검토 큐 스키마를 `0.3`으로 올렸다.
- 검토 큐 생성 함수가 사용자 판단 기록 목록을 선택 입력으로 받도록 확장했다.
- 같은 공고 식별자와 같은 분석 ID에 연결된 판단만 현재 큐에 반영했다.
- 같은 분석에 여러 판단이 있으면 시간대가 포함된 `reviewed_at` 기준으로 가장 최근 기록을 선택했다.
- 과거 분석 ID에 연결된 판단은 현재 분석의 사용자 판단으로 표시하지 않았다.
- 공고 식별자, 스키마 버전, 허용값과 개인정보 제외 표시를 검증했다.
- 큐 요약에 `reviewed`와 `not_reviewed` 수를 추가했다.
- 큐 생성 명령과 다음 공고 분석 명령이 `private-data/human-reviews/`를 선택적으로 읽도록 연결했다.
- 피드백 디렉터리가 아직 없어도 정상 실행하도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/review/queue.py`
- `src/career_agent/review/feedback.py`
- `scripts/build_greenhouse_review_queue.py`
- `scripts/analyze_next_greenhouse_review.py`
- `tests/test_greenhouse_review_queue.py`
- `docs/REVIEW_QUEUE_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 최신 판단 선택, 과거 분석 판단 제외와 잘못된 공고 식별자 거부를 포함한 검토 관련 테스트 20개가 통과했다.
- 전체 단위 테스트 109개가 통과했다.
- 실제 사용자 판단 디렉터리가 없는 상태에서도 새 `0.3` 큐가 정상 생성됐다.
- 실제 큐는 기존 상세 분석 완료 3건과 분석 필요 7건을 유지했다.
- 사용자가 아직 실제 판단을 입력하지 않았으므로 사용자 검토 완료는 0건으로 표시됐다.
- 큐 생성 과정에서 추가 네트워크 요청은 발생하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 75%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 입력한 판단이 후속 큐에 병합되는 과정은 실제 입력 전이라 합성 테스트로만 확인했다.
- 사용자 판단을 후보 검색 순위 또는 추천 규칙의 개선 신호로 사용하지 않는다.
- Slack 메시지와 첨부 문서를 받는 인터페이스는 아직 구현하지 않았다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 공고 식별자뿐 아니라 분석 ID도 같은 피드백만 병합했다.
  - 이유: 프로필이나 분석 규칙이 바뀐 뒤의 결과에 과거 판단을 현재 판단처럼 붙이면 평가 맥락이 달라질 수 있기 때문이다.
- 잘못된 피드백 파일을 조용히 건너뛰지 않았다.
  - 이유: 사용자 학습 신호가 일부 사라진 상태로 정상 실행된 것처럼 보이는 문제를 막기 위해서다.
- 피드백으로 검색 순위를 즉시 변경하지 않았다.
  - 이유: 실제 사용자 표본이 없는 상태에서 한 번의 판단을 일반 규칙으로 확대하면 추천이 쉽게 왜곡될 수 있기 때문이다.

### 다음 할 일

1. Slack 첨부 전에 재사용할 수 있는 사용자 문서의 비공개 입력 저장 경계를 설계하고 최소 구현한다.
2. 텍스트 입력부터 기존 사용자 프로필의 후보 업데이트로 변환하는 과정을 분리한다.
3. 남은 실제 공고는 새 입력 기능과 병행해 한 건씩 구조화 품질을 확인한다.

## 2026-09-14 - 사용자 경력 문서 비공개 수신함

### 목표

향후 Slack 또는 Telegram에서 받을 이력서, 경력기술서와 포트폴리오 원본을 공개 저장소와 분리해 검증하고 보존하는 최소 입력 경계를 구현한다.

### 수행한 작업

- 사용자 문서 입력과 저장을 담당하는 `profile_input` 모듈을 추가했다.
- 문서 종류를 이력서, 경력기술서, 포트폴리오와 기타 자료로 제한했다.
- `.txt`, `.md`, `.pdf`, `.docx` 형식과 최대 10MB 크기를 허용했다.
- 텍스트 UTF-8, PDF 파일 서명과 DOCX 필수 ZIP 항목을 최소 검증했다.
- 문서 종류와 전체 내용 SHA-256으로 결정적인 문서 ID를 생성했다.
- 같은 종류와 같은 내용의 재입력은 기존 원본을 덮어쓰지 않고 재사용했다.
- 원본 절대 경로와 문서 내용을 제외한 manifest를 원본과 함께 비공개 디렉터리에 원자적으로 저장했다.
- 로컬 파일 1개를 가져오는 명령행 도구를 추가했다.
- 문서 입력의 개인정보 경계, 지원 형식과 현재 한계를 별도 문서로 기록했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/__init__.py`
- `src/career_agent/profile_input/document_store.py`
- `scripts/import_profile_document.py`
- `tests/test_profile_document_store.py`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 합성 텍스트 저장, 동일 문서 재사용, 최소 DOCX 허용과 잘못된 PDF·확장자·크기·시간대 거부를 검증한 테스트 7개가 통과했다.
- 전체 단위 테스트 116개가 통과했다.
- 명령행 도움말에서 입력 파일, 문서 종류와 비공개 저장 경로 인자를 확인했다.
- manifest에는 원본 절대 경로와 문서 본문이 포함되지 않았다.
- 테스트는 임시 디렉터리에서만 실행했으며 실제 사용자 문서를 가져오지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 76%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자의 PDF 또는 DOCX 문서 입력은 아직 확인하지 않았다.
- PDF와 DOCX의 본문 추출, 이미지 PDF OCR과 악성 파일 검사는 구현하지 않았다.
- 저장된 문서에서 사용자 프로필 후보를 생성하고 확인 후 반영하는 기능은 없다.
- Slack 또는 Telegram 파일 다운로드 어댑터는 아직 없다.

### 막힌 점

현재 없음. 실제 사용자 문서 검증은 사용자가 문서 경로 또는 Slack 첨부를 제공한 뒤 가능하다.

### 주요 판단과 이유

- 문서 해석보다 원본 저장 경계를 먼저 만들었다.
  - 이유: 개인정보가 포함된 첨부자료의 보존 위치, 중복 처리와 실패 동작을 먼저 고정해야 채널 연결과 AI 추출을 안전하게 확장할 수 있기 때문이다.
- 문서 내용을 manifest와 콘솔에 복제하지 않았다.
  - 이유: 메타데이터 확인 과정에서 민감한 경력 정보가 불필요하게 노출되는 것을 줄이기 위해서다.
- 같은 내용은 해시 기반으로 재사용했다.
  - 이유: Slack 재시도나 중복 업로드가 원본 파일을 계속 늘리지 않도록 하기 위해서다.
- 기존 사용자 프로필을 자동 변경하지 않았다.
  - 이유: 아직 본문 추출과 사용자 확인이 없으므로 새 문서가 기존 사실을 조용히 덮어쓰면 안 되기 때문이다.

### 다음 할 일

1. UTF-8 텍스트 문서 1개의 본문을 읽고 기존 프로필과 분리된 추출 후보 파일로 저장한다.
2. 추출 후보에는 원문 근거 위치와 미확인 항목을 포함하고 사용자가 승인하기 전에는 프로필을 변경하지 않는다.
3. 이후 PDF와 DOCX 텍스트 추출을 각각 작은 단계로 추가한다.

## 2026-09-14 - UTF-8 경력 문서의 프로필 후보 추출

### 목표

비공개 저장된 TXT 또는 Markdown 경력 문서에서 확인 가능한 섹션의 문장만 프로필 후보로 만들고, 기존 사용자 프로필과 분리해 검토 가능한 결과로 저장한다.

### 수행한 작업

- 저장된 문서 ID, manifest, 원본 크기와 SHA-256을 다시 검증하는 로더를 추가했다.
- 경력, 프로젝트, 기술, 학력, 희망 직무와 업무 선호의 한국어·영어 제목을 제한적으로 인식했다.
- 인식된 제목 아래의 비어 있지 않은 문장만 프로필 후보로 생성했다.
- Markdown 목록 기호를 제거하되 원문 문장과 줄 번호 근거를 유지했다.
- 제목 없는 자유 문장과 인식하지 못한 Markdown 섹션은 사실로 추정하지 않았다.
- 이메일, 전화번호, 주민등록번호 형태와 연락처·주소 섹션을 후보에서 제외했다.
- 모든 후보를 `needs_review`로 표시하고 `profile_updated`를 `false`로 유지했다.
- 같은 문서와 같은 추출 규칙의 결과는 기존 파일을 재사용하도록 했다.
- 동일 추출 ID의 기존 후보·요약·근거가 달라졌으면 변조 또는 충돌로 보고 재사용을 거부했다.
- 저장된 문서 ID로 후보를 만드는 명령행 도구와 비식별 예제 문서를 추가했다.
- 프로필 추출 후보 스키마와 개인정보 제외 범위를 별도 문서로 기록했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/document_store.py`
- `src/career_agent/profile_input/text_extraction.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/extract_profile_text.py`
- `tests/test_profile_text_extraction.py`
- `data/profile_document.example.md`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 문서 저장과 텍스트 추출 관련 테스트 15개가 통과했다.
- 전체 단위 테스트 124개가 통과했다.
- 비식별 예제 문서를 실제 명령으로 가져오고 저장된 문서 ID로 후보 추출을 실행했다.
- 예제에서 경력 2개, 프로젝트 1개, 기술 3개와 희망 직무 1개로 총 7개 후보가 생성됐다.
- 예제 이메일 1줄은 후보에서 제외됐고 저장 결과 문자열에도 포함되지 않았다.
- 모든 후보에 원문 줄 번호 근거가 있었고 상태는 `needs_review`였다.
- 추출 결과의 `profile_updated`는 `false`였다.
- 같은 비식별 예제 문서를 다시 실행했을 때 새 파일을 만들지 않고 기존 추출 결과를 재사용했다.
- 실제 사용자 문서는 읽거나 변경하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 78%로 판단했다.

### 아직 확인하지 못한 점

- 제목이 없거나 표 형태인 실제 이력서에서 후보를 충분히 찾을 수 있는지 확인하지 않았다.
- 연락처 제외 규칙이 모든 개인정보 표현을 찾는다고 보장할 수 없다.
- 기간, 경력 연수, 숙련도, 성과와 복합 기술 목록은 의미 단위로 구조화하지 않는다.
- PDF, DOCX와 이미지 OCR 입력은 아직 후보 추출을 지원하지 않는다.
- 추출 후보의 승인·거부와 기존 프로필 병합은 아직 없다.

### 막힌 점

현재 없음.

### 주요 판단과 이유

- 제목이 없는 문장을 사용자 사실로 추정하지 않았다.
  - 이유: 규칙 기반 MVP가 자유 서술의 의미를 잘못 분류해 사용자 프로필을 오염시키는 것을 막기 위해서다.
- 후보에 줄 번호 근거를 붙였다.
  - 이유: Slack에서 사용자에게 승인 여부를 물을 때 어떤 원문에서 나온 내용인지 확인할 수 있어야 하기 때문이다.
- 연락처 형태의 줄은 후보에서 제외하고 개수만 기록했다.
  - 이유: 직무 검색과 적합도 분석에 필요하지 않은 개인정보를 구조화 결과에 반복 저장하지 않기 위해서다.
- PDF와 DOCX 추출을 동시에 구현하지 않았다.
  - 이유: 텍스트 입력에서 후보·근거·승인 경계를 먼저 검증한 뒤 형식별 추출기를 추가해야 실패 원인을 분리할 수 있기 때문이다.

### 다음 할 일

1. 프로필 후보 한 건의 사용자 승인 또는 거부를 별도 불변 기록으로 저장한다.
2. 승인된 후보만 모아 기존 프로필을 덮어쓰지 않는 갱신안 JSON을 만든다.
3. 이 흐름이 확인된 뒤 DOCX 본문 추출을 추가한다.

## 2026-09-14 - 프로필 추출 후보의 사용자 승인·거부 기록

### 목표

문서에서 추출된 프로필 후보 한 건에 대한 사용자의 명시적인 승인 또는 거부를 기존 추출 결과와 프로필을 변경하지 않고 별도 기록한다.

### 수행한 작업

- 저장된 프로필 추출 ID 형식과 결과 스키마를 검증하는 로더를 추가했다.
- 후보 ID 중복, `needs_review` 상태, 프로필 영역과 원문 문서·줄 근거를 검증했다.
- 후보 결정은 `approve`와 `reject`만 허용했다.
- 사용자 메모는 선택 입력으로 두고 최대 1000자로 제한했다.
- 검토 시각과 추출·후보·문서 근거로 불변 검토 레코드를 생성했다.
- 검토 레코드에 후보 문장 자체를 복제하지 않았다.
- 기존 검토 파일을 덮어쓰지 않는 원자적 저장 함수를 추가했다.
- 명령행에서 추출 결과와 후보를 지정해 사용자 결정을 기록하도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/text_extraction.py`
- `src/career_agent/profile_input/candidate_review.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/review_profile_candidate.py`
- `tests/test_profile_candidate_review.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 후보 승인, 후보 문장 비복제, 잘못된 결정·후보·중복 ID·긴 메모 거부와 불변 저장을 포함한 후보 검토 테스트 7개가 통과했다.
- 텍스트 추출 관련 테스트와 함께 총 15개가 통과했다.
- 전체 단위 테스트 131개가 통과했다.
- 명령행 도움말에서 추출 ID, 후보 ID, 승인·거부와 메모 인자를 확인했다.
- 실제 비식별 예제에서 존재하지 않는 `candidate-999` 승인은 종료 코드 1로 거부됐다.
- 실패 실행에서는 `private-data/profile-candidate-reviews/` 디렉터리와 파일이 생성되지 않았다.
- 사용자의 실제 승인 결정은 받지 않았으므로 예제 후보를 임의 승인하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 79%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 입력한 승인 또는 거부 기록은 아직 없다.
- 같은 후보에 여러 결정이 있을 때 가장 최근 판단을 선택하는 갱신안 기능은 아직 없다.
- 승인된 한 줄을 현재 `USER_PROFILE_SCHEMA`의 세부 객체로 변환하지 않는다.
- Slack 대화의 승인 버튼이나 메시지 응답은 아직 연결하지 않았다.

### 막힌 점

현재 없음. 실제 승인 기록에는 사용자의 명시적인 결정이 필요하다.

### 주요 판단과 이유

- 승인과 거부만 허용하고 미검토는 기록 파일 부재로 표현했다.
  - 이유: 명시적인 사용자 행동과 아직 답하지 않은 상태를 구분하기 위해서다.
- 후보 문장을 검토 레코드에 복제하지 않았다.
  - 이유: 후보 내용은 원래 추출 파일에서 확인할 수 있으며 민감할 수 있는 경력 문장을 여러 파일에 반복 저장할 필요가 없기 때문이다.
- 사용자 결정으로 기존 추출 결과와 프로필을 변경하지 않았다.
  - 이유: 승인 이력 보존과 실제 프로필 갱신을 분리해 잘못된 반영을 되돌릴 수 있어야 하기 때문이다.

### 다음 할 일

1. 후보별 가장 최근 사용자 결정을 선택하는 검토 집계를 구현한다.
2. 가장 최근 결정이 `approve`인 후보만 포함한 프로필 갱신안 JSON을 만든다.
3. 갱신안은 사용자 확인 전까지 기존 프로필을 덮어쓰지 않는다.

## 2026-09-14 - 최신 승인 후보의 비파괴 프로필 갱신안

### 목표

프로필 추출 후보별 사용자 판단 중 가장 최근 결정을 선택하고, 최종 결정이 승인인 후보만 기존 프로필과 분리된 갱신안으로 저장한다.

### 수행한 작업

- 프로필 추출 결과와 후보 검토 기록의 스키마 및 개인정보 보호 메타데이터를 다시 검증했다.
- 검토 기록 ID를 원본 추출 ID, 후보 ID와 검토 시각으로 다시 계산해 변조 또는 불일치를 거부했다.
- 같은 후보에 여러 검토 기록이 있으면 시간대가 포함된 `reviewed_at` 기준으로 가장 최근 판단을 선택했다.
- 최신 결정이 `approve`인 후보만 갱신안 항목으로 포함하고 `reject`와 미검토 후보는 요약 개수로 구분했다.
- 승인 후보의 문장, 원문 문서와 줄 번호, 승인 기록 ID를 하나의 제안 항목으로 연결했다.
- 기준 사용자 프로필의 정규화된 JSON에 대한 SHA-256 지문을 기록했다.
- 승인 문장을 세부 프로필 객체로 추정하지 않고 `needs_mapping` 상태로 유지했다.
- 같은 기준 프로필, 추출 결과와 최신 검토 집합은 같은 제안 ID로 만들고 기존 내용이 동일할 때만 재사용했다.
- 갱신안을 새 비공개 파일로 저장하고 기존 사용자 프로필을 수정하지 않는 명령행 도구를 추가했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/update_proposal.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/build_profile_update_proposal.py`
- `tests/test_profile_update_proposal.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/profile-update-proposals/profile-update-proposal-cb1e577c9d6248c60dc674d0.json` 생성, Git 제외

### 확인된 결과

- 최신 승인 우선, 최신 거부 우선, 미검토 분리, 다른 추출 결과의 검토 분리, 검토 ID와 원문 근거 변조 거부, 프로필 변경 감지, 동일 제안 재사용과 시간대 검증을 포함한 새 테스트 9개가 통과했다.
- 전체 단위 테스트 140개가 통과했다.
- 비식별 예제 추출 결과에 실제 명령을 실행해 전체 후보 7개, 검토 0개, 승인 0개, 거부 0개, 미검토 7개를 확인했다.
- 실제 실행 결과는 `no_approved_candidates` 상태이고 `proposed_additions`는 비어 있었다.
- 갱신안의 기준 프로필 SHA-256이 현재 예제 프로필 내용과 일치했다.
- 갱신안의 `profile_updated`는 `false`였고 생성 파일은 `.gitignore`의 `private-data/` 규칙으로 Git 추적에서 제외됐다.
- 사용자의 실제 승인 판단은 없으므로 예제 후보를 임의 승인하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 81%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자 승인 기록이 포함된 갱신안은 아직 실행하지 않았다.
- 승인 문장을 `USER_PROFILE_SCHEMA`의 기술, 경력, 프로젝트와 목표 직무 객체로 변환하지 않는다.
- 기준 프로필 지문을 다시 확인한 뒤 실제 프로필에 적용하는 단계는 아직 없다.
- Slack 대화에서 후보를 보여주고 승인받아 갱신안을 만드는 흐름은 아직 연결하지 않았다.

### 막힌 점

현재 없음. 실제 승인 후보가 있는 통합 확인은 사용자의 명시적인 판단이 있어야 한다.

### 주요 판단과 이유

- 후보별 가장 최근 결정만 사용했다.
  - 이유: 사용자가 이전 결정을 수정한 경우 과거 승인이나 거부가 현재 의사를 덮지 않도록 하기 위해서다.
- 프로필 전체 내용의 지문을 기록했다.
  - 이유: 제안 생성 뒤 프로필이 바뀌었을 때 오래된 제안을 다른 기준에 적용하는 것을 이후 단계에서 차단하기 위해서다.
- 승인 문장을 바로 기존 프로필 객체로 변환하지 않았다.
  - 이유: 한 줄 문장만으로 기술 숙련도, 경력 기간과 성과 구조를 임의로 만들면 확인되지 않은 사용자 정보를 추가할 수 있기 때문이다.
- 승인 후보가 없어도 빈 제안을 저장했다.
  - 이유: 아무것도 반영하지 않은 실행도 재현 가능하게 확인하면서 사용자 판단을 꾸며내지 않기 위해서다.

### 다음 할 일

1. 승인된 `skills` 후보 한 건을 기존 기술과 중복 비교하는 가장 작은 타입 매핑 제안을 만든다.
2. 숙련도와 사용 증거가 확인되지 않으면 실제 기술 객체를 만들지 않고 추가 확인 항목으로 남긴다.
3. 이 흐름을 확인한 뒤 목표 직무, 경력과 프로젝트 순서로 매핑 범위를 한 영역씩 넓힌다.

## 2026-09-14 - 승인 기술 후보의 보수적인 매핑

### 목표

승인된 기술 후보를 기존 사용자 프로필의 기술 목록과 비교하고, 확인되지 않은 숙련도나 사용 증거를 만들지 않는 비파괴 매핑안을 저장한다.

### 수행한 작업

- 저장된 프로필 갱신안 ID 형식과 Git 제외·프로필 미변경 메타데이터를 검증하는 로더를 추가했다.
- 현재 프로필의 내용 지문과 갱신안 생성 당시 기준 지문이 다르면 매핑을 거부했다.
- 기존 기술명과 후보 기술명을 Unicode NFKC, 대소문자와 연속 공백만 정리해 보수적으로 비교했다.
- 이름이 정확히 같은 후보는 `duplicate_existing`으로 기존 `skill_id`와 연결했다.
- 새 단일 기술명 후보는 숙련도와 사용 증거가 없으므로 `needs_details`로 분류했다.
- 쉼표, 세미콜론, 세로줄 또는 가운뎃점이 있는 후보는 여러 기술이 섞였을 가능성을 표시하는 `needs_separation`으로 분류했다.
- 기술 영역이 아닌 승인 후보는 내용을 다른 영역으로 재해석하지 않고 제외 개수만 기록했다.
- 모든 매핑 항목을 `profile_change_ready: false`로 유지하고 기존 프로필과 기술 숙련도를 변경하지 않았다.
- 동일한 기준 프로필, 갱신안과 매핑 규칙은 같은 결과 ID로 재사용하도록 원자적 저장을 구현했다.
- 승인 수, 원문 줄 범위, 승인 결정과 갱신안 상태가 불일치하거나 기존 저장 파일이 변조됐으면 거부하도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/update_proposal.py`
- `src/career_agent/profile_input/skill_mapping.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/build_profile_skill_mapping.py`
- `tests/test_profile_skill_mapping.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/profile-skill-mappings/profile-skill-mapping-7982917ea49b9a42d5a74ae9.json` 생성, Git 제외

### 확인된 결과

- 중복 기술, 새 기술, 복합 기술, 다른 프로필 영역 분리와 프로필·승인·저장 파일 변조 차단을 포함한 새 테스트 10개가 통과했다.
- 전체 단위 테스트 150개가 통과했다.
- 합성 승인 데이터에서 `python`은 기존 `Python`과 같은 기술로 연결됐다.
- 합성 승인 데이터의 `FastAPI`는 `level`과 `evidence`가 없는 `needs_details`로 남았다.
- 합성 승인 데이터의 `Docker, Kubernetes`는 `needs_separation`으로 남았고 개별 기술로 임의 분리하지 않았다.
- 합성 승인 데이터의 희망 직무 문장은 기술 매핑에서 제외됐다.
- 실제 비공개 예제 갱신안은 승인 후보가 없으므로 기술 후보 0개의 `no_skill_candidates` 결과를 만들었다.
- 실제 기술 매핑 결과의 `profile_updated`는 `false`이고 결과 파일은 `.gitignore`의 `private-data/` 규칙으로 Git 추적에서 제외됐다.
- 사용자의 실제 승인이나 숙련도 판단은 만들지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 82%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 승인한 기술 후보의 중복·신규 분류는 아직 확인하지 않았다.
- 새 기술 후보의 `level`과 `evidence`를 명시적으로 확인해 저장하는 기능은 아직 없다.
- 기술 별칭과 유사어는 같은 기술로 연결하지 않는다.
- 목표 직무, 경력, 프로젝트와 다른 프로필 영역은 아직 타입 매핑하지 않는다.
- 완성된 기술 객체를 현재 프로필 지문을 다시 확인한 뒤 적용하는 기능은 아직 없다.

### 막힌 점

현재 없음. 실제 기술 숙련도와 사용 증거를 확정하려면 사용자 자료의 근거 또는 사용자의 명시적 확인이 필요하다.

### 주요 판단과 이유

- 기술명 유사도나 별칭 사전을 사용하지 않았다.
  - 이유: `REST API`, 특정 제품명과 비슷한 일반 표현을 같은 기술로 잘못 합치는 것보다 명시적 확인 대상으로 남기는 편이 안전하기 때문이다.
- 새 기술명만으로 `level`과 `evidence`를 만들지 않았다.
  - 이유: 문서에 기술명이 있다는 사실만으로 학습·프로젝트·실무 사용 수준을 판단할 수 없기 때문이다.
- 여러 기술이 섞인 문장을 자동 분리하지 않았다.
  - 이유: 쉼표 같은 기호가 항상 기술 구분자라는 보장이 없고 분리된 각 기술의 증거 범위도 다시 확인해야 하기 때문이다.
- 다른 프로필 영역을 기술 매핑에서 해석하지 않았다.
  - 이유: 한 번에 한 영역만 구현해 오분류 원인과 사용자 확인 질문을 분리하기 위해서다.

### 다음 할 일

1. `needs_details` 기술 후보 한 건에 사용자가 확인한 `level`과 `evidence`를 별도 불변 기록으로 저장한다.
2. 허용 숙련도 값과 근거 문장 개수·길이를 제한하고 기존 매핑안과 원문 참조를 검증한다.
3. 이 확인 기록까지 갖춘 후보만 완성된 기술 추가안으로 만들고 실제 프로필 적용은 계속 분리한다.

## 2026-09-14 - 새 기술 후보의 숙련도·사용 증거 확인 기록

### 목표

`needs_details` 상태인 새 기술 후보 한 건에 사용자가 명시적으로 확인한 숙련도와 실제 사용 증거를 기존 매핑안과 프로필을 변경하지 않고 별도 기록한다.

### 수행한 작업

- 저장된 기술 매핑안 ID 형식을 검증하고 경로 이탈 없이 비공개 JSON을 읽는 로더를 추가했다.
- 기술 매핑안의 스키마, 규칙 버전, Git 제외와 프로필 미변경 상태를 다시 검증했다.
- `needs_details`이며 기존 기술과 중복되지 않은 항목만 세부정보 확인 대상으로 허용했다.
- 허용 숙련도를 `none`, `exposure`, `learning`, `basic`, `project`, `work`로 제한했다.
- 사용 증거는 최소 1개, 최대 10개, 항목당 300자 이하로 제한하고 빈 값과 대소문자만 다른 중복을 거부했다.
- 선택 메모는 최대 1000자로 제한했다.
- 확인 기록에 후보 기술명을 자동 복제하지 않고 매핑 ID, 항목 ID, 원문 문서와 줄 번호, 승인 기록 ID를 연결했다.
- 확인 출처를 `explicit_user_input`으로 명시해 AI 추정과 구분했다.
- 같은 기술 후보를 나중에 다시 확인할 수 있도록 각 확인을 이전 파일과 분리된 불변 레코드로 저장했다.
- 명령행에서 숙련도와 하나 이상의 사용 증거를 입력해 확인 기록을 만들도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/skill_mapping.py`
- `src/career_agent/profile_input/skill_confirmation.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/confirm_profile_skill.py`
- `tests/test_profile_skill_confirmation.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 모든 허용 숙련도, 증거 제한, 대상 상태, 원문 줄 범위, 경로 이탈, ID 불일치와 불변 저장을 포함한 새 테스트 9개가 통과했다.
- 전체 단위 테스트 159개가 통과했다.
- 합성 `FastAPI` 후보를 `project` 수준과 합성 프로젝트 사용 증거 1개로 확인하는 기록을 생성했다.
- 합성 확인 기록에는 `FastAPI` 후보명이 복제되지 않았고 `profile_updated`는 `false`였다.
- 실제 비공개 기술 매핑안은 승인 기술 후보가 0개이므로 확인 명령이 종료 코드 1과 `확인할 새 기술 후보가 있는 매핑안이 아님`으로 안전하게 거부됐다.
- 실패한 실제 명령은 `private-data/profile-skill-confirmations/` 디렉터리나 파일을 만들지 않았다.
- 실제 사용자 숙련도나 사용 증거를 임의로 기록하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 83%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 승인한 새 기술 후보의 숙련도·사용 증거 확인은 아직 없다.
- 같은 후보에 여러 세부정보 확인이 있을 때 가장 최근 기록을 선택하지 않는다.
- 확인 기록과 후보 기술명을 합쳐 완성된 `skills` 추가 객체를 만드는 기능은 아직 없다.
- 완성된 추가안을 현재 프로필 지문과 다시 비교해 적용하는 기능은 아직 없다.
- Slack 대화에서 이 확인 질문과 답변을 연결하지 않았다.

### 막힌 점

현재 없음. 실제 성공 기록은 승인된 새 기술 후보와 사용자의 명시적 세부정보가 있어야 한다.

### 주요 판단과 이유

- 숙련도와 사용 증거를 모두 필수로 했다.
  - 이유: 기술명만 있는 객체는 현재 프로필 스키마의 근거 기반 기술 수준 원칙을 충족하지 못하기 때문이다.
- 확인 출처를 명시적 사용자 입력으로 고정했다.
  - 이유: 이후 Agent가 문서에서 추론한 값과 사용자가 직접 확인한 값을 구분해야 하기 때문이다.
- 후보 기술명을 확인 기록에 복제하지 않았다.
  - 이유: 매핑안에서 이미 확인할 수 있는 개인정보 가능 문장을 새 파일마다 반복 저장하지 않기 위해서다.
- 같은 항목의 재확인을 허용하되 기존 기록을 덮어쓰지 않았다.
  - 이유: 사용자가 숙련도나 근거를 수정했을 때 변경 이력을 보존하고 다음 단계에서 최신 판단을 선택할 수 있어야 하기 때문이다.

### 다음 할 일

1. 기술 후보별 가장 최근 세부정보 확인을 시간대가 포함된 `confirmed_at` 기준으로 선택한다.
2. 최신 확인이 있는 `needs_details` 후보만 `skill_id`, `name`, `level`, `evidence`가 있는 완성된 기술 추가안으로 만든다.
3. 기준 프로필 지문과 원문·승인·확인 참조를 유지하고 실제 프로필 적용은 계속 분리한다.

## 2026-09-14 - 최신 확인 기반 완성 기술 추가안

### 목표

기술 후보별 가장 최근 사용자 세부정보 확인을 선택하고, 확인이 끝난 새 기술만 필수 필드가 갖춰진 추가안으로 만들되 기존 사용자 프로필은 변경하지 않는다.

### 수행한 작업

- 기술 매핑안의 스키마, 규칙 버전, 상태, 후보 항목 구성과 개인정보 보호 메타데이터를 다시 검증했다.
- 현재 사용자 프로필의 ID와 내용 SHA-256이 기술 매핑 당시 기준과 다르면 추가안 생성을 거부했다.
- 기존 프로필 기술명의 중복과 `skill_id` 중복을 다시 확인했다.
- 기술 확인 기록의 스키마, 명시적 사용자 입력 출처, 숙련도, 증거, 확인 ID와 원문 참조를 검증했다.
- 같은 기술 후보에 여러 확인 기록이 있으면 시간대가 포함된 `confirmed_at`이 가장 최근인 기록만 선택했다.
- 다른 기술 매핑안의 유효한 확인 기록은 현재 추가안에서 제외했다.
- 최신 확인이 있는 `needs_details` 후보만 `skill_id`, `name`, `level`, `evidence`, `notes`를 갖춘 완성된 기술 객체로 구성했다.
- 새 `skill_id`는 기술 매핑 ID, 항목 ID와 정규화된 기술명에서 결정적으로 생성했다.
- 미확인 후보, 기존 기술 중복과 기술명 분리 필요 후보는 추가안에서 제외하고 요약 개수로 남겼다.
- 각 기술 객체를 `needs_final_review` 상태로 유지하고 실제 프로필을 변경하지 않았다.
- 동일 기준 프로필, 기술 매핑과 최신 확인 집합은 기존 추가안 파일을 재사용하도록 원자적 저장을 구현했다.
- 명령행에서 기술 매핑 ID만 지정하면 비공개 확인 기록을 모아 완성된 기술 추가안을 만들도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/skill_mapping.py`
- `src/career_agent/profile_input/skill_addition.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/build_profile_skill_additions.py`
- `tests/test_profile_skill_addition.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/profile-skill-additions/profile-skill-addition-1e00ae3a52b4156078fe0009.json` 생성, Git 제외

### 확인된 결과

- 최신 확인 선택, 미확인 제외, 다른 매핑 확인 분리, 확인 ID·원문 참조·프로필 변조 차단과 불변 저장을 포함한 새 테스트 11개가 통과했다.
- 전체 단위 테스트 170개가 통과했다.
- 합성 `FastAPI` 후보의 이전 `learning` 확인보다 나중의 `project` 확인이 선택됐다.
- 합성 추가안에 결정적 `skill_id`, `FastAPI`, `project`와 합성 프로젝트 사용 증거가 포함됐다.
- 합성 추가안의 항목 상태는 `needs_final_review`였고 `profile_updated`는 `false`였다.
- 실제 비공개 기술 매핑안에는 새 기술 후보와 확인 기록이 없어 기술 추가 0개의 `no_confirmed_skills` 결과가 생성됐다.
- 실제 추가안 파일의 `profile_updated`는 `false`이며 `.gitignore`의 `private-data/` 규칙으로 Git 추적에서 제외됐다.
- 실제 사용자 기술이나 세부정보를 임의로 생성하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 84%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 확인한 기술로 완성된 추가안을 만드는 통합 실행은 아직 없다.
- 완성된 기술 추가안의 최종 승인 또는 거부 기록은 아직 없다.
- 최종 승인된 기술을 현재 프로필 지문과 다시 비교해 적용하는 기능은 아직 없다.
- 기술 이외의 목표 직무, 경력과 프로젝트 영역은 아직 타입 매핑하지 않는다.
- Slack 대화에서 기술 후보 확인과 최종 승인을 연결하지 않았다.

### 막힌 점

현재 없음. 실제 기술 추가안 생성에는 승인된 새 기술 후보와 사용자의 명시적 세부정보 확인이 필요하다.

### 주요 판단과 이유

- 가장 최근 확인 기록만 사용했다.
  - 이유: 사용자가 숙련도나 증거를 수정했을 때 과거 입력보다 현재 판단이 우선해야 하기 때문이다.
- 완성된 기술 객체에도 최종 검토 상태를 유지했다.
  - 이유: 필수 필드가 채워진 것과 사용자가 실제 프로필 추가를 승인한 것은 서로 다른 결정이기 때문이다.
- 기술 ID를 입력 순서가 아닌 안정적인 원본 참조에서 만들었다.
  - 이유: 같은 확인 집합을 다시 처리해도 중복 기술 ID가 생기지 않아야 하기 때문이다.
- 현재 프로필과의 중복을 추가안 생성 시점에 다시 확인했다.
  - 이유: 매핑 뒤 프로필이 바뀌었거나 잘못 구성된 매핑안이 들어와 기존 기술을 다시 추가하는 것을 막기 위해서다.

### 다음 할 일

1. 완성된 기술 추가안 한 건의 최종 `approve` 또는 `reject`를 별도 불변 기록으로 저장한다.
2. 최종 판단 기록에는 기술 내용을 복제하지 않고 추가안 ID와 항목 ID만 연결한다.
3. 이후 최신 최종 판단이 승인인 항목만 새 버전의 사용자 프로필 파일에 적용하고 기존 원본은 보존한다.

## 2026-09-14 - 완성 기술 추가안의 최종 승인·거부 기록

### 목표

완성된 기술 추가안 한 건에 사용자의 최종 승인 또는 거부를 기술 내용과 기존 프로필을 변경하지 않고 별도 불변 기록으로 저장한다.

### 수행한 작업

- 저장된 기술 추가안 ID 형식을 검증하고 경로 이탈 없이 비공개 JSON을 읽는 로더를 추가했다.
- 기술 추가안의 스키마, 규칙 버전, 최종 검토 상태, 완성 기술 수와 개인정보 보호 메타데이터를 검증했다.
- 제안 기술의 `skill_id`, `name`, 허용 숙련도, 사용 증거와 메모 구성을 다시 검증했다.
- 기술 추가 항목 ID와 제안 `skill_id` 중복, 원문 줄 범위와 참조 필드 이상을 거부했다.
- 최종 결정은 `approve`와 `reject`만 허용하고 선택 메모는 1000자로 제한했다.
- 최종 판단 출처를 `explicit_user_input`으로 명시했다.
- 최종 검토 기록에 기술명, 숙련도와 사용 증거를 자동 복제하지 않고 추가안 ID, 항목 ID와 확인 기록 ID만 연결했다.
- 같은 항목을 다시 판단할 수 있도록 각 결정을 기존 기록과 분리된 불변 파일로 저장했다.
- 명령행에서 추가안 ID, 항목 ID와 최종 결정을 입력해 검토 기록을 만들도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/skill_addition.py`
- `src/career_agent/profile_input/skill_addition_review.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/review_profile_skill_addition.py`
- `tests/test_profile_skill_addition_review.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 합성 최종 승인·거부, 기술 내용 비복제, 잘못된 결정·항목·상태·기술·원문 참조 거부와 불변 저장을 포함한 새 테스트 9개가 통과했다.
- 전체 단위 테스트 179개가 통과했다.
- 합성 `FastAPI` 추가안에 대한 승인 기록과 거부 기록을 각각 생성했다.
- 합성 최종 검토 기록에는 `FastAPI`, 숙련도와 합성 사용 증거가 복제되지 않았다.
- 합성 최종 검토 기록의 `review_source`는 `explicit_user_input`, `profile_updated`는 `false`였다.
- 실제 비공개 기술 추가안은 완성 기술이 0개이므로 승인 명령이 종료 코드 1과 `최종 검토할 기술이 있는 추가안이 아님`으로 안전하게 거부됐다.
- 실패한 실제 명령은 `private-data/profile-skill-addition-reviews/` 디렉터리나 파일을 만들지 않았다.
- 실제 사용자 최종 승인을 임의로 기록하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 85%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 확인한 기술 추가안의 최종 승인 또는 거부는 아직 없다.
- 같은 추가 항목에 여러 최종 판단이 있을 때 가장 최근 기록을 선택하지 않는다.
- 최신 최종 판단이 승인인 기술을 새 버전의 사용자 프로필에 적용하는 기능은 아직 없다.
- 기술 이외의 목표 직무, 경력과 프로젝트 영역은 아직 타입 매핑하지 않는다.
- Slack 대화에서 최종 승인 질문과 답변을 연결하지 않았다.

### 막힌 점

현재 없음. 실제 성공 기록에는 완성된 기술 추가안과 사용자의 명시적 최종 판단이 필요하다.

### 주요 판단과 이유

- 완성된 기술 객체 생성과 최종 승인 결정을 분리했다.
  - 이유: 필수 필드가 채워졌다는 사실만으로 사용자가 실제 프로필 변경을 허용했다고 볼 수 없기 때문이다.
- 최종 검토 기록에 기술 내용을 복제하지 않았다.
  - 이유: 기술 내용과 개인 근거는 원래 추가안에서 확인할 수 있으며 판단 이력마다 반복 저장할 필요가 없기 때문이다.
- 승인과 거부만 허용하고 보류는 파일 부재로 표현했다.
  - 이유: 명시적 최종 결정과 아직 답하지 않은 상태를 구분하기 위해서다.
- 기존 최종 판단을 덮어쓰지 않았다.
  - 이유: 사용자가 결정을 바꾼 경우 변경 이력을 보존하고 적용 단계에서 가장 최근 판단을 선택할 수 있어야 하기 때문이다.

### 다음 할 일

1. 추가 항목별 가장 최근 최종 판단을 시간대가 포함된 `reviewed_at` 기준으로 선택한다.
2. 최신 결정이 `approve`인 기술만 현재 프로필 지문과 다시 비교한다.
3. 기존 프로필 원본을 덮어쓰지 않고 새 버전 파일에 승인 기술을 추가하며 적용 이력을 별도 저장한다.

## 2026-09-14 - 최신 최종 승인 기술의 비파괴 프로필 적용

### 목표

기술 추가 항목별 가장 최근 최종 판단을 선택하고, 최신 결정이 승인인 기술만 현재 프로필과 다시 비교해 원본과 분리된 새 프로필 버전에 적용한다.

### 수행한 작업

- 기술 추가안 전체를 검증하고 항목 ID로 조회하는 공통 검증 함수를 분리했다.
- 추가 항목마다 시간대가 포함된 `reviewed_at`과 검토 ID를 기준으로 가장 최근 최종 판단을 선택했다.
- 최종 판단 출처가 `explicit_user_input`인지 확인하고 승인과 거부 외의 값, 변조된 검토 ID와 원본 참조를 거부했다.
- 적용 직전에 기준 프로필 ID와 전체 내용 SHA-256을 추가안 생성 당시 값과 다시 비교했다.
- 현재 프로필과 승인 기술 사이의 정규화된 기술명 및 `skill_id` 중복을 다시 검사했다.
- 최신 결정이 `approve`인 기술만 복사한 프로필에 추가하고 기준 프로필 입력은 수정하지 않았다.
- 적용 이력에는 기술명과 사용 증거를 복제하지 않고 추가 항목, 최종 검토와 `skill_id` 참조만 저장했다.
- 승인 기술이 없으면 `no_approved_skills` 이력만 만들고 새 프로필 파일을 만들지 않도록 했다.
- 같은 기준 프로필, 추가안과 최신 판단 집합은 같은 적용 ID를 사용하고 저장된 내용이 같을 때만 재사용했다.
- 명령행에서 추가안 ID만 지정하면 비공개 최종 검토를 모아 적용 결과를 저장하도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/skill_addition_review.py`
- `src/career_agent/profile_input/skill_application.py`
- `src/career_agent/profile_input/__init__.py`
- `scripts/apply_profile_skill_additions.py`
- `tests/test_profile_skill_application.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/profile-applications/profile-skill-application-f40046dc642a41a4a31f1d6a/application.json` 생성, Git 제외

### 확인된 결과

- 최신 승인 우선, 최신 거부 우선, 미검토 및 다른 추가안 분리, 오래된 프로필과 변조된 검토 참조 거부, 기술 중복 차단, 동일 결과 재사용과 무승인 저장을 포함한 새 테스트 9개가 통과했다.
- 전체 단위 테스트 188개가 통과했다.
- 합성 `FastAPI` 기술은 더 늦은 최종 승인이 있을 때만 복사된 새 프로필에 추가됐다.
- 합성 적용 이력에는 `FastAPI` 기술명과 합성 사용 증거가 복제되지 않았다.
- 합성 기준 프로필 객체는 적용 전후 동일하게 유지됐다.
- 실제 비공개 기술 추가안은 항목과 최종 검토가 모두 0개라 `no_approved_skills` 적용 이력만 생성됐다.
- 실제 적용 디렉터리에는 `application.json`만 있고 `profile.json`은 생성되지 않았다.
- 실제 적용 결과는 `.gitignore`의 `private-data/` 규칙으로 Git 추적에서 제외됐다.
- 실제 사용자 승인이나 기술 내용을 임의로 만들지 않았고 공개 예제 프로필도 수정하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 87%로 판단했다.

### 아직 확인하지 못한 점

- 실제 사용자가 최종 승인한 기술을 사용한 통합 적용은 아직 없다.
- 기술 이외의 목표 직무, 경력과 프로젝트 영역은 아직 타입 매핑과 적용을 지원하지 않는다.
- Slack 대화에서 문서 업로드, 후보 확인, 최종 승인과 적용 명령을 연결하지 않았다.
- 실제 공고 10건 중 3건만 상세 분석됐고 사용자의 실제 공고 판단 기록은 아직 없다.

### 막힌 점

현재 없음. 실제 승인 기술 적용 확인에는 사용자가 문서 후보와 세부정보 및 최종 승인을 제공해야 하지만, 다음 Slack 인터페이스의 최소 구현은 독립적으로 진행할 수 있다.

### 주요 판단과 이유

- 원본 프로필을 직접 덮어쓰지 않고 새 버전과 적용 이력을 한 디렉터리에 저장했다.
  - 이유: 잘못된 승인이나 재시도에서도 기준 프로필을 복구할 필요 없이 보존하고 결과의 출처를 추적할 수 있어야 하기 때문이다.
- 승인 기술이 없을 때 빈 새 프로필을 만들지 않았다.
  - 이유: 실제 데이터 변경이 없는 결과를 새 프로필 버전으로 오해하지 않게 하기 위해서다.
- 최신 판단 선택 뒤에도 기준 프로필 지문과 중복을 다시 검사했다.
  - 이유: 추가안 생성 후 프로필이 바뀌거나 오래된 승인이 현재 프로필에 중복 적용되는 것을 막기 위해서다.
- 적용 이력과 새 프로필 저장을 디렉터리 단위로 완료 처리했다.
  - 이유: 승인 기술이 있을 때 두 파일 중 하나만 저장되는 부분 실패를 피하기 위해서다.
- 다음 기능을 최소 Slack 입력 인터페이스로 정했다.
  - 이유: 기술 프로필의 로컬 입력부터 승인 적용까지 내부 경계가 연결됐으므로, 궁극적인 대화형 Agent 목표에서 가장 큰 남은 사용자 가치가 채팅 입력 연결이기 때문이다.

### 다음 할 일

1. Slack 메시지 이벤트를 실제 네트워크 연결 없이 검증 가능한 내부 요청 객체로 변환하는 최소 경계를 정의한다.
2. 첫 범위는 사용자 ID와 채널 ID를 허용 목록으로 제한하고, 텍스트 명령 1개만 기존 기능에 연결한다.
3. Slack 서명 검증, 파일 다운로드와 배포 방식은 다음 단계로 분리한다.

## 2026-09-14 - Slack app_mention의 첫 내부 명령 라우팅

### 목표

실제 Slack 연결 전에 합성 `app_mention` 이벤트를 검증하고, 허용된 사용자의 `다음 공고 찾아줘` 호출을 기존 다음 공고 분석 동작의 실행 전 요청으로 안전하게 변환한다.

### 수행한 작업

- Slack 공식 Events API, `app_mention`, Socket Mode와 요청 서명 검증 문서를 확인했다.
- `event_callback` 안의 `app_mention` 이벤트에서 워크스페이스, 앱, 사용자, 채널과 이벤트 시각 형식을 검증했다.
- 허용 사용자와 허용 채널을 비어 있지 않은 목록으로 제한하고 다른 ID의 이벤트는 내부 동작으로 연결하지 않았다.
- 봇이 만든 이벤트와 메시지 하위 유형을 무시해 자기 응답 반복 가능성을 줄였다.
- 봇 호출을 제거한 뒤 공백만 정규화하고 `다음 공고 찾아줘` 한 문장만 `find_next_job` 명령으로 인식했다.
- 첫 명령을 기존 `analyze_next_greenhouse_review` 동작명에 연결했다.
- 실제 Slack 인증과 작업 실행 전 단계임을 `network_request_verified: false`, `local_validation_only: true`, `execution_status: not_executed`로 명시했다.
- 사용자의 Slack 메시지 원문을 내부 요청 파일에 저장하지 않았다.
- Slack의 이벤트 재전송에 대비해 `team_id`와 `event_id`로 안정적인 요청 ID를 만들고 동일 이벤트를 재사용했다.
- 합성 Slack 설정과 `app_mention` 예제, 로컬 변환 명령을 추가했다.
- AWS와 Docker 없이 시작할 실제 연결 방식으로 Slack Socket Mode를 선택하고 설계 근거와 다음 입력을 문서화했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/__init__.py`
- `src/career_agent/interfaces/slack_events.py`
- `scripts/parse_slack_event.py`
- `tests/test_slack_events.py`
- `data/slack_interface.example.json`
- `data/slack_app_mention.example.json`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/slack-command-requests/slack-command-request-6ba751a7748f64b372e10649.json` 생성, Git 제외

### 확인된 결과

- 정상 명령, 공백 정규화, 미지원 명령, 허용되지 않은 사용자와 채널, 봇 이벤트, 잘못된 워크스페이스와 앱, 잘못된 이벤트 형식, 안전하지 않은 설정, 시간대, 재전송과 손상 저장 파일을 포함한 새 테스트 11개가 통과했다.
- 전체 단위 테스트 199개가 통과했다.
- 공개 합성 이벤트의 `<@U01234567> 다음 공고 찾아줘`가 `find_next_job`과 `analyze_next_greenhouse_review`로 변환됐다.
- 변환 결과는 `not_executed`였으며 실제 Greenhouse API 요청이나 다음 공고 분석은 실행되지 않았다.
- 같은 합성 이벤트를 다시 실행했을 때 새 파일을 만들지 않고 기존 Slack 요청을 재사용했다.
- 저장된 실제 로컬 요청에는 Slack 메시지 원문이 없고 `contains_message_text`가 `false`였다.
- 실제 로컬 요청은 `.gitignore`의 `private-data/` 규칙으로 Git 추적에서 제외됐다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 88%로 판단했다.

### 아직 확인하지 못한 점

- 실제 Slack 워크스페이스와 Socket Mode 연결은 아직 하지 않았다.
- 실제 Slack 이벤트의 인증, 확인 응답과 채널 답변 전송은 아직 없다.
- 식별된 동작을 기존 다음 공고 분석 워크플로로 실제 실행하는 dispatcher는 아직 없다.
- Slack 파일 첨부 다운로드와 문서 입력 연결은 아직 없다.
- 실제 공고 10건 중 3건만 상세 분석됐고 사용자의 실제 공고 판단 기록은 아직 없다.

### 막힌 점

현재 로컬 구현에는 막힌 점이 없다. 실제 연결 시험에는 사용자가 설치 권한을 가진 Slack 워크스페이스와 로컬에서만 설정할 App Token 및 Bot Token이 필요하다.

### 주요 판단과 이유

- 첫 이벤트를 일반 채널 메시지가 아니라 `app_mention`으로 제한했다.
  - 이유: 모든 채널 메시지를 구독하지 않고 사용자가 봇을 직접 호출한 입력만 받는 최소 권한 범위이기 때문이다.
- 허용 사용자와 채널을 모두 검사했다.
  - 이유: 개인용 MVP에서 다른 사용자의 호출이나 잘못된 채널의 실행을 기본적으로 막기 위해서다.
- Slack 메시지 원문을 요청 이력에 저장하지 않았다.
  - 이유: 동작 실행에는 정규화된 명령명만 필요하며 채팅 내용을 불필요하게 보존할 이유가 없기 때문이다.
- 이번 단계에서 실제 공고 분석을 실행하지 않았다.
  - 이유: 네트워크에서 온 이벤트의 인증과 빠른 확인 응답이 구현되기 전에 외부 API 작업을 연결하면 위조 요청과 Slack 재전송으로 중복 분석할 수 있기 때문이다.
- 실제 연결 방식으로 Socket Mode를 선택했다.
  - 이유: 공개 HTTP 서버가 필요 없어 AWS와 Docker 경험이 없는 현재 개인용 로컬 MVP에서 가장 작은 실제 시험 경로이기 때문이다.

### 다음 할 일

1. 최소 권한의 Slack App manifest와 로컬 비밀정보 환경 변수 예제를 만든다.
2. 공식 Slack SDK를 사용한 Socket Mode 수신 어댑터를 추가하되 Token은 환경 변수에서만 읽는다.
3. 이벤트를 즉시 확인 응답하고 인증된 요청만 별도 dispatcher에 넘긴다.
4. 실제 연결 직전에 사용자가 Slack 앱을 워크스페이스에 설치하고 Token을 로컬 `.env`에 입력한다.

## 2026-09-14 - Slack 앱 생성 manifest와 로컬 준비 검사

### 목표

사용자가 Slack 앱 설치 권한을 미리 알지 못해도 최소 권한 앱 생성을 시도할 수 있게 하고, 실제 Token을 공개하지 않은 상태에서 로컬 연결 준비 여부를 확인한다.

### 수행한 작업

- Slack 공식 Bolt for Python 시작 안내와 Socket Mode 앱 생성 절차를 확인했다.
- `career_break` 봇 사용자와 Socket Mode, `app_mention` 구독이 포함된 Slack App manifest를 추가했다.
- Bot Token Scope를 `app_mentions:read`와 `chat:write` 두 개로 제한했다.
- Socket Mode App Token에는 별도로 `connections:write`만 필요하다는 점을 문서화했다.
- 실제 Token 이름만 포함하고 값은 교체 표시인 `.env.example`을 추가했다.
- 실제 Slack ID 설정과 App Token 및 Bot Token 존재 여부를 확인하는 로컬 준비 검사기를 구현했다.
- 검사기는 Token 접두사와 형식만 확인하고 Token 값을 반환하거나 출력하지 않도록 했다.
- 프로세스 환경 변수를 `.env`보다 우선하고, 중복 키와 잘못된 환경 변수 파일 형식을 거부했다.
- 회사 워크스페이스에서 설치가 제한되면 개인 테스트용 워크스페이스로 검증할 수 있음을 안내했다.

### 생성 또는 수정한 파일

- `.env.example`
- `data/slack_app_manifest.example.json`
- `src/career_agent/interfaces/slack_events.py`
- `src/career_agent/interfaces/slack_setup.py`
- `src/career_agent/interfaces/__init__.py`
- `scripts/check_slack_setup.py`
- `tests/test_slack_setup.py`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 최소 manifest 권한, Token 비포함, `.env`와 프로세스 환경 변수 검사, 설정·Token 누락, 교체 표시, 중복 키와 잘못된 줄 형식을 포함한 새 테스트 6개가 통과했다.
- 전체 단위 테스트 205개가 통과했다.
- 공개 manifest에는 `app_mentions:read`, `chat:write`, `app_mention`과 Socket Mode 활성화만 포함됐다.
- 공개 manifest에는 `xapp-` 또는 `xoxb-` Token이 포함되지 않았다.
- `.env`와 `private-data/slack_interface.json`이 각각 Git 제외 규칙에 걸리는 것을 확인했다.
- 공개 예제 Token으로 준비 검사를 실행했을 때 실제 값이 아니라는 이유로 안전하게 실패했고 Token 값은 출력되지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 89%로 판단했다.

### 아직 확인하지 못한 점

- 사용자가 실제 Slack 워크스페이스에서 앱을 생성하고 설치할 수 있는지는 아직 확인하지 않았다.
- 실제 App Token과 Bot Token을 사용한 인증 연결은 아직 없다.
- Slack SDK 설치와 Socket Mode 수신 프로세스는 아직 구현하지 않았다.
- 실제 채널에서 봇 호출과 응답은 아직 확인하지 않았다.

### 막힌 점

코드와 문서 준비에는 막힌 점이 없다. 실제 연결로 넘어가려면 사용자가 Slack 앱 생성과 워크스페이스 설치를 시도해야 한다.

### 주요 판단과 이유

- 앱 설정을 수동으로 여러 화면에서 구성하기보다 manifest를 제공했다.
  - 이유: 사용자가 Slack 앱을 처음 만들어도 필요한 권한과 이벤트를 빠뜨리지 않고 재현할 수 있어야 하기 때문이다.
- Bot Token Scope를 두 개로 제한했다.
  - 이유: 첫 기능은 직접 호출 메시지를 받고 같은 채널에 답하는 것뿐이므로 파일이나 채널 기록 읽기 권한은 아직 필요하지 않기 때문이다.
- 준비 검사에서 Slack 네트워크에 접속하지 않았다.
  - 이유: 실제 SDK 연결 전에도 로컬 파일과 비밀정보 배치 오류를 분리해서 확인할 수 있어야 하기 때문이다.
- Token을 사용자에게 채팅으로 요청하지 않았다.
  - 이유: Token은 비밀번호와 같은 비밀정보이며 공개 대화나 작업일지에 남기지 않아야 하기 때문이다.

### 다음 할 일

1. 사용자가 Slack 앱 생성 화면에서 `data/slack_app_manifest.example.json`으로 앱을 만든다.
2. 워크스페이스 설치가 허용되는지 확인한다.
3. 설치가 끝나면 실제 ID 설정과 Token을 로컬 비공개 파일에 입력하고 준비 검사 결과를 확인한다.
4. 준비 완료 뒤 공식 Slack SDK와 Socket Mode 수신 어댑터를 구현한다.

## 2026-09-14 - 실제 Slack Token 인증과 Socket Mode 가용성 확인

### 목표

사용자가 로컬 `.env`에 입력한 Bot Token과 App Token을 화면이나 저장 결과에 노출하지 않고 Slack 공식 API로 검증하며, Socket Mode 연결을 시작할 수 있는지 확인한다.

### 수행한 작업

- Slack 공식 `auth.test`와 `apps.connections.open` 응답 계약을 확인했다.
- `.env` 또는 프로세스 환경 변수에서 검증된 App Token과 Bot Token을 읽는 내부 함수를 분리했다.
- Bot Token은 `auth.test`, App Token은 `apps.connections.open`에 각각 POST로 보내는 최소 인증 검증기를 구현했다.
- Slack API 호출 대상을 허용된 두 메서드로 제한하고 외부 리디렉션을 거부했다.
- 응답 HTTP 상태, JSON 형식, 최대 64KB 크기와 필수 Slack ID 형식을 검증했다.
- Socket Mode 응답 URL이 `wss`인지 확인하되 URL 자체는 결과에 저장하지 않았다.
- 인증 결과에 Token과 Socket URL을 포함하지 않고 상태와 식별자만 비공개 디렉터리에 저장했다.
- 같은 앱과 워크스페이스의 인증 결과는 안정적인 ID로 재사용했다.
- 실제 Slack 인증 요청을 실행했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_setup.py`
- `src/career_agent/interfaces/slack_auth.py`
- `src/career_agent/interfaces/__init__.py`
- `scripts/verify_slack_tokens.py`
- `tests/test_slack_auth.py`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/slack-auth/` 아래 인증 결과 1개 생성, Git 제외

### 확인된 결과

- 정상 인증, App ID가 없는 응답, Slack 오류, 잘못된 식별자와 Socket URL, JSON·크기 제한, 입력 시간대와 timeout, Token 비저장 및 결과 재사용을 포함한 새 테스트 7개가 통과했다.
- 전체 단위 테스트 212개가 통과했다.
- 실제 Bot Token이 Slack `auth.test`에서 인증됐다.
- 실제 App Token으로 Socket Mode 연결 URL을 발급받을 수 있음을 확인했다.
- 실제 인증 결과 재실행 시 기존 비공개 결과를 재사용했다.
- Token과 임시 WebSocket URL은 콘솔, 인증 결과와 작업일지에 기록하지 않았다.
- 실제 `.env`와 인증 결과 디렉터리가 Git 제외 규칙에 걸리는 것을 다시 확인했다.
- `auth.test` 응답에는 App ID가 없어 수동 확인이 필요한 상태다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 90%로 판단했다.

### 아직 확인하지 못한 점

- 실제 앱의 App ID, 사용자 본인 ID와 테스트 채널 ID를 허용 설정에 반영하지 않았다.
- 실제 Socket Mode 연결을 유지하고 `app_mention` 이벤트를 받지 않았다.
- Slack 채널에 응답하거나 다음 공고 분석을 실제 실행하지 않았다.
- Slack SDK 의존성을 프로젝트에 추가하지 않았다.

### 막힌 점

실제 허용 설정 생성에는 사용자가 Slack 화면에서 확인할 수 있는 App ID, 본인 User ID와 테스트 Channel ID가 필요하다. 이 값들은 Token이 아니지만 개인 워크스페이스 식별 정보이므로 공개 문서와 작업일지에는 기록하지 않는다.

### 주요 판단과 이유

- Bot Token과 App Token을 서로 다른 공식 API로 검증했다.
  - 이유: Bot Token의 앱 설치 인증과 App Token의 Socket Mode 연결 권한은 서로 다른 목적이므로 둘 다 성공해야 실제 수신을 시작할 수 있기 때문이다.
- 임시 WebSocket URL을 버렸다.
  - 이유: 연결할 때마다 새로 발급되는 인증 포함 URL이며 준비 확인 결과로 보존할 필요가 없기 때문이다.
- 인증 결과에는 상태와 ID만 저장했다.
  - 이유: 다음 설정 자동화에 필요한 최소 정보만 남기고 비밀정보 노출 위험을 줄이기 위해서다.

### 다음 할 일

1. 사용자가 App ID, 본인 User ID와 테스트 Channel ID를 확인한다.
2. 세 ID를 비공개 `private-data/slack_interface.json`에 반영한다.
3. 준비 검사를 통과시킨 뒤 Slack SDK와 실제 Socket Mode 수신 어댑터를 구현한다.

## 2026-09-14 - 실제 Slack 개인 허용 설정 생성

### 목표

사용자가 확인한 App ID, 본인 User ID와 테스트 Channel ID를 검증된 Slack 앱 인증 결과와 안전하게 결합하고, 실제 Socket Mode 수신기 구현에 필요한 로컬 설정을 완성한다.

### 수행한 작업

- 검증된 Slack 인증 결과만 ID로 안전하게 불러오는 로더를 구현했다.
- 인증 결과의 워크스페이스와 봇 사용자 정보에 사용자가 확인한 앱·사용자·채널 ID를 결합했다.
- ID 형식과 개인 허용 목록을 기존 Slack 이벤트 설정 계약으로 다시 검증했다.
- 비공개 설정을 원자적으로 생성하고, 같은 설정은 재사용하되 다른 내용으로 기존 파일을 덮어쓰지 않도록 했다.
- 실제 식별자 값을 콘솔에 다시 출력하지 않는 설정 생성 명령을 추가했다.
- 실제 비공개 설정을 생성한 뒤 로컬 Slack 준비 검사를 실행했다.
- 공개 문서에는 실제 ID를 기록하지 않고 재현 가능한 자리표시자 명령과 현재 상태만 반영했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_config.py`
- `src/career_agent/interfaces/__init__.py`
- `scripts/configure_slack_interface.py`
- `tests/test_slack_config.py`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`
- `private-data/slack_interface.json` 생성, Git 제외

### 확인된 결과

- 실제 인증 결과의 워크스페이스·봇 정보와 사용자가 제공한 앱·사용자·채널 ID가 유효한 비공개 설정으로 결합됐다.
- `python scripts/check_slack_setup.py`에서 ID와 허용 목록, App Token, Bot Token이 모두 준비된 것으로 확인됐다.
- 같은 설정 생성 명령을 다시 실행했을 때 기존 비공개 설정을 재사용했다.
- `.env`와 `private-data/slack_interface.json`이 Git 제외 규칙에 걸리는 것을 확인했다.
- 인증 결과 위조, App ID 불일치, 잘못된 ID, 경로 조작과 기존 설정 덮어쓰기를 거부하는 새 테스트 7개가 통과했다.
- 전체 단위 테스트 219개가 통과했다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 91%로 판단했다.

### 아직 확인하지 못한 점

- 실제 Socket Mode 연결을 유지하고 Slack `app_mention` 이벤트를 받지 않았다.
- Slack 채널에 확인 메시지나 분석 결과를 보내지 않았다.
- Slack 명령을 실제 다음 공고 분석 작업으로 실행하지 않았다.
- Slack SDK 의존성을 프로젝트에 추가하지 않았다.

### 막힌 점

현재 단계의 막힌 점은 없다. 실제 Slack 메시지 수신기를 구현하고 실행한 뒤에는 사용자가 지정 채널에서 봇을 호출해 확인해야 한다.

### 주요 판단과 이유

- 사용자가 제공한 식별자는 Token과 분리해 Git 제외 JSON에만 저장했다.
  - 이유: Slack 이벤트 허용 검증에는 필요하지만 공개 저장소에 개인 워크스페이스 정보를 남길 필요는 없기 때문이다.
- 워크스페이스와 봇 사용자 ID는 사용자가 다시 입력하지 않고 검증된 인증 결과에서 가져왔다.
  - 이유: 수동 입력을 줄이고 실제 인증된 앱 정체성과 허용 설정이 어긋날 가능성을 낮추기 위해서다.
- 기존 설정이 다르면 자동 갱신하지 않고 오류로 중단한다.
  - 이유: 잘못된 사용자나 채널로 허용 범위가 조용히 바뀌는 일을 방지하기 위해서다.

### 다음 할 일

1. 공식 Slack SDK를 최소 의존성으로 추가한다.
2. Socket Mode로 `app_mention` 이벤트 1건을 받아 기존 로컬 변환기에 전달하는 수신기를 구현한다.
3. 첫 실행에서는 공고 분석을 호출하기 전에 안전한 확인 응답만 Slack 채널로 보낸다.
4. 실제 채널 호출이 확인되면 기존 다음 공고 1건 분석 동작을 연결한다.

## 2026-09-14 - 실제 Slack Socket Mode 수신기 구현과 연결

### 목표

AWS, Docker와 공개 HTTP 서버 없이 로컬 PC에서 실제 Slack `app_mention`을 받을 수 있는 최소 수신기를 구현하고, 공고 분석을 실행하기 전 안전한 고정 확인 응답 경계까지 만든다.

### 수행한 작업

- Slack 공식 문서의 Bolt for Python 내장 Socket Mode 방식을 현재 기준으로 다시 확인했다.
- 공식 `slack-bolt` 패키지 버전을 고정한 의존성 파일을 추가하고 로컬 Python에 설치했다.
- 기존 Slack 설정 파일을 불러오면서 즉시 계약을 검증하는 공개 로더를 추가했다.
- 실제 `app_mention`을 기존 워크스페이스·앱·사용자·채널 허용 검사와 명령 변환기에 전달하는 수신 어댑터를 구현했다.
- 지원 명령에는 공고 분석이 아직 실행되지 않았다는 고정 문구를 원래 메시지 스레드에 보내도록 했다.
- 허용된 사용자의 미지원 명령에는 현재 지원 명령만 안내하고, 허용되지 않은 사용자·채널에는 답하지 않도록 했다.
- 같은 `event_id` 재전송에는 기존 요청을 재사용하고 응답을 중복 전송하지 않도록 했다.
- 합성 로컬 이벤트와 실제 Socket Mode 이벤트의 전송 경로 메타데이터를 구분했다.
- 실제 비공개 Token과 설정으로 Socket Mode 수신기를 실행했다.

### 생성 또는 수정한 파일

- `requirements.txt`
- `src/career_agent/interfaces/slack_setup.py`
- `src/career_agent/interfaces/slack_events.py`
- `src/career_agent/interfaces/slack_socket.py`
- `src/career_agent/interfaces/__init__.py`
- `scripts/run_slack_socket.py`
- `tests/test_slack_events.py`
- `tests/test_slack_socket.py`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `slack-bolt`와 그 공식 SDK 의존성이 로컬 Python에 설치됐다.
- 지원 명령, 미지원 명령, 비허용 사용자, 중복 이벤트, 스레드 응답과 잘못된 이벤트 거부를 포함한 새 Socket Mode 경계 테스트 5개가 통과했다.
- 실제 전송 경로 메타데이터 구분 테스트 1개가 통과했다.
- 전체 단위 테스트 225개가 통과했다.
- 실제 Token으로 Socket Mode 수신기가 시작됐고 SDK의 연결 실행 상태를 확인했다.
- Token, 실제 Slack 식별자와 메시지 원문을 콘솔이나 공개 파일에 기록하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 92%로 판단했다.

### 아직 확인하지 못한 점

- 사용자가 실제 지정 채널에서 봇을 호출했을 때 이벤트가 저장되고 스레드 답변이 보이는지 확인하지 않았다.
- Slack 명령을 기존 다음 공고 분석 동작으로 실행하지 않았다.
- 공고 분석 완료 또는 실패 결과를 Slack으로 보내지 않았다.
- PC 재시작 후 수신기를 자동 실행하는 운영 방식은 정하지 않았다.

### 막힌 점

실제 수신과 답변의 최종 확인에는 사용자가 지정 Slack 채널에서 `@career_break 다음 공고 찾아줘`를 한 번 보내야 한다. 수신기는 현재 실행 중이다.

### 주요 판단과 이유

- 공식 Slack Bolt의 내장 Socket Mode 어댑터를 선택했다.
  - 이유: 공개 서버, AWS와 Docker 없이 로컬 연결이 가능하고 Slack이 기본 어댑터 사용을 권장하기 때문이다.
- 이번 단계에서는 공고 분석을 실행하지 않고 고정 응답만 보낸다.
  - 이유: 외부 채팅 수신·허용 검사·응답 경계를 먼저 실제로 검증한 뒤 기존 분석 동작을 연결해야 실패 지점을 분리할 수 있기 때문이다.
- 실제 이벤트와 합성 이벤트의 메타데이터를 다르게 기록한다.
  - 이유: 실제 Slack 전송 경로를 로컬 테스트로 잘못 표시하면 이후 실행 감사 기록의 근거가 부정확해지기 때문이다.

### 다음 할 일

1. 사용자가 지정 채널에서 봇을 호출한다.
2. 비공개 요청 저장과 Slack 스레드 확인 응답을 실제로 확인한다.
3. 확인이 끝나면 기존 다음 공고 1건 분석 워크플로를 Slack 명령 뒤에 연결한다.

## 2026-09-15 - 실제 Slack 채널 수신 실패 진단과 설정 교정

### 목표

사용자가 실제 Slack 채널에서 봇을 호출했지만 스레드 답변이 나타나지 않은 원인을 로그와 비공개 요청 기록으로 분리해 확인하고, 실제 수신·라우팅·응답 전송까지 검증한다.

### 수행한 작업

- 이전 Socket Mode 프로세스가 대화 턴 종료와 함께 종료된 사실을 확인했다.
- Git 제외 런타임 로그를 사용하는 숨김 백그라운드 프로세스로 수신기를 다시 실행했다.
- 실제 `app_mention` 이벤트가 수신됐지만 허용 채널 불일치로 거부된 결과를 확인했다.
- 워크스페이스·앱·사용자 검증을 통과한 실제 이벤트의 Channel ID로 비공개 허용 설정을 교정했다.
- 기존 비공개 설정은 `private-data/` 안에 별도 백업으로 보존했다.
- 준비 검사를 다시 통과시킨 뒤 백그라운드 수신기를 재시작했다.
- 새 실제 이벤트의 라우팅 결과와 런타임 오류 로그를 확인했다.

### 생성 또는 수정한 파일

- `작업일지.md`
- `private-data/slack_interface.json` 갱신, Git 제외
- `private-data/slack_interface.before-channel-fix-20260915.json` 백업, Git 제외
- `private-data/slack-runtime/` 런타임 로그 생성, Git 제외
- `private-data/slack-command-requests/` 실제 요청 기록 추가, Git 제외

### 확인된 결과

- 백그라운드 Socket Mode 수신기가 실행 중이며 SDK 연결 상태가 정상이다.
- 교정 전 실제 이벤트는 `channel_not_allowed`로 안전하게 거부됐다.
- 교정 후 새 실제 이벤트는 `find_next_job` 명령과 `analyze_next_greenhouse_review` 동작으로 정상 식별됐다.
- 실제 이벤트의 네트워크 인증 경로 표시가 참이고 메시지 원문 제외 표시가 유지됐다.
- 현재 단계의 실행 상태는 의도대로 `not_executed`다.
- Slack 응답 전송 과정에서 SDK 오류가 발생하지 않았다.
- 사용자가 실제 Slack 화면에서 봇의 스레드 답변을 확인했다.
- 실제 Slack ID와 Token은 공개 파일과 로그에 기록하지 않았다.

### 아직 확인하지 못한 점

- Slack 명령으로 실제 다음 공고 분석을 실행하지 않았다.

### 막힌 점

현재 단계의 막힌 점은 없다.

### 주요 판단과 이유

- 첫 실패를 곧바로 코드 오류로 가정하지 않고 프로세스, 수신 기록과 허용 설정을 순서대로 확인했다.
  - 이유: 관찰 결과 첫 시도는 수신기 종료, 다음 시도는 채널 불일치라는 서로 다른 원인이었기 때문이다.
- 사용자가 제공한 채널 URL 값보다 실제로 검증된 Slack 이벤트의 Channel ID를 사용했다.
  - 이유: 같은 워크스페이스·앱·사용자 검사를 통과해 지정 채널에서 도착한 이벤트가 실제 런타임의 직접 근거이기 때문이다.
- 기존 설정을 삭제하지 않고 비공개 백업으로 이동했다.
  - 이유: 설정 교정 전후를 복구할 수 있게 보존하기 위해서다.

### 다음 할 일

1. 고정 안내 응답 뒤 기존 다음 공고 1건 분석 워크플로를 연결한다.
2. 분석 완료 또는 실패 결과를 같은 Slack 스레드에 전달한다.

## 2026-09-15 - Slack 명령에 다음 공고 1건 분석 연결

### 목표

실제 Slack의 `다음 공고 찾아줘` 명령이 기존 검토 큐에서 미분석 공고 1건만 분석하고, 민감한 내부 정보를 제외한 결과를 같은 스레드에 전달하도록 연결한다.

### 수행한 작업

- Slack에서 허용할 내부 동작을 `analyze_next_greenhouse_review` 하나로 제한한 실행 어댑터를 구현했다.
- 쉘 명령 문자열을 사용하지 않고 현재 Python과 고정된 분석 스크립트 경로만 실행하도록 했다.
- 실행 제한 시간을 최대 300초로 제한하고 기본값을 180초로 설정했다.
- 분석 시작 안내를 먼저 전송하고 성공 또는 실패 요약을 같은 스레드에 보내도록 수신기를 확장했다.
- 성공 출력에서 사용자에게 필요한 회사·공고명, 지원 판단, 확인 항목, 과제 수와 큐 상태만 선택했다.
- 외부 공고 문자열의 Slack 제어문자를 변환하고 로컬 경로와 원본 오류는 응답에서 제외했다.
- 같은 Slack 이벤트 재전송에는 분석 실행과 응답을 중복하지 않도록 기존 불변 요청 저장 경계를 유지했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_actions.py`
- `src/career_agent/interfaces/slack_socket.py`
- `src/career_agent/interfaces/__init__.py`
- `scripts/run_slack_socket.py`
- `tests/test_slack_actions.py`
- `tests/test_slack_socket.py`
- `docs/SLACK_INTERFACE.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 허용 동작의 고정 실행, 출력 선별, Slack 제어문자 변환, 실패 정보 비노출과 제한 시간 테스트 4개가 통과했다.
- Slack 수신기의 분석 실행 1회 제한과 안전한 실패 응답 테스트 2개가 통과했다.
- 전체 단위 테스트 231개가 통과했다.
- 실제 Slack 수신·고정 응답 단계에서 사용자가 스레드 답변을 확인했다.

### 아직 확인하지 못한 점

- 확장된 수신기를 실제 Slack 명령으로 실행해 다음 공고 분석과 결과 요약을 확인하지 않았다.
- 현재 분석 명령은 기존 CLI와 같은 비식별 예제 프로필과 로컬 검토 큐를 사용한다.
- 장시간 분석을 별도 작업 큐로 분리하거나 중단 후 재개하는 기능은 없다.

### 막힌 점

실제 분석 확인에는 확장된 수신기를 실행한 뒤 사용자가 Slack 명령을 한 번 새로 보내야 한다.

### 주요 판단과 이유

- 기존 검증된 CLI를 고정 인자 하위 프로세스로 재사용했다.
  - 이유: 이번 단계에서 검증된 분석 로직을 다시 구현하지 않고 Slack 실행 경계만 가장 작게 연결하기 위해서다.
- 성공 응답을 CLI 전체 출력이 아니라 허용 목록 필드로 구성했다.
  - 이유: 로컬 경로와 예상하지 못한 외부 문자열이 Slack으로 그대로 노출되는 일을 막기 위해서다.
- 오류 원문을 Slack으로 보내지 않는다.
  - 이유: 외부 응답과 로컬 경로가 포함될 수 있으며 상세 원인은 기존 비공개 실행 이력과 런타임 로그에서 확인할 수 있기 때문이다.

### 다음 할 일

1. 확장된 Socket Mode 수신기를 실행한다.
2. 사용자가 `@career_break 다음 공고 찾아줘`를 새로 보낸다.
3. 분석 실행 이력과 Slack 결과 요약을 함께 확인한다.

## 2026-09-15 - Slack 명령의 실제 다음 공고 분석 확인

### 목표

확장된 Socket Mode 수신기에서 실제 Slack 명령을 받아 검토 큐의 다음 공고 1건을 분석하고 안전한 결과 요약을 전송하는 전체 경로를 확인한다.

### 수행한 작업

- 분석 기능이 연결된 새 Socket Mode 수신기를 비공개 백그라운드 프로세스로 실행했다.
- 실행 전 Slack 요청, 분석 실행 이력과 분석 파일 수를 기준값으로 확인했다.
- 사용자가 지정 채널에서 새 `다음 공고 찾아줘` 명령을 보냈다.
- 새 요청의 라우팅 결과, 새 분석 파일, 실행 이력과 갱신된 검토 큐를 확인했다.
- Slack 런타임 오류 로그와 수신기 실행 상태를 확인했다.

### 생성 또는 수정한 파일

- `README.md`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `작업일지.md`
- `private-data/slack-command-requests/` 실제 요청 기록 추가, Git 제외
- `private-data/agent-runs/` 실제 분석 결과 추가, Git 제외
- `private-data/execution-runs/` 실제 실행 이력 추가, Git 제외
- `private-data/review-queues/` 갱신 큐 추가, Git 제외

### 확인된 결과

- 실제 Slack 요청이 `find_next_job`과 `analyze_next_greenhouse_review`로 정상 식별됐다.
- Moloco의 `Software Engineer - Ads Experience` 공고가 실제로 분석됐다.
- 실행 이력 상태가 `analyzed`로 저장됐다.
- 현재 큐의 분석 완료 수가 4건, 분석 필요 수가 6건으로 갱신됐다.
- Slack 런타임 오류 로그는 비어 있고 수신기는 계속 실행 중이다.
- 실제 Slack ID, Token과 메시지 원문을 공개 파일에 기록하지 않았다.
- 이번 기능 반영 후 확장된 개인용 MVP 전체 진행률은 약 94%로 판단했다.

### 아직 확인하지 못한 점

- 사용자가 이번 분석 요약 두 번째 답변을 Slack 화면에서 직접 확인했는지는 아직 답변받지 않았다.
- Slack 결과에는 공고 원문 링크가 포함되지 않는다.
- Slack에서 적합·보류·부적합 사용자 판단을 기록할 수 없다.

### 막힌 점

현재 분석 실행의 기술적 막힌 점은 없다. 다음 기능 우선순위 판단을 위해 사용자가 Slack 분석 요약 표시를 확인할 필요가 있다.

### 주요 판단과 이유

- 실제 실행 전후의 요청·분석·실행 이력 수를 비교했다.
  - 이유: Slack 답변 표시만으로 판단하지 않고 내부 분석이 정확히 1건 수행됐는지 확인하기 위해서다.
- Slack 런타임 오류 로그와 실행 이력을 함께 확인했다.
  - 이유: 분석 저장 성공과 메시지 전송 경계의 오류 유무를 분리해서 검증하기 위해서다.

### 다음 할 일

1. 사용자가 Slack에서 분석 완료 요약 표시를 확인한다.
2. 성공 요약에 검증된 공고 원문 링크를 추가한다.
3. Slack의 사용자 적합·보류·부적합 판단을 기존 비공개 피드백 기록에 연결한다.

## 2026-09-15 - Slack 공고 분석 답변 정보 확장

### 목표

숫자 위주의 최소 분석 응답이 실제 공고 판단에 부족하다는 사용자 피드백을 반영해, 기존 분석 근거와 원문 링크를 Slack에서 바로 읽을 수 있는 요약으로 제공한다.

### 수행한 작업

- 추가 명령이 남은 검토 큐를 계속 소비하지 않도록 백그라운드 수신기를 종료했다.
- 최신 실제 분석 파일에 원문 URL, 강점, 부족, 미확인 질문과 지원 판단 근거가 이미 저장돼 있음을 확인했다.
- CLI가 저장한 분석 파일 경로를 `private-data/agent-runs/` 아래 JSON으로 제한해 다시 불러오도록 했다.
- 최대 2MB 파일 크기, 현재 분석 워크플로와 상태를 검증했다.
- HTTPS이며 인증정보와 Slack 링크 제어문자가 없는 원문 URL만 응답 링크로 허용했다.
- 회사·공고명, 원문 링크, 지원 판단 근거, 강점과 프로젝트 증거, 부족, 우선 확인 질문, 다음 행동과 큐 상태를 영역별로 구성했다.
- 외부 문자열의 Slack 제어문자를 변환하고 각 항목 수와 길이를 제한했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_actions.py`
- `tests/test_slack_actions.py`
- `README.md`
- `docs/SLACK_INTERFACE.md`
- `작업일지.md`

### 확인된 결과

- 성공 상세 요약, 근거 표시, 경로 비노출, 허용 경로 밖 파일 거부와 위험 URL 거부를 포함한 Slack 동작 테스트 6개가 통과했다.
- 전체 단위 테스트 233개가 통과했다.
- 최신 실제 분석 결과로 만든 요약은 939자이며 Slack 메시지 제한 안이다.
- 실제 요약에 원문 링크, 지원 판단, 확인된 강점과 우선 확인 질문이 포함되는 것을 확인했다.
- 여러 실제 Slack 명령 실행 결과 현재 큐는 분석 완료 5건, 분석 필요 5건이다.

### 아직 확인하지 못한 점

- 개선된 상세 요약을 실제 Slack 스레드에서 확인하지 않았다.
- Slack에서 사용자 적합·보류·부적합 판단을 기록할 수 없다.

### 막힌 점

개선 결과의 실제 화면 확인에는 새 수신기를 실행하고 사용자가 다음 공고 명령을 한 번 보내야 한다.

### 주요 판단과 이유

- 새 분석을 만들지 않고 이미 저장된 근거를 사용자 응답에 선별했다.
  - 이유: 문제는 분석 정보의 부재가 아니라 Slack 포맷터가 기존 결과를 버리고 있던 것이기 때문이다.
- 전체 공고 원문과 모든 미확인 항목을 보내지 않았다.
  - 이유: Slack에서 읽을 수 있는 길이를 유지하고 중요한 판단 근거를 우선하기 위해서다.
- 분석 파일 경로와 URL을 다시 검증한 뒤 읽고 표시한다.
  - 이유: 하위 프로세스 출력과 외부 공고 URL을 그대로 신뢰하지 않기 위해서다.

### 다음 할 일

1. 개선된 수신기를 다시 실행한다.
2. 사용자가 새 다음 공고 명령을 보내 상세 요약을 확인한다.
3. 확인 후 적합·보류·부적합 사용자 판단 입력을 연결한다.

## 2026-09-15 - 비정기 모집 공고 자동 분석 제외

### 목표

Slack 상세 응답에서 확인된 채용관심등록 공고가 일반 채용공고처럼 다음 분석 대상으로 선택되는 문제를 가장 작은 규칙으로 막는다.

### 수행한 작업

- 사용자가 Slack에서 원문 링크와 판단 근거를 포함한 상세 응답이 정상 표시되는 것을 확인했다.
- `Expression of Interest`, `채용관심등록`, `Talent Pool`, `Talent Community`, `General Application`, `인재풀`이 제목에 명시된 후보를 비정기 모집으로 분류했다.
- 새 발견 기록에서는 해당 후보를 삭제하지 않고 `low` 우선순위로 보존하도록 했다.
- 과거 발견 기록이 이미 `review`로 저장돼 있어도 새 검토 큐를 만들 때 같은 제목 규칙으로 제외하도록 했다.
- 기존 분석 결과와 큐 파일을 덮어쓰지 않고 새 검토 큐 스냅샷을 생성했다.

### 생성 또는 수정한 파일

- `src/career_agent/discovery/ranking.py`
- `src/career_agent/review/queue.py`
- `tests/test_greenhouse_discovery.py`
- `tests/test_greenhouse_review_queue.py`
- `README.md`
- `작업일지.md`
- `private-data/review-queues/` 새 검토 큐 추가, Git 제외

### 확인된 결과

- 새 수집 시 채용관심등록 제목을 `low`로 낮추는 재현 테스트가 통과했다.
- 과거 `review` 기록의 비정기 모집 제목을 검토 큐에서 제외하는 재현 테스트가 통과했다.
- 일반 `Software Engineer` 제목은 같은 규칙으로 제외되지 않았다.
- 전체 단위 테스트 235개가 통과했다.
- 실제 새 큐 10건에 이전 채용관심등록 공고가 포함되지 않았다.
- 새 큐는 최신 규칙 분석 완료 5건, 상세 분석 필요 5건이다.

### 아직 확인하지 못한 점

- 제목에 비정기 모집 성격이 명시되지 않고 본문에만 표시된 공고는 이번 규칙으로 구분하지 못한다.
- 다음 미분석 후보 중 `Security Engineer`가 현재 프로필에 충분히 가까운지는 아직 상세 분석 전이다.
- Slack에서 적합·보류·부적합 사용자 판단을 기록할 수 없다.

### 막힌 점

현재 단계의 기술적 막힌 점은 없다.

### 주요 판단과 이유

- 명시적인 제목 표현만 사용했다.
  - 이유: 정상 채용공고를 추측으로 제외하는 오탐을 줄이기 위해서다.
- 후보를 발견 저장소에서 삭제하지 않고 `low`로 보존했다.
  - 이유: 수집 이력을 유지하면서 실제 분석 비용만 막기 위해서다.
- 큐에서도 같은 규칙을 다시 적용했다.
  - 이유: 기존에 저장된 발견 결과를 다시 수집하지 않아도 즉시 잘못된 선택을 막기 위해서다.

### 다음 할 일

1. `review` 후보의 직무 관련성 정렬을 개선해 일반적인 `Engineer` 단어만으로 보안·관리 직무가 앞서는 문제를 줄인다.
2. 그다음 Slack에서 적합·보류·부적합 사용자 판단을 기존 비공개 피드백 기록에 연결한다.

## 2026-09-15 - 프로필 근거 없는 일반 직무 후보 제외

### 목표

`Security Engineer`처럼 일반적인 `Engineer` 단어 하나만 프로필 직무 표현과 겹치는 공고가 다음 자동 분석 대상으로 선택되지 않도록 한다.

### 수행한 작업

- 검토 큐의 단어 비교를 전체 직무 표현의 단순 합산에서 목표 직무 축별 조합 비교로 변경했다.
- `review` 후보는 같은 직무 축의 단어가 두 개 이상 일치하거나 `ITSM`, `FinOps`처럼 한 단어 자체가 고유한 검색 표현일 때만 큐에 포함했다.
- `Engineer`와 `엔지니어`는 다른 구별 신호가 함께 있을 때만 근거가 되도록 했다.
- 영문과 한글로 같은 일반 직무명이 병기된 경우를 서로 다른 두 근거로 세지 않도록 했다.
- 특정 보안 직무를 수동 제외어로 추가하지 않고 현재 프로필에서 생성된 직무 축만 사용했다.
- 기존 파일을 덮어쓰지 않고 실제 저장 목록으로 새 검토 큐를 생성했다.

### 생성 또는 수정한 파일

- `src/career_agent/review/queue.py`
- `tests/test_greenhouse_review_queue.py`
- `docs/REVIEW_QUEUE_SCHEMA.md`
- `README.md`
- `작업일지.md`
- `private-data/review-queues/` 새 검토 큐 추가, Git 제외

### 확인된 결과

- 구별 신호가 없는 `Security Engineer`와 영문·한글 일반 직무명 중복 후보가 큐에서 제외되는 테스트가 통과했다.
- `Automation + Engineer` 조합과 단일 고유 표현 `ITSM`은 유지되는 테스트가 통과했다.
- 전체 단위 테스트 236개가 통과했다.
- 실제 최신 큐에서 `Security Engineer`와 일반 직무명만 일치한 후보가 제외됐다.
- 실제 큐는 4건으로 줄었고 최신 분석 완료 1건, 상세 분석 필요 3건이다.
- 미분석 3건은 모두 지역 또는 고용 형태가 명시적으로 불일치해 현재 자동 상세 분석 가능한 후보는 0건이다.

### 아직 확인하지 못한 점

- 본문을 읽으면 관련성이 드러나지만 제목에 목표 직무 신호가 없는 공고는 이번 목록 단계에서 제외될 수 있다.
- 새 적합 후보가 없을 때 Slack이 이를 일반 실행 실패가 아닌 정상적인 검색 결과로 안내하지는 않는다.
- Slack에서 적합·보류·부적합 사용자 판단을 기록할 수 없다.

### 막힌 점

현재 단계의 기술적 막힌 점은 없다.

### 주요 판단과 이유

- 특정 직무의 제외어 목록을 만들지 않았다.
  - 이유: 프로필이 바뀌어도 사용자에게 제외 키워드를 다시 요구하지 않고 생성된 목표 직무 축으로 판단하기 위해서다.
- 관련 근거가 약하면 자동 상세 조회하지 않는 쪽을 선택했다.
  - 이유: 관련 없는 공고를 억지로 추천하는 것보다 새 적합 후보가 없다고 알리는 것이 개인용 Agent의 신뢰도에 더 중요하기 때문이다.
- `ITSM`, `FinOps` 같은 한 단어 검색 표현에는 예외를 뒀다.
  - 이유: 의미가 고유한 단일 표현까지 두 단어 조건으로 누락하지 않기 위해서다.

### 다음 할 일

1. 새 적합 후보가 없을 때 Slack에 실패가 아닌 정상 검색 결과로 안내한다.
2. 그다음 Slack에서 적합·보류·부적합 사용자 판단을 기존 비공개 피드백 기록에 연결한다.

## 2026-09-15 - Slack 빈 검색 결과 정상 처리와 MVP 목표 재정렬

### 목표

현재 조건에 맞는 새 공고가 없을 때 Slack에 일반 실패가 아닌 정상 결과를 전달하고, 프로젝트 문서를 사용자가 다시 확인한 궁극 목표에 맞춘다.

### 수행한 작업

- 유효한 큐에 분석 가능한 후보가 없는 상태를 별도 예외로 구분했다.
- 실행 이력에 오류가 없는 `no_eligible_candidate` 상태를 추가했다.
- 분석 명령이 이 상태에서 공고 상세 API를 호출하거나 분석 파일을 만들지 않고 정상 종료하도록 했다.
- Slack 어댑터가 고정 완료 표시와 큐 상태만 읽어 새 공고 없음 안내를 만들도록 했다.
- 오류 원문, 로컬 경로 또는 외부 문자열을 Slack 안내에 포함하지 않았다.
- PRD의 복사·붙여넣기 중심 초기 범위와 94% 진행률을 현재 합의한 Slack 첨부자료 기반 개인용 MVP로 다시 정의했다.
- 궁극 목표 기준 현재 진행률을 약 65%로 수정하고 남은 핵심 경로를 명시했다.

### 생성 또는 수정한 파일

- `src/career_agent/review/analysis.py`
- `src/career_agent/review/__init__.py`
- `src/career_agent/execution/log.py`
- `src/career_agent/interfaces/slack_actions.py`
- `scripts/analyze_next_greenhouse_review.py`
- `tests/test_greenhouse_review_analysis.py`
- `tests/test_execution_log.py`
- `tests/test_slack_actions.py`
- `docs/PRD.md`
- `docs/SLACK_INTERFACE.md`
- `README.md`
- `작업일지.md`
- `private-data/execution-runs/` 정상 빈 결과 실행 이력 추가, Git 제외

### 확인된 결과

- 후보 없음 예외, 정상 실행 이력과 Slack 고정 응답 테스트가 통과했다.
- 실제 최신 큐로 분석 명령이 종료 코드 0과 `no_eligible_candidate` 실행 상태를 반환했다.
- 실제 빈 결과 실행에는 분석 참조가 생성되지 않았다.
- 전체 단위 테스트 239개가 통과했다.

### 아직 확인하지 못한 점

- 새 빈 결과 안내가 실제 Slack 스레드에 표시되는지는 확인하지 않았다.
- Slack 첨부파일 이벤트 수신과 파일 다운로드는 구현하지 않았다.
- 첨부파일에서 추출한 프로필 후보를 Slack에서 확인하는 대화는 구현하지 않았다.
- 사람인, 잡코리아, 원티드와 추가 기업 채용 소스는 아직 연결하지 않았다.

### 막힌 점

현재 단계의 기술적 막힌 점은 없다.

### 주요 판단과 이유

- 후보 없음과 분석 실패를 별도 상태로 분리했다.
  - 이유: 관련 없는 공고를 억지로 추천하지 않는 것도 정상적인 Agent 판단이기 때문이다.
- 궁극 목표 진행률을 약 65%로 다시 산정했다.
  - 이유: Slack 연결 자체보다 첨부자료에서 개인 프로필을 만들고 자동 검색까지 잇는 사용자 흐름이 더 큰 완료 기준이기 때문이다.
- 다음 우선순위를 사용자 피드백 입력보다 첨부파일 수신으로 변경했다.
  - 이유: 사용자가 자료 한 개만 보내도 개인화가 시작되는 경험이 이번에 다시 확인한 제품 목표의 핵심이기 때문이다.

### 다음 할 일

1. 실제 Slack 빈 결과 안내를 한 번 확인한다.
2. Slack 첨부파일 한 개의 메타데이터를 안전하게 검증하는 입력 경계를 구현한다.
3. 검증 뒤 비공개 문서 저장소와 기존 프로필 추출 파이프라인을 연결한다.

## 2026-09-15 - Slack 첨부파일 1개 메타데이터 검증

### 목표

사용자가 Slack에서 봇을 호출하고 경력 관련 파일 한 개를 첨부했을 때 파일 내용을 내려받기 전에 안전한 메타데이터만 검증한다.

### 수행한 작업

- 사용자가 실제 Slack 스레드에서 새 공고 없음 안내를 확인했다.
- 해당 실제 요청이 인증된 이벤트이며 실행 상태가 `no_eligible_candidate`, 분석 참조가 없음, 수신기 오류가 없음을 확인했다.
- 공식 Slack 문서에서 `app_mention` 이벤트의 선택적 `files` 배열과 파일 객체 구조를 확인했다.
- 공식 Slack 문서에서 파일 정보 조회와 다운로드에 Bot Token의 `files:read` 권한이 필요함을 확인했다.
- `프로필 분석해줘` 명령 또는 첨부파일 1개와 봇 호출만 있는 입력을 프로필 자료 제출로 식별했다.
- Slack 파일 ID, 안전한 파일명, 허용 확장자와 MIME 형식, 10MB 이하 크기, Slack-hosted 여부를 검증했다.
- 메시지 원문, 파일 내용과 `url_private` 다운로드 URL을 요청 기록에 저장하지 않도록 했다.
- 파일이 없거나 여러 개인 경우와 다른 명령에 파일이 붙은 경우를 별도 안내로 구분했다.
- 코드 변경 전에 이전 Socket Mode 수신기를 종료했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_events.py`
- `src/career_agent/interfaces/slack_socket.py`
- `scripts/run_slack_socket.py`
- `tests/test_slack_events.py`
- `tests/test_slack_socket.py`
- `docs/SLACK_INTERFACE.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 정상 PDF와 Markdown 파일, 봇 호출만 있는 첨부 입력, 파일 누락·복수 파일과 위험 메타데이터 테스트가 통과했다.
- 저장된 합성 요청에 파일 내용과 다운로드 URL이 없음을 확인했다.
- 첨부파일 입력은 내부 실행 동작으로 연결되지 않고 `input_validated` 상태만 남긴다.
- 전체 단위 테스트 246개가 통과했다.

### 아직 확인하지 못한 점

- 실제 Slack 첨부 이벤트에 예상한 메타데이터 필드가 모두 전달되는지 확인하지 않았다.
- Bot Token에 `files:read` 권한이 없으므로 파일 내용은 다운로드할 수 없다.
- 파일 내용 검증, 비공개 원본 저장과 프로필 후보 추출은 아직 연결하지 않았다.
- 전달된 텍스트의 `현재` 첫 글자가 HTML 문자 참조로 보인 현상이 실제 Slack 화면에서도 같은지는 확인하지 않았다.

### 막힌 점

메타데이터 단계에는 기술적 막힌 점이 없다. 실제 파일 다운로드 단계에는 Slack 앱의 `files:read` 권한 추가와 사용자 재승인이 필요하다.

### 주요 판단과 이유

- 이번 단계에서는 `files:read` 권한을 추가하지 않았다.
  - 이유: 이벤트 메타데이터 경계를 먼저 검증하고 실제 파일 접근은 별도 단계로 분리해 최소 권한을 유지하기 위해서다.
- 외부 드라이브와 Slack Connect의 추가 확인 파일을 지원하지 않았다.
  - 이유: 인증과 접근 경계가 다른 입력을 첫 구현에 섞지 않기 위해서다.
- 실제 이력서 대신 비민감 합성 파일로 먼저 시험한다.
  - 이유: 다운로드와 저장이 아직 구현되지 않은 단계에서 개인 원문을 불필요하게 노출하지 않기 위해서다.

### 다음 할 일

1. 최신 수신기를 실행하고 비민감 합성 파일 1개로 실제 메타데이터 이벤트를 확인한다.
2. 확인 후 manifest에 `files:read`를 추가하고 사용자가 앱 권한을 다시 승인한다.
3. Slack 인증 다운로드를 비공개 문서 저장소에 연결한다.

## 2026-09-15 - Slack 인증 파일 다운로드와 비공개 저장 준비

### 목표

실제 Slack에서 검증된 첨부파일을 인증된 경로로만 내려받아 기존 비공개 사용자 문서 저장소에 연결할 수 있는 최소 구현을 준비한다.

### 수행한 작업

- 사용자가 실제 Slack에서 DOCX 첨부파일 메타데이터 검증 응답을 확인했다.
- 최신 비공개 요청의 라우팅 상태, 명령, MIME 형식과 처리 상태만 확인하고 메시지 원문과 파일 내용은 열지 않았다.
- 실제 요청이 `input_validated`, `submit_profile_document`, `metadata_validated_download_not_started`이며 수신기 오류가 없음을 확인했다.
- Slack 공식 파일 예제에서 TXT가 `text/plain`, `filetype: text`, `mode: snippet`으로 처리될 수 있음을 확인했다.
- TXT와 Markdown은 `hosted`와 `snippet`, PDF와 DOCX는 `hosted`만 허용하도록 메타데이터 검증을 수정했다.
- 최소 Bot Token Scope에 `files:read`를 추가한 예제 manifest를 갱신했다.
- `files.info` 응답을 최초 이벤트의 파일 ID, 이름, 확장자, MIME 형식과 크기에 다시 대조하도록 했다.
- `files.slack.com/files-pri/`의 HTTPS URL만 허용하고 외부 호스트와 리디렉션에는 Bot Token을 보내지 않도록 했다.
- 다운로드 크기와 문서 형식을 다시 검증한 뒤 기존 비공개 문서 저장소에 `slack_attachment` 출처로 저장하도록 연결했다.
- 같은 Slack 이벤트 재전송에는 가져오기를 중복 실행하지 않고, 성공과 실패 모두 파일명이나 내부 경로를 Slack에 노출하지 않는 고정 응답을 사용하도록 했다.
- 소스 수정 전에 실제 Socket Mode 수신기를 종료했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_files.py`
- `src/career_agent/interfaces/slack_events.py`
- `src/career_agent/interfaces/slack_socket.py`
- `src/career_agent/interfaces/__init__.py`
- `scripts/run_slack_socket.py`
- `tests/test_slack_files.py`
- `tests/test_slack_events.py`
- `tests/test_slack_socket.py`
- `tests/test_slack_setup.py`
- `data/slack_app_manifest.example.json`
- `docs/SLACK_INTERFACE.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 실제 Slack DOCX 첨부 이벤트에 필요한 메타데이터가 전달되고 저장되는 것을 확인했다.
- 실제 요청 기록에 파일 내용과 다운로드 URL이 없고 수신기 오류 로그가 비어 있음을 확인했다.
- 합성 DOCX의 `files.info` 조회, 인증 다운로드, 최소 형식 검증과 비공개 저장 테스트가 통과했다.
- 외부 다운로드 URL, `files:read` 누락 응답과 다운로드 크기 변경을 거부하는 테스트가 통과했다.
- 전체 단위 테스트 253개가 통과했다.

### 아직 확인하지 못한 점

- 실제 Slack 앱에 `files:read`를 추가하고 재승인하지 않았다.
- 실제 Slack 파일 다운로드와 `private-data/profile-documents/` 저장은 확인하지 않았다.
- 저장된 DOCX의 본문 추출과 사용자 확인용 프로필 후보 생성은 연결하지 않았다.
- 첨부자료의 종류는 아직 자동 판별하지 않고 `other`로 저장한다.

### 막힌 점

실제 다운로드 검증 전 사용자가 Slack 앱의 Bot Token Scope에 `files:read`를 추가하고 앱을 다시 승인해야 한다.

### 주요 판단과 이유

- 이벤트에 들어 있는 다운로드 URL을 바로 신뢰하지 않고 `files.info`로 다시 조회한다.
  - 이유: 다운로드 직전에 파일 식별 정보와 접근 권한을 다시 확인하기 위해서다.
- HTTP 리디렉션을 따르지 않고 Slack 비공개 파일 호스트만 허용한다.
  - 이유: Bot Token이 외부 호스트로 전달되는 경로를 차단하기 위해서다.
- 다운로드 파일은 우선 `other` 종류와 `stored_unparsed` 상태로 저장한다.
  - 이유: 문서 종류와 실제 경력 사실은 본문 추출과 사용자 확인 전에 임의로 확정하지 않기 위해서다.
- 실제 권한 승인 전에 수신기를 다시 시작하지 않는다.
  - 이유: 권한이 없는 상태에서 실제 첨부 입력을 실패시키거나 사용자에게 완료로 오해될 응답을 주지 않기 위해서다.

### 다음 할 일

1. 사용자가 Slack 앱의 Bot Token Scope에 `files:read`를 추가하고 앱을 다시 승인한다.
2. 최신 수신기를 시작하고 비민감 DOCX 한 개의 실제 비공개 저장을 확인한다.
3. 저장된 DOCX 본문 추출을 검토용 프로필 후보 생성에 연결한다.

## 2026-09-15 - 실제 Slack DOCX 비공개 저장 확인

### 목표

재승인한 `files:read` 권한으로 실제 Slack DOCX 한 개를 내려받아 비공개 문서 저장소에 안전하게 저장하는 전체 경계를 확인한다.

### 수행한 작업

- 사용자가 Slack 앱의 Bot Token Scope에 `files:read`를 추가하고 앱을 다시 승인했다.
- 기존 비공개 요청의 파일 ID로 `files.info` 접근만 확인하고 이때 파일 본문은 다운로드하지 않았다.
- 최신 Socket Mode 수신기를 다시 실행했다.
- 사용자가 새 Slack 메시지에 DOCX 한 개와 `프로필 분석해줘` 명령을 보냈다.
- 메시지 원문과 원본 파일 내용을 열지 않고 최신 요청과 저장 manifest의 상태만 확인했다.

### 생성 또는 수정한 파일

- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/SLACK_INTERFACE.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- `files:read` 권한과 `files.info` 메타데이터 접근이 정상이다.
- 실제 요청은 `input_validated`, `submit_profile_document`로 처리됐다.
- 요청과 저장 manifest의 가져온 시각, DOCX 형식과 28,395바이트 크기가 일치했다.
- 저장 출처는 `slack_attachment`, 처리 상태는 `stored_unparsed`다.
- 요청과 manifest에 파일 내용과 다운로드 URL이 없고 Git 추적 금지 표시가 유지됐다.
- 최신 Socket Mode 수신기가 실행 중이며 오류 로그 크기는 0바이트다.

### 아직 확인하지 못한 점

- 저장된 DOCX 본문은 아직 추출하지 않았다.
- 첨부자료에서 만든 프로필 후보를 Slack에 보여주고 사용자가 승인하는 흐름은 연결하지 않았다.
- 승인된 개인 프로필에서 검색 표현을 다시 만들고 공고 발견을 실행하는 흐름은 연결하지 않았다.

### 막힌 점

현재 단계에는 막힌 점이 없다.

### 주요 판단과 이유

- 원본 DOCX 본문을 직접 열지 않고 manifest의 형식·크기·상태와 수신기 오류만 확인했다.
  - 이유: 이번 완료 기준은 인증 다운로드와 비공개 저장 경계이며 사용자 자료 내용 분석은 다음 단계이기 때문이다.
- Slack 첨부파일 비공개 수신 항목을 완료로 표시하고 궁극적인 MVP 진행률을 약 70%로 갱신했다.
  - 이유: 실제 사용자 흐름에서 메타데이터뿐 아니라 권한 승인, 다운로드와 기존 저장소 연결까지 확인했기 때문이다.

### 다음 할 일

1. 저장된 DOCX에서 텍스트를 추출하되 외부 서비스로 보내지 않는다.
2. 추출 텍스트에서 사실 후보를 만들고 실제 프로필 반영 전 Slack에서 사용자에게 확인받는다.
3. 승인된 프로필로 검색 계획을 갱신해 자동 공고 발견에 연결한다.

## 2026-09-15 - DOCX 로컬 본문 추출

### 목표

Slack에서 비공개 저장한 DOCX의 본문 문단을 외부 서비스 없이 안전하게 읽고, 기존 제목 기반 검토 후보 추출기에 연결한다.

### 수행한 작업

- 실제 Socket Mode 수신기를 중지하고 DOCX 추출기를 구현했다.
- `word/document.xml`의 문단과 표 안 문단을 순서대로 읽고 여러 텍스트 조각을 한 문단으로 결합했다.
- DOCX 본문 XML을 최대 8MB, 최대 20,000개 문단으로 제한했다.
- 암호화된 본문, 잘못된 XML과 DTD·ENTITY 선언을 거부하도록 했다.
- 추출 규칙 버전을 `0.2`로 올리고 DOCX 추출 방식을 `heading_based_docx`로 구분했다.
- 합성 DOCX에서 경력·기술 후보, 원문 문단 순서 근거와 연락처 형태 제외를 검증했다.
- 실제 비공개 저장 DOCX에 추출기를 실행하되 후보 문장, 문서 ID와 파일명은 콘솔에 출력하지 않았다.

### 생성 또는 수정한 파일

- `src/career_agent/profile_input/text_extraction.py`
- `scripts/extract_profile_text.py`
- `tests/test_profile_text_extraction.py`
- `docs/PROFILE_EXTRACTION_SCHEMA.md`
- `docs/PROFILE_DOCUMENT_INGESTION.md`
- `docs/PRD.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- DOCX 추출 테스트를 포함한 전체 단위 테스트 256개가 통과했다.
- 실제 저장 DOCX는 `heading_based_docx` 방식으로 오류 없이 추출됐다.
- 실제 문서에서는 비어 있지 않은 미분류 문단 1개만 확인됐고 검토 후보는 0개였다.
- 추출 결과는 `needs_review`, `profile_updated: false`, `git_tracking_allowed: false` 상태로 비공개 저장됐다.
- 후보 문장과 원본 내용은 콘솔과 작업일지에 기록하지 않았다.

### 아직 확인하지 못한 점

- 실제 이력서처럼 여러 섹션이 있는 DOCX에서 유용한 후보가 생성되는지 확인하지 않았다.
- DOCX 이미지, 머리글, 바닥글, 각주, 주석과 일부 텍스트 상자는 추출하지 않는다.
- Slack 파일 저장 직후 추출기를 자동 실행하고 결과를 스레드에 알리는 흐름은 연결하지 않았다.
- 후보 문장을 Slack에서 사용자에게 보여주고 승인받는 흐름은 연결하지 않았다.

### 막힌 점

현재 단계에는 막힌 점이 없다. 실제 문서의 후보 품질은 구조화된 경력 내용이 있는 자료로 별도 확인해야 한다.

### 주요 판단과 이유

- XML 크기와 문단 수를 제한하고 DTD·ENTITY를 거부했다.
  - 이유: 압축된 DOCX 내부의 과도한 XML 확장과 불필요한 XML 기능을 차단하기 위해서다.
- 섹션 제목이 없는 문장을 경력 사실로 추정하지 않았다.
  - 이유: 현재 규칙은 의미 추론을 하지 않으며 사용자 확인 전 허위 프로필 사실을 만들면 안 되기 때문이다.
- 실제 문서에서 후보가 0개여도 추출 성공과 후보 품질을 구분했다.
  - 이유: 문서를 기술적으로 읽은 결과와 현재 규칙으로 분류할 수 있는 정보의 존재는 서로 다른 문제이기 때문이다.

### 다음 할 일

1. Slack 첨부파일 저장 직후 DOCX 추출을 자동 실행한다.
2. 후보가 없으면 그 사실을 정상 결과로 안내하고 실제 프로필을 변경하지 않는다.
3. 후보가 있으면 개수와 영역 요약만 먼저 Slack에 전달하고 개별 후보 검토 흐름을 별도로 구현한다.

## 2026-09-15 - 채용공고 정보 충분도 판정

### 목표

사용자 적합도와 별개로 채용공고 자체가 비교 가능한 정보를 충분히 제공하는지 판정한다.

### 수행한 작업

- 기존 매칭, 추천, Greenhouse 구조화와 Slack 출력 흐름을 대조했다.
- 공고의 명시된 필드만 검사하는 정보 충분도 판정기를 추가했다.
- 주요 업무와 필수 조건이 모두 없으면 `insufficient`, 일부 핵심 정보가 없으면 `partial`, 핵심 두 영역과 근무 조건이 명시되면 `sufficient`로 판정하도록 했다.
- 우대사항 부재는 공고 작성 방식일 수 있으므로 충분도 하락 조건에서 제외했다.
- 판정 결과를 기존 매칭 결과의 `job_posting_information`에 독립적으로 추가했다.
- 상세 공고, 일부 누락 공고, 정보 부족 공고와 잘못된 구조를 단위 테스트로 검증했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/job_posting_information.py`
- `src/career_agent/matching/service.py`
- `src/career_agent/matching/__init__.py`
- `tests/test_job_posting_information.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `docs/MATCHING_RULES.md`
- `작업일지.md`

### 확인된 결과

- 새 정보 충분도 테스트 5개가 통과했다.
- 전체 단위 테스트 261개가 통과했다.
- 상세 예제 공고는 `sufficient`, 주요 업무만 빠진 공고는 `partial`, 주요 업무와 필수 조건이 모두 빠진 공고는 `insufficient`로 판정됐다.
- 기존 예제의 지원 판단은 `적극 지원`으로 유지되어 기존 기능과 호환된다.

### 아직 확인하지 못한 점

- `insufficient`를 최종 `HOLD` 판단에 연결하지 않았다.
- `RECOMMEND`, `HOLD`, `NOT_RECOMMEND` 표준 상태를 기존 한국어 판단과 함께 제공하지 않는다.
- Slack 응답에 공고 정보 충분도와 표준 추천 상태를 표시하지 않는다.
- 실제 Greenhouse 공고 표본별 충분도 분포는 확인하지 않았다.

### 막힌 점

현재 단계에는 막힌 점이 없다.

### 주요 판단과 이유

- 충분도를 적합도 점수와 분리했다.
  - 이유: 상세 정보가 없는 공고를 사용자에게 부적합하다고 오판하지 않기 위해서다.
- 주요 업무와 필수 조건이 모두 없는 경우만 즉시 `insufficient`로 분류했다.
  - 이유: 한 영역만 인식되지 않은 공고는 남은 명시 정보로 제한적인 비교가 가능하기 때문이다.
- 프로필에 증거가 없다는 사실을 이번 공고 충분도 판정에 사용하지 않았다.
  - 이유: 사용자 정보 부족과 공고 정보 부족은 서로 다른 문제이기 때문이다.

### 다음 할 일

1. 확인된 지원 불가나 필수 역량 부족은 `NOT_RECOMMEND`, 정보 부족과 미확인은 `HOLD`, 충분한 근거의 일치는 `RECOMMEND`로 표준 상태를 추가한다.
2. `insufficient` 공고가 근거 없이 추천되지 않는 통합 테스트를 추가한다.
3. Slack 응답에 공고 정보 충분도, 확인된 일치, 확인된 부족, 공고·사용자 미확인을 구분해 표시한다.

## 2026-09-15 - 표준 추천 상태와 정보 부족 우선 규칙

### 목표

최종 추천을 `RECOMMEND`, `HOLD`, `NOT_RECOMMEND`로 표준화하고 정보가 부족한 공고를 근거 없이 추천하지 않는다.

### 수행한 작업

- 기존 한국어 `decision`을 유지하면서 자동 처리용 `status`를 추가했다.
- `partial` 또는 `insufficient` 공고는 확인된 불일치가 없는 경우 `판단 보류`, `HOLD`로 처리했다.
- 확인된 지원 불가 조건이나 필수 역량 부족은 `NOT_RECOMMEND`로 처리하고 공고 정보 부족보다 우선하도록 했다.
- `reasons`의 첫 항목을 `recommendation_reason`으로도 제공해 핵심 판단 근거를 바로 사용할 수 있게 했다.
- 상세, 정보 부족, 명확한 필수 역량 불일치의 세 대표 사례를 통합 테스트로 검증했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/recommendation.py`
- `src/career_agent/matching/service.py`
- `tests/test_application_recommendation.py`
- `tests/test_job_posting_information.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `docs/MATCHING_RULES.md`
- `작업일지.md`

### 확인된 결과

- 상세 예제 공고는 `RECOMMEND`다.
- 주요 업무와 필수 조건이 없는 공고는 적합성을 추정하지 않고 `판단 보류`, `HOLD`다.
- 상세 공고의 Python 필수 조건과 사용자 증거가 명확히 불일치하면 `NOT_RECOMMEND`다.
- 전체 단위 테스트 263개가 통과했다.

### 아직 확인하지 못한 점

- Slack 응답은 아직 표준 상태와 공고 정보 충분도를 표시하지 않는다.
- `strengths`를 요구사항 명칭 중심의 `confirmed_matches`로 별도 제공하지 않는다.
- 공고에서 빠진 정보와 사용자 자료에서 확인되지 않은 정보를 Slack에서 별도 소제목으로 나누지 않는다.

### 막힌 점

현재 단계에는 막힌 점이 없다.

### 주요 판단과 이유

- 기존 `decision`을 제거하지 않고 `status`를 추가했다.
  - 이유: 기존 저장 결과와 Slack 표시를 깨지 않으면서 새 요구사항의 세 가지 상태를 안정적으로 제공하기 위해서다.
- 확인된 불일치는 정보 부족보다 우선하도록 했다.
  - 이유: 공고의 다른 정보가 부족하더라도 명시된 필수 조건과의 불일치는 실제 확인된 판단 근거이기 때문이다.
- 단순히 사용자 문서에 기술이 없다는 이유로 `NOT_RECOMMEND`를 만들지 않았다.
  - 이유: 사용자 근거 부재는 실제 역량 부재가 아니라 `unknown`일 수 있기 때문이다.

### 다음 할 일

1. Slack 결과에 공고 정보 충분도와 표준 추천 상태를 표시한다.
2. 사용자에게 `확인된 일치`, `확인된 부족`, `공고에서 확인 불가`, `사용자 자료에서 확인 불가`를 구분해 보여준다.
3. 실제 Slack 호출로 세 유형 중 최소 한 유형을 확인한다.

## 2026-09-15 - MVP 최종 결과 필드 정렬

### 목표

재정의된 MVP가 요구한 핵심 결과 필드를 기존 매칭 결과에 호환 방식으로 제공한다.

### 수행한 작업

- `jd_information_level`, `confirmed_matches`, `recommendation`, `recommendation_reason`을 최상위 결과에 추가했다.
- `confirmed_matches`에는 `strong_match`와 `match` 판정이면서 사용자 근거가 실제로 있는 항목만 포함했다.
- 각 확인된 일치에 공고 영역, 조건명, 판정, 공고 원문 근거와 사용자 근거를 함께 저장했다.
- 기존 `job_posting_information`, `strengths`, `application_recommendation`은 호환성을 위해 유지했다.

### 생성 또는 수정한 파일

- `src/career_agent/matching/service.py`
- `tests/test_requirement_matching_service.py`
- `tests/test_job_posting_information.py`
- `docs/MATCH_RESULT_SCHEMA.md`
- `작업일지.md`

### 확인된 결과

- 상세 예제 결과에서 Python과 LLM API의 확인된 일치가 사용자 증거와 함께 생성됐다.
- `gap`인 Docker는 `confirmed_matches`에 포함되지 않았다.
- 정보 부족 공고도 명시된 LLM API 우대사항의 일치는 보존하지만 최종 추천은 `HOLD`로 유지했다.
- 전체 단위 테스트 263개가 통과했다.

### 아직 확인하지 못한 점

- Slack 화면은 새 핵심 결과 필드를 직접 표시하지 않는다.
- 공고의 누락 필드와 사용자 근거 미확인을 Slack에서 별도 영역으로 표시하지 않는다.
- 실제 Slack 분석 결과 파일이 새 필드를 포함하는지는 수신기 재시작 후 확인해야 한다.

### 막힌 점

현재 단계에는 막힌 점이 없다.

### 주요 판단과 이유

- `confirmed_matches`와 `strengths`를 별도 유지했다.
  - 이유: 전자는 일치한 공고 조건 목록이고 후자는 여러 조건을 뒷받침하는 사용자 강점 요약이기 때문이다.
- 정보 부족 공고의 개별 확인 사실을 제거하지 않았다.
  - 이유: 전체 추천을 보류하는 것과 실제 확인된 우대 조건 일치를 기록하는 것은 모순이 아니기 때문이다.
- 새 핵심 필드는 기존 중첩 객체의 값을 그대로 참조하도록 했다.
  - 이유: 인터페이스 사용을 단순하게 하면서 기존 결과 구조와 의미가 갈라지는 것을 방지하기 위해서다.

### 다음 할 일

1. Slack 결과에 공고 정보 충분도와 `RECOMMEND`, `HOLD`, `NOT_RECOMMEND`를 표시한다.
2. `confirmed_matches`, `gaps`, 공고 누락 필드와 사용자 미확인을 구분해 요약한다.
3. 수신기를 새 코드로 다시 시작하고 실제 호출 결과를 확인한다.

## 2026-09-15 - Slack 근거 중심 결과 표시

### 목표

재정의된 MVP의 공고 정보 수준과 비교 결과 구분을 실제 Slack 응답 형식에 반영한다.

### 수행한 작업

- Slack 성공 응답에 공고 정보 수준과 표준 추천 상태를 추가했다.
- 기존 `확인된 강점` 표시를 공고 조건 중심의 `확인된 일치`로 교체했다.
- 확인된 부족 또는 불일치, 공고 누락 정보, 비교를 위해 추가 확인할 정보를 별도 영역으로 나눴다.
- 확인된 일치에는 필수·주요 업무·우대 영역, 조건명과 사용자 근거를 표시하도록 했다.
- 출력 항목 수와 문자열 길이 제한, HTTPS 원문 링크 검증과 Slack 제어문자 변환은 유지했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_actions.py`
- `tests/test_slack_actions.py`
- `docs/SLACK_INTERFACE.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- Slack 출력 단위 테스트 7개가 통과했다.
- 전체 단위 테스트 263개가 통과했다.
- 부분 정보 공고 예제에서 `일부 부족 (partial)`, `HOLD`, 확인된 필수 조건 일치, 공고의 주요 업무·고용 형태 누락이 서로 다른 영역으로 표시됐다.
- 기존 회사명·공고명·원문 링크·다음 행동·검토 큐 표시는 유지됐다.
- 변경 내용을 커밋 `422bb89`로 원격 `main`에 반영했다.
- 기존 수신기가 이 저장소의 `scripts/run_slack_socket.py`임을 확인한 뒤 새 코드로 교체했다.
- 새 Socket Mode 수신기는 실행 중이며 시작 직후 표준 오류 로그 크기는 0바이트다.

### 아직 확인하지 못한 점

- 실제 Slack 메시지에서 새 응답 형식을 사용자가 확인하지 않았다.
- 현재 검토 큐에 새 미분석 공고가 없으면 실제 성공 응답 대신 기존 빈 결과가 표시될 수 있다.
- 공고 누락 정보와 비교 미확인 항목의 일부 의미가 실제 공고에서 겹칠 수 있다.

### 막힌 점

현재 단계에는 막힌 점이 없다.

### 주요 판단과 이유

- 사용자 강점 요약 대신 공고 조건별 확인된 일치를 Slack의 기본 표시로 선택했다.
  - 이유: 사용자는 추천의 근거가 된 공고 조건과 실제 증거를 바로 대조할 수 있어야 하기 때문이다.
- 공고 누락과 비교 미확인을 별도 영역으로 표시했다.
  - 이유: 공고가 말하지 않은 정보와 사용자 자료에서 확인되지 않은 정보를 같은 부족으로 오해하지 않도록 하기 위해서다.
- 내부 상세 결과와 로컬 경로는 계속 Slack에 노출하지 않았다.
  - 이유: 개인 자료와 내부 실행 정보의 공개 범위를 최소화하기 위해서다.

### 다음 할 일

1. 사용자가 `@career_break 다음 공고 찾아줘`를 보내 새 응답 또는 정상 빈 결과를 확인한다.
2. 실제 상세 분석 가능한 공고가 없으면 비공개 고정 입력으로 Slack 렌더링을 확인하는 별도 개발 명령은 다음 단계에서 검토한다.
3. 첨부자료에서 만든 프로필 후보를 Slack에서 확인하고 적용하는 흐름을 이어서 구현한다.

## 2026-09-15 - Slack 오래된 검토 큐 자동 복구

### 목표

매칭 규칙 변경 후 기존 검토 큐 때문에 실제 Slack 분석이 실패하는 문제를 복구하고 재발을 방지한다.

### 수행한 작업

- 실제 Slack 실패 시각의 수신기 로그와 실행 이력을 확인했다.
- 최신 실패 실행 이력에서 `매칭 규칙이 바뀌었음. 검토 큐를 다시 생성해야 함` 오류를 확인했다.
- 기존 발견 결과를 사용해 새 검토 큐를 생성하고 공고 한 건을 로컬에서 분석했다.
- Slack 실행 경계에 오래된 큐 오류 전용 자동 복구를 추가했다.
- 고정된 큐 생성 스크립트를 실행한 뒤 분석을 한 번만 재시도하도록 제한했다.
- 다른 실패, 큐 재생성 실패와 과도한 출력은 기존처럼 안전한 고정 오류로 처리했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_actions.py`
- `tests/test_slack_actions.py`
- `docs/SLACK_INTERFACE.md`
- `작업일지.md`

### 확인된 결과

- 기존 수신기의 표준 오류 로그는 0바이트였고 Slack 연결은 유지됐다.
- 실패 원인은 Slack 권한이나 네트워크가 아니라 기존 큐의 매칭 규칙 버전 불일치였다.
- 새 큐에는 검토 후보 4개가 포함됐고 로컬 상세 분석 한 건이 성공해 3개가 남았다.
- 실제 새 분석 결과는 `sufficient`, `HOLD`, 확인된 일치 3개, 확인된 부족 0개이며 핵심 추천 이유가 존재한다.
- 실제 분석 결과로 Slack 메시지 렌더링을 실행했고 필수 표시 영역이 모두 생성됐다.
- 오래된 큐 자동 복구 테스트를 포함한 Slack 테스트 8개와 전체 단위 테스트 264개가 통과했다.
- 자동 복구 코드를 커밋 `99eb581`로 원격 `main`에 반영했다.
- 최신 코드로 Socket Mode 수신기를 다시 시작했고 시작 직후 오류 로그는 0바이트다.

### 아직 확인하지 못한 점

- 자동 복구 코드가 반영된 실제 Slack 메시지는 사용자가 아직 확인하지 않았다.
- 남은 후보의 상세 공고 정보 수준과 추천 상태는 아직 분석하지 않았다.

### 막힌 점

현재 단계에는 막힌 점이 없다.

### 주요 판단과 이유

- 큐 버전 검사를 제거하지 않았다.
  - 이유: 오래된 프로필이나 규칙으로 만든 후보를 그대로 분석하는 것을 막는 안전장치는 필요하기 때문이다.
- 명시적인 오래된 큐 오류에서만 자동 재생성하도록 했다.
  - 이유: 네트워크, 데이터 구조 또는 권한 오류까지 무조건 재시도하면 실제 원인을 감추고 중복 실행할 수 있기 때문이다.
- 자동 재시도를 한 번으로 제한했다.
  - 이유: 반복 실패 시 무한 실행과 외부 API 중복 호출을 방지하기 위해서다.

### 다음 할 일

1. 사용자가 Slack에서 다음 공고 요청을 다시 보내 실제 새 결과 형식을 확인한다.
2. 실제 확인 후 첨부자료 후보 승인과 개인 프로필 적용 흐름을 이어서 구현한다.

## 2026-09-15 - 최신 발견 스냅샷과 빈 큐 표시 수정

### 목표

공식 소스를 갱신한 뒤에도 검토 큐가 과거 목록을 사용하는 문제를 수정하고 빈 결과의 큐 숫자를 정확히 설명한다.

### 수행한 작업

- 최신 검토 큐의 후보별 분석 상태, 지역 판정과 고용 형태 판정을 확인했다.
- 미분석 3건이 모두 지역 또는 고용 형태 불일치로 제외된 사실을 확인했다.
- 빈 결과 Slack 메시지에서 분석 완료, 조건 불일치 제외, 현재 분석 가능 개수를 구분했다.
- Greenhouse 공식 API 실행에서 기존 분석을 재사용하거나 high 후보가 없어도 최신 발견 스냅샷을 비공개 실행 경로에 저장하도록 했다.
- 발견 스냅샷에는 프로필 원문과 공고 상세 분석을 복제하지 않도록 했다.
- 실제 공식 Greenhouse 조회와 새 큐 생성을 다시 실행했다.

### 생성 또는 수정한 파일

- `src/career_agent/interfaces/slack_actions.py`
- `scripts/run_greenhouse_agent.py`
- `tests/test_slack_actions.py`
- `tests/test_greenhouse_agent_script.py`
- `docs/SLACK_INTERFACE.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 전체 단위 테스트 265개가 통과했다.
- 실제 공식 Greenhouse 보드 조회는 성공 1개, 실패 0개다.
- 최신 발견 스냅샷이 새 파일로 저장됐다.
- 새 검토 큐가 전날 분석 파일이 아니라 방금 생성한 발견 스냅샷을 출처로 사용했다.
- 현재 공고 후보는 2개이며 기존 분석 완료 1개, 고용 형태 불일치 1개, 현재 분석 가능 0개다.
- 변경 내용을 커밋 `f8ff2f1`로 원격 `main`에 반영하고 Socket Mode 수신기를 최신 코드로 다시 시작했다.
- 새 수신기의 시작 직후 오류 로그는 0바이트다.

### 아직 확인하지 못한 점

- 수정된 빈 결과 큐 문구는 실제 Slack에서 사용자가 아직 확인하지 않았다.
- Slack의 다음 공고 명령은 빈 큐일 때 공식 소스 갱신을 자동 실행하지 않는다.
- 현재 등록된 Greenhouse 보드는 한 개라 자동 발견 범위가 좁다.

### 막힌 점

현재 등록된 공식 Greenhouse 소스에서는 새로 분석 가능한 공고가 없다.

### 주요 판단과 이유

- `분석 필요`를 `미분석이지만 조건 불일치`로 바꿨다.
  - 이유: 분석하지 않은 공고와 실제로 다음 분석이 가능한 공고는 같은 뜻이 아니기 때문이다.
- 재사용 결과에서도 발견 스냅샷을 별도로 저장했다.
  - 이유: 상세 분석 재사용 여부와 현재 공고 목록의 갱신 여부는 독립적이기 때문이다.
- 발견 스냅샷에 상세 분석을 복제하지 않았다.
  - 이유: 큐 생성에는 목록 정보만 필요하며 개인 근거가 포함된 결과 복제를 줄이기 위해서다.

### 다음 할 일

1. 빈 큐에서 `다음 공고 찾아줘`가 공식 소스를 한 번 갱신한 뒤 다시 검사하도록 연결한다.
2. 정책 확인이 끝난 공식 ATS 보드를 한 곳씩 추가해 발견 범위를 넓힌다.

## 2026-09-15 - 빈 큐 공식 소스 자동 갱신

### 목표

Slack의 `다음 공고 찾아줘`가 빈 검토 큐만 반복 확인하지 않고 허용된 공식 소스를 한 번 갱신한 뒤 다시 검사한다.

### 수행한 작업

- 외부 공개 프로젝트 `NomaDamas/k-skill`의 `job-posting-match` 문서, 지침, 구현과 테스트를 비교했다.
- 집중 검색 표현, 소스별 실패 격리와 빈 결과 후 재검색 원칙만 참고하기로 했다.
- 이용약관 검토 전 잡코리아·사람인 검색 HTML 직접 수집과 목록 요약 기반 100점 점수는 도입하지 않았다.
- Greenhouse 워크플로에 상세 공고를 읽지 않는 `discovery_only` 모드를 추가했다.
- 발견 전용 실행 상태와 비공개 실행 이력을 추가했다.
- Slack 첫 큐 검사에서 후보가 없으면 공식 목록 갱신, 새 큐 생성, 다음 후보 재검사를 한 번만 수행하도록 연결했다.
- 소스 갱신 실패는 기존 빈 결과와 구분하되 외부 오류 내용을 Slack에 노출하지 않도록 했다.

### 생성 또는 수정한 파일

- `src/career_agent/workflows/greenhouse_agent.py`
- `src/career_agent/execution/log.py`
- `scripts/run_greenhouse_agent.py`
- `src/career_agent/interfaces/slack_actions.py`
- `tests/test_greenhouse_agent_workflow.py`
- `tests/test_execution_log.py`
- `tests/test_slack_actions.py`
- `docs/JOB_DISCOVERY_PLAN.md`
- `docs/SLACK_INTERFACE.md`
- `README.md`
- `작업일지.md`

### 확인된 결과

- 전체 단위 테스트 269개가 통과했다.
- 실제 `--discovery-only` 실행에서 공식 Greenhouse 보드 조회 성공 1개, 실패 0개를 확인했다.
- 발견 스냅샷과 실행 이력이 생성됐고 상세 분석 파일 수는 실행 전후 12개로 동일했다.
- 실제 Slack 동작 함수는 빈 큐에서 공식 목록을 갱신한 뒤 `no_candidate`를 반환했다.
- 최종 메시지에 공식 채용 소스를 방금 갱신했다는 사실이 포함됐다.

### 아직 확인하지 못한 점

- 자동 갱신 흐름을 실제 Slack 메시지에서 사용자가 확인하지 않았다.
- 현재 등록된 Greenhouse 공식 보드는 한 개라 갱신 후에도 분석 가능한 새 후보가 없다.
- 인크루트 RSS 갱신은 같은 Slack 명령에 아직 연결하지 않았다.

### 막힌 점

현재 등록된 공식 Greenhouse 소스에는 사용자 조건에 맞는 새 미분석 공고가 없다.

### 주요 판단과 이유

- 공개 프로젝트에서는 구현 코드가 아니라 검색과 실패 처리 원칙만 참고했다.
  - 이유: CareerBreak의 근거 중심 상세 분석과 정책 우선 수집 경계를 유지하기 위해서다.
- 목록 갱신과 상세 분석을 분리했다.
  - 이유: 빈 큐를 확인하는 과정에서 사용자에게 보여주지 않을 공고 상세 분석을 먼저 소비하지 않기 위해서다.
- 갱신과 재검사를 한 번으로 제한했다.
  - 이유: 새 공고가 없을 때 반복적인 외부 요청과 무한 재시도를 방지하기 위해서다.

### 다음 할 일

1. 변경 내용을 커밋하고 Socket Mode 수신기를 최신 코드로 다시 시작한다.
2. 실제 Slack에서 빈 큐 자동 갱신 안내를 확인한다.
3. 첨부자료 후보 승인과 실제 개인 프로필 적용 흐름을 구현한다.
